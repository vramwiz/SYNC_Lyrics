# SYNC_Lyrics 設計仕様

確定済みの製品仕様と設計判断を置く。通常は作業対象に関係する節だけを参照する。
現在の開発段階と次の作業は `note.md`、過去の経緯は `HISTORY.md` を参照する。

## 兄妹プラグイン

Syncroh2から音楽関係の機能を分離し、次の3製品とする。共通接頭辞の `SYNC_` はシリーズ名であると同時に、
音楽との同期も表す。

| 製品名 | 日本語名称 | 役割 |
| --- | --- | --- |
| `SYNC_Motion` | 音楽同期アニメーション | 音楽に合わせて画像等を縦・横に揺らす |
| `SYNC_Lyrics` | 歌詞テロップ | カラオケやMV用の歌詞を描画する |
| `SYNC_PianoRoll` | ピアノロール | 音楽に同期したピアノロールを描画する |

## 実装参照元

入力、フィルター、歌詞データ管理、時間同期、描画処理を検討するときは、次を参照する。

- `D:\DelphiProg\test\SYNC_PianoRoll`
- `D:\DelphiProg\test\Syncroh2\Syncroh2_Filter_LyricTelop.dpr`
- `D:\DelphiProg\test\Syncroh2\Syncroh2_Extension2.dpr`
- `D:\DelphiProg\test\Syncroh2\Plugin_Filter\LyricTelop`
- `D:\DelphiProg\test\Syncroh2\Plugin_Extension\Music\LyricTelop`

`SYNC_PianoRoll`からは現在のAviUtl2 Input／Filter境界、仮想入力、共有メモリ、並列呼び出し対策を
参照する。Syncroh2からは歌詞テロップ固有のデータ、同期、描画、拡張機能の責務を確認する。
旧構成をそのままコピーせず、SYNC_Lyricsに必要な依存だけを採用する。

## AviUtl2上の識別名

表示名は内部の識別やAPIの引数にも使われるため、一般的な日本語名称だけにはしない。
空白や装飾記号を避け、次の形式を固定識別名として使う。

```text
SYNC_<日本語機能名>_<Input|Filter>
```

SYNC_Lyricsでは次を使用する。

| 種類 | 識別名 |
| --- | --- |
| 入力プラグイン | `SYNC_歌詞テロップ_Input` |
| フィルタープラグイン | `SYNC_歌詞テロップ_Filter` |
| フィルターグループ名 | `SYNC` |

- 識別名はエイリアス、設定取得・書き戻し、既存プロジェクトの互換性へ影響するため、公開後は変更しない。
- Delphiプロジェクト名と配布ファイル名にはASCIIの `SYNC_Lyrics_Input` と
  `SYNC_Lyrics_Filter` を使う。
- GUI項目名もAPIの引数に使う可能性があるため、確定後の変更は互換性を確認してから行う。

## 目的

1曲分の歌詞を1つのFilterオブジェクトへ保持し、内部の行データとして入力、同期、配置、描画する。
1行1オブジェクト方式は採用しない。各行は安定した行ID、表示段、同期範囲、同期データ、配置データを
持ち、必要な項目だけを曲共通設定から上書きできるようにする。

同期計算と描画方法を分離し、同じ消費結果を複数の表示タイプで利用できる構造を目指す。

## 打ち合わせで確定した基本方針

- 完成品はWAV、専用Input、専用Filterの3オブジェクトを3レイヤーで使用する。
- 専用Inputと専用Filter側の描画オブジェクトは、曲全体を担当する1組としてグループ化して扱う。
- Filterオブジェクトは1曲分の歌詞行配列を保持し、現在フレームで表示対象となる行を内部で選択する。
- 同時表示は表示段1～3の最大3行とし、同じ表示段で重なる場合は表示開始が新しい行を優先する。
- WAVは実際の音を鳴らし、Inputは本来のフレーム位置を渡し、Filterは歌詞を描画する。
- 歌詞の進行は、フレーム位置から得られる音の消費数で決める。
- 同期パラメーター番号は音番号でも文字番号でもなく、同期段階の処理順だけを表す。
- 同期パラメーター値にかかわらず、1段階が終われば必ず番号が1つ大きい次のパラメーターへ進む。
- 各行は同期開始・終了と表示開始・終了をオブジェクト内フレームで保持する。
- 発音前の表示には行別の`事前表示時間`、発音後の表示には行別の`維持時間`を使用する。
- コピーで同じ値が複製される同期識別IDは使用しない。
- 描画方法は `タイプ` で選択できるようにする。
- 標準GUIにはフォント選択機能がないため、専用GUIは残す。

## 完成品のタイムライン構成

完成品は主に3オブジェクト、3レイヤーを使う。

| オブジェクト | 役割 |
| --- | --- |
| WAV音声オブジェクト | 実際に鳴る音声データ |
| `SYNC_Lyrics_Input` の素材オブジェクト | 要求された本来の絶対フレーム位置をFilterへ渡す |
| `SYNC_Lyrics_Filter` を適用した描画オブジェクト | 受け取ったフレーム位置に対応する歌詞を描画する |

InputとFilter側の描画オブジェクトを1曲分の組としてグループ化して配置、移動する。
歌詞の行ごとには分割しない。WAV音声オブジェクトは実音を担当し、このグループには含めない。

Inputはフレーム位置を渡すだけの単純なプラグインとし、利用者向けには必要最小限の説明に留める。

### 利用者の基本編集手順

1. WAV音声オブジェクトを配置する。
2. 専用Inputの素材オブジェクトと、専用Filter側の描画オブジェクトを配置する。
3. InputとFilter側の描画オブジェクトをグループ化する。
4. Filterの曲全体編集画面へ1曲分の歌詞を入力する。
5. 行ごとに表示段と音符対応を設定し、同期範囲を確定する。
6. 行共通の表示設定を整え、必要な行だけ配置を上書きする。
7. プロジェクト保存時は曲全体データをFilter文字列項目として一緒に保存する。

InputとFilter側の描画オブジェクトは、曲全体を通して同じグループとして移動・長さ変更する。

## InputとFilterの同期

Inputは描画内容を判断せず、自分へ要求された本来の絶対フレーム位置を共有メモリへ公開する。
Filterはその位置から、同期すべき音と歌詞の進行状態を求めて描画する。

Inputプラグインの映像キャッシュにより、同じ素材の読込処理が2回目以降に発火せず、共有フレームが
更新されない場合がある。そのためFilterは、最初に受信した共有絶対フレームと、その時点の
Filter側ローカルフレームの対応を記憶する。

- 共有情報が更新された場合は、新しい絶対フレームとローカルフレームで基準を更新する。
- 更新されない間は、記憶した対応関係と現在のローカルフレームとの差から絶対フレームを補間する。
- 新規オブジェクトの基準には直近1秒以内に更新された共有値だけを採用し、過去の値を誤用しない。
- InputとFilterを曲全体でグループ化し、移動後もInputから本来のフレーム位置を得る。
- 複数オブジェクトや複数フィルターの対応関係が混在しないよう、コンテキストを分離する。
- AviUtl2からの並列呼び出しを前提に、共有状態の読み書きを排他する。

コピーすると同じ値が複製され、オブジェクト同士を直接同期させる構造でもないため、利用者が設定する
同期識別IDは設けない。

## 1行の消費モデル

フレーム位置から、その時点までに同期すべき音がいくつ進んだかを取得する。この進行を「音の消費」と呼ぶ。
1行に含まれる表示対象を順番に消費し、担当する歌詞をすべて消費した時点でその行を完了状態とする。

音と表示対象が常に1対1になるとは限らないため、順番に処理する数値パラメーター列を持つ。
各パラメーターの初期値は `0` とし、値の意味は次のとおりとする。

| 値 | その同期段階で消費する内容 |
| ---: | --- |
| `0` | 1音で1表示単位 |
| 正の `n` | `n + 1`音で1表示単位 |
| 負の `n` | 1音で `abs(n) + 1` 表示単位 |

例:

| 値 | 意味 |
| ---: | --- |
| `2` | 3音で1表示単位 |
| `1` | 2音で1表示単位 |
| `0` | 1音で1表示単位 |
| `-1` | 1音で2表示単位 |
| `-2` | 1音で3表示単位 |

### パラメーター番号の重要な規則

`同期1`、`同期2`、…の番号は音番号でも文字番号でもなく、単なる処理順である。値によって消費する
音数や表示単位数が変わっても、1段階が完了した後は必ず次のパラメーターへ進む。

```text
同期1 = -1  → 1音で2表示単位を消費 → 次は同期2
同期2 =  1  → 2音で1表示単位を消費 → 次は同期3
```

処理中は概念上、少なくとも次の3つを独立して進める。

| 状態 | 意味 |
| --- | --- |
| 同期パラメーター位置 | 現在処理している `同期n` の `n` |
| 音消費位置 | 行の開始後、いくつの音を消費したか |
| 表示消費位置 | 歌詞の先頭から、いくつの表示単位を消費したか |

例えば `同期1 = -1` の場合、音消費位置は1、表示消費位置は2進み、次の処理位置は必ず
`同期2` になる。`同期1 = 1` の場合は、音を2つ消費し終えた時点で表示消費位置が1進み、
次の処理位置は `同期2` になる。値を音位置または文字位置への添字として使用してはならない。

歌詞の文字数と同期パラメーター数は一致させない。特に英語では、複数文字または複数単語を1音で
まとめて消費する場合があり、文字数より少ない同期段階で表現できる。1行の日本語歌詞は通常15～25文字、
長くても30文字前後を想定し、内部文字列には余裕を持たせる。

英語では最大級のまとまりとして「4単語を1音で消費」のようなケースも想定する。このため、
同期パラメーターの必要数を歌詞の文字数から決めず、実際の発音上のまとまり数を基準にする。

### 行内の処理状態

| 状態 | 条件 | 基本描画 |
| --- | --- | --- |
| 消費前 | 行の最初の同期音より前 | 全体を変化前状態で表示 |
| 消費中 | 1つ以上の音を消費し、歌詞が残っている | 消費済みと未消費の状態をタイプに従って描き分ける |
| 消費完了 | 歌詞の全文字／全表示単位を消費済み | 全体を変化後状態で表示 |

行の終了判定は「同期パラメーターをすべて読んだか」ではなく、担当する歌詞の表示単位をすべて
消費したかを基本とする。オブジェクト区間が実際の発音より長い場合も、消費完了状態を維持できるようにする。

### 音楽ファイル同期の第1検証

同期データの読込には`SYNC_PianoRoll\Source\Lib\SongReader`と同じソース一式を使用する。
SongReaderが各形式を共通のノート開始・終了秒へ変換し、その開始時刻を音の消費位置として使用する。

| 分類 | 対応拡張子 |
| --- | --- |
| MIDI | `.mid`、`.midi` |
| UTAU | `.ust` |
| VOCALOID | `.vsq`、`.vsqx` |
| MusicXML | `.musicxml`、`.mxl`、`.xml` |
| MuseScore | `.mscx`、`.mscz` |

Filterへ次の設定を追加する。

| 設定 | 意味 |
| --- | --- |
| 音楽ファイル | 同期元のMIDIファイル |
| トラック (-1=全て) | 0始まりのMIDIトラック番号。`-1`は全トラック |

Filterオブジェクト先頭に対応する入力素材の絶対秒を同期開始位置とし、そこから現在の絶対秒までに
開始した対象トラックのノート数を数える。第1検証では同期値をすべて`0`とみなし、1音につき本文の
先頭から1表示単位を変化前色の白から変化後色の水色へ切り替える。

通常文字は1文字を1表示単位とするが、`[漢字](かんじ)`のようなルビ指定は対応本文全体とルビ全体を
合わせて1表示単位とする。本文とルビは同じノート開始・終了時刻から同じ進捗率を求める。ただし横幅は
個別に測り、本文側は本文幅、ルビ側はルビ幅へ進捗率を掛けて、それぞれ左から右へ水色のクリップ領域を
広げる。同じピクセル幅や画面上の同じ境界座標は共有しない。

```text
進捗率 = (現在秒 - ノート開始秒) / (ノート終了秒 - ノート開始秒)
本文境界 = 対応本文の横幅 × 進捗率
ルビ境界 = 対応ルビの横幅 × 進捗率
```

ノート開始時は全体が白、発音中はフレームごとにクリップ境界が進み、ノート終了時は表示単位全体が
水色になる。終了時刻が開始時刻以下など有効な長さを持たないノートは即時完了として扱う。

第1検証時点では同期値をすべて`0`としていた。現在は`同期1`～`同期16`を適用し、16段階を
超えた部分は暗黙の`0`として処理を継続する。表示単位の明示区切り、開始・同期・終了演出はまだ適用しない。

### 同期調整で検証すること

- GUIへ`同期1`～`同期16`を実装した。16段階を超える部分は1音1表示単位で進むため通常行は完了できるが、
  17段階目以降にも個別調整が必要な実データがあるか検証する。
- 「表示単位」をUnicode文字、書記素、歌詞データ内の明示区切り、単語のどれとして扱うか。
- 英語の「4単語を1音」のような指定は負数で複数表示単位をまとめられるが、文字数の指定より
  明示区切りの方が編集しやすいか検証する。
- 終端値は設けず、歌詞の表示単位をすべて消費した時点で完了する。未指定段階は`0`として扱う。
- ルビ対応本文とルビ全体は既に1表示単位として扱う。

## 行区間と表示時間

各行の同期音符から、オブジェクト内フレームによる同期範囲と表示範囲を生成して曲全体データへ保持する。
毎フレームの描画では再計算せず、保存済み範囲の判定だけを行う。

- `同期開始`は、その行へ割り当てた最初の音符の開始位置とする。
- `同期終了`は、その行が消費する最後の音符の終了位置とする。
- `表示開始 = 同期開始 - 事前表示時間`とし、オブジェクト先頭より前は0へ制限する。
- `表示終了 = 同期終了 + 維持時間`とし、この位置を越えた行は描画しない。
- 同期開始より前は未消費、同期終了以後は全消費として描画状態を固定する。
- 音数不足で同期終了を求められない行は範囲未確定とし、警告対象にする。
- 同じ表示段で表示範囲が重なる場合は、表示開始が新しい行を優先する。

開始演出と終了演出は、各行の表示開始と表示終了からの相対時間として扱う。

## Filterの設定要素

旧方式の設定をそのまま移植するのではなく、曲全体方式で共通設定と行別設定へ分類する。

### Filter標準画面へ残す項目

| 要素 | 用途 |
| --- | --- |
| 曲全体データ | 行配列、同期範囲、同期データ、行別配置を保存する文字列 |
| 音楽・同期データファイル | フレーム位置から音の進行を取得する元データ |
| 配置モード | 1行配置または文字自由配置を選ぶ |
| 表示設定 | 配置モード別の専用GUIを開く |
| 同期演出 | 消費の進行に伴う文字演出 |
| 同期アニメーション | 同期中の補助アニメーション |
| 開始演出／時間 | 行の表示開始時の演出 |
| 終了演出／時間 | 行の表示終了時の演出 |
| 事前表示 (秒) | 新しい行が継承する既定の事前表示時間 |
| 同期 | 曲全体の歌詞入力、行管理、音と表示単位の対応を専用GUIで編集する |
| 表示設定データ | 表示GUIの結果を保存する文字列 |

### 表示設定データへ格納する項目

`X`、`Y`、本文・ルビのフォント名、太字、斜体、下線、取り消し線、同期前色、同期後色、
本文・ルビサイズ、ルビ間隔補正、本文字間、ルビ字間はFilterの個別項目を持たず、
バージョン付きの`表示設定`文字列へ行共通設定として保存する。文字自由配置の個別配置と
個別書式も同じ文字列へ保存し、未指定値は文字列内の行共通設定を継承する。

縁取り、影等の文字装飾はSYNC_Lyricsへ重複実装せず、AviUtl2の既存Filterを後段へ適用して構成する。
これにより歌詞同期と文字装飾の責務を分離し、AviUtl2側で装飾の種類、強さ、重ね順を編集できるようにする。

### 表示プリセット

別プロジェクトや例外的に配置する複数の曲全体オブジェクトへ同じ表示設定を反映するため、
Filter標準画面の`表示設定`直下へ
`プリセット1`～`プリセット10`の選択リストと`保存`、`読込`ボタンを置く。

- 保存先は`マイドキュメント\SYNC_Lyrics`固定とする。
- 選択値0～9をそのままファイル名にした`0.slpreset`～`9.slpreset`を使用する。
- 保存は確認なしで上書きし、保存・読込の失敗は表示せず現在値を維持する。
- 本文・ルビのフォント、サイズ、装飾、同期前後色、字間、ルビ間隔、同期演出、
  同期アニメーション、開始・終了演出と時間を保存する。
- オブジェクト座標、文字自由配置の個別配置、歌詞、音楽・同期データ、配置モード、
  事前表示時間は保存しない。
- 読込時は対象オブジェクトが保持している座標と文字自由配置を維持する。

### 削除または不要候補

| 要素 | 理由 |
| --- | --- |
| 行高 | 表示段の標準配置と行別配置で扱うため、独立したFilter項目は不要 |
| 同期識別ID／更新日時ID | コピーで複製され、今回の同期方式では識別に使わない |

### 旧方式からの移行整理

| 旧方式の要素 | 新方式での扱い |
| --- | --- |
| 歌詞テロップファイル | 外部ファイルを使わず、曲全体データをFilter文字列項目へ保存 |
| フォント選択 | 専用GUIに残す |
| フォント名 | 専用GUIと表示設定データへ移す |
| 変化前色／変化後色 | 専用GUIと表示設定データへ移す |
| 歌詞サイズ／ルビサイズ | 専用GUIと表示設定データへ移す |
| 行高 | 原則削除 |
| 表示先行／表示維持 | 行別の事前表示時間と維持時間として曲全体データへ保存 |
| 同期演出 | 残す |
| 開始演出／終了演出 | 残す |
| 更新日時等の識別値 | 同期識別には使わない |

### 歌詞入力の第1検証

専用構文と同期消費へ進む前に、標準GUIへ1行文字列の`歌詞`項目を1つ登録し、入力したUnicode文字列を
描画できることを確認する。この段階ではルビ構文を解釈せず、入力値全体を1行の白文字として透明背景の
中央へ描画する。空文字は透明画像にする。

この設定は入力経路と描画経路を分離して検証するための最小実装であり、文字列の保存形式、専用構文、
ルビ対応、同期による色分け、フォントや装飾の設定は後続段階で追加する。

### ルビ構文

既存の歌詞テロップと互換性のある`[本文](ルビ)`を使用する。本文が1文字の場合は角括弧を省略して
`字(ルビ)`と書ける。

```text
[漢字](かんじ)を読(よ)む
```

表示本文は`漢字を読む`となり、`かんじ`は`漢字`、`よ`は`読`の幅の中央上へ配置する。
1行内へ複数のルビを指定できる。閉じ括弧が不足するなど完成していない構文は入力中の文字を失わないよう、
構文記号を含む通常文字として表示する。専用GUIによる構文入力補助は後続段階で追加する。

### 専用GUI

タイプだけなら標準パラメーターで選択できるが、標準機能にフォント選択がないため専用GUIを用意する。
フォント設定GUIと同期設定GUIは役割を分ける。同期設定GUIは、曲同期では音と表示単位の対応関係を
編集し、WAV手動同期では波形表示と音声再生を使って同期境界を登録する。

### 同期設定の次期方針

`同期1`～`同期16`は消費モデルを検証するための数値パラメーター列として実装した後、標準GUIから削除し、
曲同期と手動同期の設定値を同じ1つの`同期データ`テキスト項目へ保存する方式へ移行した。曲同期の初版形式は
次のとおりとし、段階値が空の場合はすべて暗黙の`0`として扱う。

```text
SYNC_LYRICS_SYNC/1;mode=music;stages=1,-1,0
```

ヘッダは`SYNC_LYRICS_SYNC/<形式バージョン>`、モードは`mode=music`または将来の`mode=manual`とする。
現行バージョンで未知の項目、重複項目、範囲外の段階値を含む文字列は処理せず、未消費表示へ戻す。

- テキスト先頭へ形式とモードを表すヘッダを付け、曲同期と手動同期を処理側で区別する。
- 設定ボタンから専用GUIを開き、既存の値が対応するヘッダならGUIの初期状態へ復元する。
- GUIで確定した値は既存文字列への追加ではなく、ヘッダを含む新しい文字列で全体を置き換える。
- 保存先は曲同期と手動同期で共通とし、モードごとに個別の数値パラメーター群は持たない。
- ヘッダには将来の形式変更を判別できるバージョンを含める。

曲同期では、同期元の音楽データから得た音と歌詞の表示単位の対応関係を専用GUIで編集する。現在の
`同期1`～`同期16`で検証した、複数音で1表示単位または1音で複数表示単位を消費する考え方は、
GUIが生成する共通テキストへ移して再利用する。

手動同期ではWAVファイルを指定し、専用GUIへ該当区間の波形と再生位置を表示する。利用者は音声を
再生しながらキーを押すか、画面上の登録操作を行って同期境界を連続入力する。

- 各登録位置は表示単位の開始位置とする。
- 表示単位が`N`個なら、最後の表示単位の終了位置を含めて`N + 1`個の境界を登録する。
- 隣り合う境界の間で、その表示単位の進捗を`0%`から`100%`まで連続的に進める。
- 最初の境界より前は全表示単位を未消費、最後の境界より後は全表示単位を完了として扱う。
- 波形上でも境界の登録、選択、移動、削除、直前操作の取り消しができるようにする。
- 次に登録する表示単位をGUI上で明示し、登録済み境界も波形上へ表示する。

連続入力で通常速度では操作が間に合わない場合があるため、`0.5`、`0.75`、`1.0`倍の再生を
用意する。高速な位置確認用として`2`倍再生も候補とする。低速再生中に保存する位置は変速後の経過時間
ではなく、元WAV上の時刻へ変換する。キー入力時刻は画面更新タイマーではなく、音声再生クロックを基準に
求め、音声バッファによる遅延も考慮する。

この専用GUI方式が成立するため、AviUtl2のタイムラインカーソルを止め、Filterの同期ボタンを1回ずつ
押して登録する旧手動案は採用しない。手動同期の登録操作は専用GUI内へ一本化する。

音声処理ライブラリはSYNC_Lyrics本体から分離し、配布条件に合わせてライセンスを変更できる構成とする。
FFmpeg DLL等の利用を許可し、WAVのデコード、シーク、変速処理へ使用できるものとする。音声出力、
波形生成、変速再生、元WAV時刻への変換は責務を分け、利用ライブラリを交換できる境界を設ける。

曲同期とWAV手動同期のどちらも確定結果は同じテキスト入力項目へ保存する。同期設定GUIは保存済みヘッダを
優先して対応モードを復元し、未設定時または別モードへ切り替える場合は利用者がモードを選択できるように
する。モードの選択をファイルの有無だけには依存させない。

## 配置モード、共通表示単位、演出

旧`タイプ`は、通常カラオケ、文字単位強調、1文字ずつ出現、文字自由配置を同じ選択肢に
並べていた。このうち文字自由配置だけは時間変化による演出ではなく、座標と表示スタイルの
決め方であるため、配置と演出を次の独立した軸へ分離した。

| 分類 | 候補 |
| --- | --- |
| 配置モード | 1行配置、文字自由配置 |
| 同期演出 | 通常カラオケ、文字単位強調、1文字ずつ出現 |
| 開始演出 | なし、フェード等 |
| 終了演出 | なし、フェード等 |

1行配置でも同期処理上は文字またはルビ付きグループを単位として進むため、`表示単位`という名称を
配置モードの意味では使わない。GUI上の本文全体・ルビ全体という選択は一括編集範囲であり、
同期上の表示単位を行全体へ変更するものではない。

### 論理同期単位

歌詞構文から生成する既存の`TLyricsDisplayUnit`は、同期上の論理単位として維持する。

- 通常文字は原則1文字を1単位とする。
- `[漢字](かんじ)`は本文`漢字`とルビ`かんじ`を合わせた1単位とする。
- `BaseStart`、`BaseLength`、`RubyIndex`で歌詞内の対応範囲を示す。
- `ConsumesNote`と`SyncUnitIndex`で音との対応を示す。
- 座標、フォント、色、倍率等の配置・描画情報は持たせない。

### 共通表示単位

論理同期単位の後段に、配置モードから独立した共通表示単位を設ける。実装時の仮称を
`TLyricsResolvedDisplayUnit`とし、1つの論理同期単位に対応する本文と任意のルビを
1グループとして保持する。

共通表示単位は少なくとも次を持つ。

| 情報 | 用途 |
| --- | --- |
| 単位番号、`SyncUnitIndex`、`ConsumesNote` | 同期進捗との対応 |
| 本文部分 | 対象文字列、元文字列内の範囲、座標、矩形、倍率、解決済みスタイル |
| ルビ部分 | 有無、文字列、座標、矩形、倍率、解決済みスタイル |
| グループ矩形と基準点 | 拡大、移動等の単位演出 |

本文・ルビの解決済みスタイルには、フォント名、文字高さ、太字、斜体、下線、取り消し線、
字間、同期前色、同期後色を含める。未指定の自由配置個別設定は、共通の行設定を継承した結果を
格納する。演出処理へ未解決の継承状態を渡さない。

座標と矩形は、配置モードと演出による一時変形を適用する前のシーンピクセル座標とする。
本文とルビは同じ同期進捗を共有するが、横幅とクリップ範囲はそれぞれ個別に保持する。

共通表示単位列全体には、配置モードと全単位を囲む行矩形も保持する。開始・終了演出は
この行矩形を基準に全体へ適用し、同期演出は各共通表示単位のグループ矩形を基準に適用する。

### 配置モード別の生成

1行配置と文字自由配置は保存形式と編集方法が異なってよい。描画前に同じ共通表示単位列へ
変換できることを共通化の条件とする。

#### 1行配置

- 本文全体とルビ全体の共通設定を入力とする。
- 本文を横一列に測定し、行全体を基準位置へ配置する。
- 論理同期単位ごとに本文の開始位置と幅を測定し、共通表示単位へ展開する。
- 各ルビは対応本文の中央を基準に配置する。
- GUIでは本文全体または全ルビを選択し、共通設定を一括編集する。
- 本文枠内のドラッグは本文を基準とした行全体の移動とし、ルビも追従する。
- ルビ枠内の上下ドラッグは本文位置を維持したルビ間隔補正とする。
- 本文・ルビとも四隅の操作点で縦横比を維持したサイズ変更、左右中央の操作点で字間を変更する。
- 選択状態は本文全体とルビ全体の2種類だけとし、操作種別は押下位置から決める。
- 一括編集後も描画用データは論理同期単位ごとの共通表示単位として生成する。

#### 文字自由配置

- 論理同期単位と保存済み`TDisplayPlacementItem`を同じ添字で対応させる。
- 個別座標、縦横倍率、本文・ルビ相対位置を使用する。
- 個別スタイルが未指定なら行共通設定を継承する。
- 継承を解決した本文部分とルビ部分を共通表示単位へ格納する。

共通表示単位は実行時の描画モデルであり、そのままFilter項目へシリアライズしない。
1行配置は行設定、文字自由配置は`SL1`形式の個別配置設定を保存し、実行時に変換する。

### 演出との境界

演出は共通表示単位の基本配置を書き換えず、フレームごとの一時状態として適用する。
一時状態は表示・非表示、透明度、相対移動、相対倍率、色変化進捗、クリップ進捗等を持つ。

| 同期演出 | 共通処理 |
| --- | --- |
| 通常カラオケ | 単位進捗を本文幅とルビ幅へ別々に適用し、同期後色のクリップ範囲を広げる |
| 文字単位強調 | 対象単位のグループ基準点へ一時的な倍率・移動等を適用する |
| 1文字ずつ出現 | 対象単位の本文と対応ルビを同時に表示状態へ切り替える |

この3演出は1行配置と文字自由配置の両方で同じ意味を持つ。1行配置の強調で隣接文字と
一時的に重なる場合も行の再配置は行わず、演出による重なりとして許可する。

開始・終了演出は共通表示単位列全体へ適用し、同期演出とは独立させる。配置モード固有の
演出が将来必要になった場合は、その演出だけ対応配置モードを明示し、配置と演出の分類自体は
再統合しない。

### 処理順

```text
歌詞構文
  → 本文・ルビ解析
  → 論理同期単位
  → 配置モード別レイアウト解決
  → 共通表示単位列
  → 同期演出の一時状態
  → 開始・終了演出の全体状態
  → 最終描画
```

文字列解析、フォント測定、基本配置の解決は、歌詞、表示設定、配置設定、シーン寸法のいずれかが
変わった場合に更新する。同期進捗だけが変化する通常フレームでは共通表示単位列を再構築せず、
演出の一時状態と描画だけを更新できる構造を目指す。

### 旧`タイプ`からの移行

旧値と分離後の組み合わせは次のとおりである。

| 旧値 | 配置モード | 同期演出 |
| --- | --- | --- |
| 通常カラオケ | 1行配置 | 通常カラオケ |
| 文字単位強調 | 1行配置 | 文字単位強調 |
| 1文字ずつ出現 | 1行配置 | 1文字ずつ出現 |
| 文字自由配置 | 文字自由配置 | 通常カラオケ |

Filterの設定項目は`配置モード`と`同期演出`へ分離済みである。従来から存在したバウンドの
選択項目は役割の衝突を避けるため`同期アニメーション`へ改名した。β版であるため旧`タイプ`値との
永続的な互換性は持たせない。

`SYNC_PianoRoll` は兄妹製品として独立しているため、ピアノロールそのものを
SYNC_Lyricsの同期演出へ統合するかは未決定とする。

## 連続座標による歌詞配置GUIの検討まとめ

### 目的

歌詞1行分を通常の横一列表示だけでなく、1文字ずつ任意の位置と大きさで配置できる専用GUIを
Filterプラグインへ実装する。

### 編集用背景

Filterプラグインが最後に受け取った画像をコピーして保持し、専用GUIの背景として表示する。
これにより、AviUtl2の編集画面を直接操作しなくても、現在の映像を確認しながら歌詞を配置できる。

- 編集開始時の背景画像は固定する。
- 必要に応じて、最後に受け取った画像を再取得できるようにする。
- 描画処理とGUIが同じ画像を同時に扱う可能性を考慮し、画像のコピーと受け渡しを排他する。

### GUIの起動と状態復元

- Filterの専用ボタンからGUIを開く。
- Filterに保存されたパラメーターを読み込む。
- 前回の配置状態から作業を再開できるようにする。
- 現在のフォント選択機能も専用GUIへ統合する。
- 編集結果は改行を含まないバージョン付き文字列として、TEdit相当のFilter項目へ保存する。
- 保存形式には将来の項目追加を判別できるバージョンを持たせる。

### 文字装飾・配置ツールバー

- 専用GUI上部へWord風の省スペースなアイコンツールバーを置く。
- アイコンは固定画像を拡大せず、ボタン寸法とDPIに合わせてVCL Canvasで描画する。
- フォント、太字、斜体、下線、取り消し線、同期前色、同期後色を文字装飾として配置する。
- 太字、斜体、下線、取り消し線は、選択配置要素の本文とルビへ同時に適用する。
- 複数選択で有効・無効が混在する装飾は混在状態として表示し、クリックすると有効へ揃える。
- 同期前色・同期後色は現在色をアイコン下部の色線として表示し、クリックでカラーダイアログを開く。
- 画面中央、選択初期化、全初期化、横一列、均等配置も同じツールバーから実行する。
- ダイアログは左MouseUpで押下表示を解除した後に開き、モーダル表示後に押下状態を残さない。
- 文字装飾・配置操作はツールバーへ一本化し、右側へ重複ボタンを置かない。

### 表示スタイル

#### 通常表示

歌詞1行全体を1つの配置要素として扱う。

#### 文字単位表示

歌詞を文字単位の配置要素に分け、それぞれの位置と大きさを編集する。
GUIを開いた時点ですべての文字を自動配置し、未配置状態は設けない。利用者は初期配置された文字を
ドラッグして調整する。

基本レイアウトは通常表示と文字単位表示で共通とし、表示スタイルに応じて操作対象と設定項目を
切り替える。

### ルビの扱い

通常表示では、現在の`[漢字](かんじ)`形式を引き続き利用する。

文字単位表示では、ルビ付き部分を1文字ずつ分解せず、親文字とルビをまとめた1つの配置グループとして
扱う。

例:

| 歌詞部分 | 配置要素 |
| --- | --- |
| `私` | 1配置要素 |
| `は` | 1配置要素 |
| `[漢字](かんじ)` | 親文字`漢字`とルビ`かんじ`をまとめた1配置要素 |
| `を` | 1配置要素 |

### 画面構成

| 領域 | 内容 |
| --- | --- |
| 左側 | 背景画像と歌詞の配置編集画面 |
| 右側 | 配置要素一覧 |

- 通常表示では行全体を編集する。
- 文字単位表示では個別または複数の配置要素を編集する。

### マウス操作

| 操作 | 動作 |
| --- | --- |
| マウスホイール | 編集画面を拡大・縮小する |
| 背景の左ドラッグ | 拡大中の表示位置を移動する |
| 要素中央付近の左ドラッグ | 要素の位置を移動する |
| 要素の端または四隅の左ドラッグ | 縦横サイズを変更する |
| 背景の右ドラッグ | 矩形による範囲選択を行う |
| 複数選択中の設定変更 | サイズやフォント等を一括変更する |
| 水色状態の左右点を横ドラッグ | 選択要素内の本文（漢字側）の字間を調整する |
| 紫状態の左右点を横ドラッグ | 選択要素内のルビ字間を調整する |
| 紫状態の上中央点をドラッグ | 本文に対するルビの相対位置を調整する |

左ドラッグは、配置要素上では要素編集、背景上では表示位置の移動として使い分ける。
配置要素の最終的な矩形サイズは端・四隅の変形で決める。本文・ルビの基準文字サイズと
縦横倍率を100%へ正規化せず、本文とルビの比率を保ったまま配置グループを変形する。
水色状態の本文字間は文字サイズ、縦横倍率、配置要素の座標を変更せず、文字間へ加える値だけを
変更する。正値で広げ、負値で詰めることができる。通常の1文字要素では字間の見た目は変わらず、
`[漢字](かんじ)`のように本文が複数文字ある配置要素で効果が現れる。中央基準で描画するため、
ハンドル移動量の2倍を本文の文字間数で割って字間へ換算し、操作した枠線の内側へ本文端を追従させる。
紫状態のルビ字間について同じ端追従計算を適用するかは保留する。

### 配置要素が保持する情報

技術的には、各配置要素へ次の情報を保持できる。

- X・Y座標
- 横サイズ・縦サイズ
- 文字サイズ
- ルビサイズ
- フォント名
- 同期前の色
- 同期後の色
- 本文字間
- ルビ字間
- ルビの相対X・Y位置

すべてを常に個別設定にすると操作が複雑になるため、行全体の共通設定と、選択要素の個別設定を分ける。

複数選択時には座標関係を維持したまま、次の設定を一括変更できるようにする。

- フォント
- 文字サイズ
- 縦横サイズ
- 同期前後の色

### 初期配置と再配置

すべての要素を最初から配置済みとし、配置済み・未配置という状態は管理しない。

再配置操作として、次を用意する。

- 選択要素を初期位置へ戻す。
- すべての要素を初期配置へ戻す。
- 横一列へ整列する。
- 要素間隔を均等にする。

### 画面外へ移動した場合の復旧

文字が画面外へ移動し、直接選択できなくなる事故に備えて、右側へListView形式の配置要素一覧を設ける。

- 全配置要素を一覧表示する。
- 一覧から画面外の要素も選択できる。
- 一覧でも複数選択できる。
- ルビ付き部分は1つのグループとして表示する。

復旧用として、次の操作を用意する。

- 選択要素を画面中央へ強制移動する。
- 選択要素を初期位置へ戻す。
- すべてを初期配置へ戻す。

`画面中央へ移動`では位置だけを変更し、フォント、サイズ、色等は維持する。複数選択時は、
要素同士の配置関係を維持したまま中央へ移動する。

### 現時点での基本方針

Filterプラグイン、最後に受け取った画像のコピー、専用歌詞配置GUI、文字単位の設定データ保存を
組み合わせる。

未配置の文字を順番に追加する方式ではなく、最初からすべてを配置し、画面上で位置と大きさを
調整する方式とする。これにより操作と内部状態を単純化しつつ、通常の1行表示と自由な文字単位配置の
両方に対応する。

### 個別色設定用カラーピッカー

文字自由配置では各配置要素へ、同期前色と同期後色の2色を個別に設定できるようにする。
未指定要素はFilterの共通色を継承し、単一選択と複数選択のどちらにも同じ操作を適用する。

Delphi標準の色選択画面は今回の用途には操作性が不足するため、既存の
`D:\DelphiProg\Lib\ColorPicker`を基にする。使用するソースは
`Source\Lib\ColorPicker`へコピーし、プロジェクト単体でビルドできる状態にする。

取り込んだユニットは次のとおり。

- `ColorPickerDialog`
- `ColorPickerDialogFrame`
- `ColorPickerColorMath`
- `ColorPickerRGBEditFrame`
- `ColorPickerHueBar`
- `ColorPickerSVArea`
- `ColorPickerPick`

MITライセンスの`LICENSE`も同じフォルダーへ保持する。サンプルアプリ、プロジェクトファイル、
旧版スポイト`ColorPickerPick_old`、ビルド成果物は取り込まない。

プラグイン統合時に次を修正した。

- RGB、Hue、SV間のプログラム更新でイベントが連鎖しないよう、更新中フラグを設ける。
- RGBへ0～255外の値を入力した場合、内部値だけでなく入力欄の表示も補正する。
- Hueバーを標準的なHSV色相グラデーションへ変更し、SV領域との変換式を共通化する。
- ダイアログと内部フレームの設計サイズを一致させる。
- `ColorPickerSVArea.pas`末尾の余分な`end.`を削除し、コンパイラ警告を解消する。
- スポイトは仮想スクリーンの左上座標と幅・高さを使い、負座標を含むマルチモニターへ対応する。
- スポイト終了時にイベント処理中のフォームを直接解放せず、安全に遅延終了する。
- `CAPTUREBLT`を使用し、レイヤードウィンドウも画面取得へ含める。
- 取り込んだPascalソースとDFMをUTF-8へ統一する。
- 所有者と初期色を渡して利用できる`ExecuteColorPicker`関数を追加する。

実機で次を確認する。

- ダイアログが異なるDPIでも崩れず表示されること。
- スポイトのDPI座標と取得画像座標が一致することを複数の表示倍率で確認する。
- 負座標を持つマルチモニターで全画面を取得し、各画面の色を選択できること。

個別色は各配置要素のバージョン付き単行文字列レコードへ保存する。
未指定色はFilterの共通色を継承する。

## 現在の最小プロジェクト構成

プロジェクトごとの子フォルダーを増やさず、リポジトリのルートを最小限に保つ。

```text
SYNC_Lyrics_Input.dpr
SYNC_Lyrics_Input.dproj
SYNC_Lyrics_Filter.dpr
SYNC_Lyrics_Filter.dproj
Source\
  Common\
    Timeline\
      SYNC_Lyrics_FrameShared.pas
      SYNC_Lyrics_ContextManager.pas
      SYNC_Lyrics_Time.pas
  Plugin\
    Input\
      SYNC_Lyrics_InputPlugin.pas
    Filter\
      SYNC_Lyrics_FilterPlugin.pas
  ...
  Lib\
    ...
Tests\
  SYNC_Lyrics_FrameContextTests.dpr
```

- `SYNC_Lyrics_Input` は `.synclyrics` の透明ベース映像を提供し、要求された絶対フレーム位置を
  `Local\SYNC_Lyrics_Frame_V1` へ公開する。
- 仮想素材のファイル名形式は `Width_Height_MaxSec_Fps_Scale.synclyrics` とする。
- 値を省略または解釈できない場合は、幅1、高さ1、3600秒、30fps、scale 1を使う。
- `SYNC_Lyrics_Filter` は `SYNC_歌詞テロップ_Filter`、グループ名 `SYNC` として登録する。
- Filterは歌詞解析、音楽・WAV手動同期、通常・文字単位・文字自由配置描画、開始・同期・終了演出、
  専用同期GUI、自由配置GUIを実装済みである。共有絶対フレームの受信と、Inputがキャッシュで
  再発火しない間のFilter相対フレーム補間も実装済みである。
- 設定項目が0個でも、AviUtl2へは直接nilではなく、nil終端した項目ポインター配列を渡す。
- SDKの最小ABI定義と共有メモリ基礎処理は `SYNC_PianoRoll\Source\Lib` からコピーした。

- `.dpr` はexport境界と必要ユニットの列挙だけに留める。
- InputとFilterのプラグインユニットは登録と処理の入口を担当する。
- 歌詞解析、同期消費、文字レイアウト、各表示タイプ、GUI等は責務ごとに専用ユニットへ分ける。
- 2つ以上の固有ユニットで共通化できる処理と汎用SDK定義は `Source\Lib` へ置く。
- 元プロジェクトからライブラリをコピーする場合も、不要なユニットを一括で持ち込まず、
  実際に使う依存だけを置く。
