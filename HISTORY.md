# SYNC_Lyrics 開発履歴

完了済みの実装経緯と日付付き検証結果を置く。
現在の作業には原則不要なので、過去の判断や回帰原因を調べる場合だけ参照する。

## 作業ログ

- 2026-07-27: `SYNC_Motion\note.md` から共通の識別名、ビルド、配布、コメント、保守、
  GitHub同期ルールを移植した。
- 2026-07-27: 打ち合わせ内容をもとに、3オブジェクト構成、InputとFilterのグループ分割、
  1行単位処理、音の消費モデル、順序を表す同期パラメーター、表示時間、専用GUI、表示タイプを整理した。
- 2026-07-27: 打ち合わせの確定事項、利用者の基本編集手順、消費処理で独立して進む3位置、
  行内の消費状態、英語の複数単語消費、旧方式の設定要素からの移行方針を追記した。
- 2026-07-27: `SYNC_PianoRoll` を参考にInputとFilterの最小プロジェクトを作成した。
  Inputは透明ベースと共有フレーム公開、Filterは設定項目なしのパススルー登録とした。
  Debug Win64で両プロジェクトが警告0、エラー0でビルドできることを確認した。
- 2026-07-27: 作業フォルダーを `D:\DelphiProg\test\SYNC_Lyric` から
  `D:\DelphiProg\test\SYNC_Lyrics` へ変更し、ノート内のビルドパスも更新した。
- 2026-07-27: Gitリポジトリを初期化し、`SYNC_PianoRoll` と同じローカル設定、
  `.gitignore`、`.gitattributes`、CRLF・binary属性を適用した。リモートは
  `https://github.com/vramwiz/SYNC_Lyrics` とする。
- 2026-07-27: Inputが公開した絶対フレームとFilterの相対フレームを
  `Object_.ID + Object_.EffectID`ごとに対応付ける補間コンテキストを実装した。
  Inputの共有更新番号が変化したときだけ再基準化し、入力キャッシュで再発火しない間は
  `現在の相対フレーム - 基準相対フレーム`を最後の絶対フレームへ加算する。
- 2026-07-27: 統合テストでInputを絶対150フレームに一度だけ発火させ、Filter相対フレーム
  10、11、40から絶対150、151、180を取得した。Inputを絶対300で再発火させると300へ
  再基準化し、次の相対フレームで301となることを確認した。FilterのDebug Win64構成も
  ロード中の配置済みファイルへ触れない検証出力先で、警告0・エラー0のビルドに成功した。
- 2026-07-27: 実装参照元として`SYNC_PianoRoll`、Syncroh2の歌詞テロップFilter／Extension
  プロジェクトと両実装フォルダーを記録した。
- 2026-07-27: 更新版Input／FilterをAviUtl2へ配置して再起動し、Rootのレイヤー1・
  230～378フレームにある`.synclyrics`素材＋`SYNC_歌詞テロップ_Filter`構成で実機確認した。
  先頭230フレームではInput共有値が絶対0、rate 30、scale 1、0秒となった。231、232を
  初回描画すると共有値は1、2へ進み、更新番号も6、8へ進んだ。その後231、232へ戻って
  再描画すると、Filter相対フレームは1、2へ変化している一方、Input共有値は絶対2、
  更新番号8、更新時刻も同じ値のまま停止した。入力キャッシュでInputが再発火しない状態を
  実機で再現し、その間を相対フレーム差で補間する必要性と実装条件を確認した。
- 2026-07-27: 歌詞の事前表示と事後維持をオブジェクト長だけで表現する方針を変更した。
  前後へ延長した区間で前の歌詞または次の歌詞へ同期することを防ぐため、
  `事前時間 (秒)`と`事後時間 (秒)`をFilter設定として持ち、その区間では同期消費を
  進めず、未消費状態または完了状態を維持する。
- 2026-07-27: 歌詞入力の第1検証として、Filterの標準GUIへ1行文字列の`歌詞`項目を追加した。
  入力したUnicode文字列を96pxの白文字で透明RGBA画像の中央へ描画し、空文字では完全透明にする。
  単体テストで日本語文字列が可視画素を生成することと空文字が透明になることを確認し、Filter全体も
  Debug Win64でビルドできることを確認した。
- 2026-07-27: AviUtl2実機で`歌詞`項目へ入力した通常文字列が表示されることを確認した。
  第1検証では専用構文を未解析のためルビは表示されない。次段階では既存の歌詞テロップ実装を参照し、
  ルビ構文の解析結果を本文とルビの上下二段へ分けて描画する。
- 2026-07-27: 既存実装の`[本文](ルビ)`構文に対応した。複数のルビ区間を解析し、構文を除いた
  本文全体を中央配置したうえで、各ルビを対応本文区間の中央上へ42pxで描画する。本文は96pxとする。
  解析テスト、ルビ上段の描画テスト、空文字テスト、入力キャッシュ補間の回帰テストがすべて成功し、
  Filter全体もDebug Win64でビルドできることを確認した。
- 2026-07-27: 本文が1文字の場合の省略記法`字(ルビ)`へ対応した。
  `[漢字](かんじ)を読(よ)む`を本文`漢字を読む`と2つのルビ区間へ解析できることを確認した。
- 2026-07-27: AviUtl2実機で`[漢字](かんじ)を読(よ)む`を表示し、複数文字のルビ`かんじ`と
  1文字省略記法のルビ`よ`が、それぞれ対応する本文の中央上へ表示されることを確認した。
  ルビ入力・解析・基本描画の段階は完了し、次は音楽データのノート時刻列を使う同期処理へ進む。
- 2026-07-27: MIDI同期の第1検証を実装した。音楽ファイルと0始まりのトラック番号を設定し、
  Filterオブジェクト先頭から現在までのノートオン数を1音1文字として消費する。消費済み本文と、
  対応本文をすべて消費したルビは水色、未消費部分は白で描画する。固定MIDIデータから0秒と0.5秒の
  2音を復元するテスト、消費済み色の描画テスト、ルビ解析・描画テスト、入力キャッシュ補間テストが
  すべて成功した。
- 2026-07-27: `SYNC_PianoRoll\Source\Lib\SongReader`の`.pas`ソース一式を取り込み、独自の
  最小MIDI解析を`TSongData.LoadFromMusicFile`利用へ置き換えた。MIDI、UST、VSQ／VSQX、
  MusicXML、MuseScoreをPianoRollと同じ読込規則で扱う。SongReaderへ一時MIDIファイルを渡す
  統合テストでノート開始時刻と区間内消費数を確認し、全ReaderをリンクしたFilterの
  Debug Win64ビルドにも成功した。
- 2026-07-27: AviUtl2実機で音楽ファイルを指定し、ノート開始時刻に合わせて歌詞が先頭から
  白から水色へ変化することを確認した。Inputの絶対フレーム、キャッシュ補間、SongReaderの
  ノート時刻列、1音1文字の消費、描画色変更までの最小同期経路が成立した。
- 2026-07-27: ノート開始時に1文字全体を即時変色する方式から、ノート開始・終了間の進捗率で
  左から右へクリッピングする方式へ変更した。ルビ指定された対応本文とルビは1表示単位として
  同じ進捗率を共有し、それぞれ異なる実測横幅へ進捗率を掛けて別々のクリップ境界を求める。
  ノート中間で`0.5`表示単位となる同期テストと、`[漢字](かんじ)`の本文・ルビ両方へ50%の
  変化後色が描画されるテストに成功した。
- 2026-07-27: AviUtl2実機で本文とルビが同じ進捗率を共有し、異なる実測幅で滑らかに変色することを
  確認した。行全体は同じレイヤーのオブジェクトをグループ制御で配置し、Filterには中央基準の
  `X`、`Y`だけを最小限の座標設定として追加した。今後追加する色、本文・ルビ別フォントとサイズ、
  フォント設定ボタン、ルビ間隔補正、字間、および不足候補もFilter設定要素へ記録した。
- 2026-07-28: 基本表示設定の第1段階として、Filterへ`フォント名`、`フォント名（ルビ）`、
  `変化前色`、`変化後色`、`歌詞サイズ`、`ルビサイズ`を追加した。描画処理へ設定レコードを渡し、
  本文とルビのフォント名・サイズおよび同期前後の色へ反映する。白文字マスクを色描画と別に生成して
  透明度を決め、黒や暗色も透明にならず表示できるようにした。設定色とサイズの描画テスト、
  既存のルビ・同期・座標・入力キャッシュ補間・音楽同期テストがすべて成功し、FilterもDebug Win64で
  警告0、エラー0のビルドに成功した。
- 2026-07-28: Syncroh2の歌詞テロップFilterを参照し、標準GUIの`フォント設定`ボタンから開く
  専用GUIを追加した。インストール済みフォントを本文用・ルビ用の一覧へ表示し、それぞれの
  プレビューを確認して選択できる。OK時だけ選択中オブジェクトの`フォント名`と
  `フォント名（ルビ）`へUTF-8で書き戻し、キャンセル時は変更しない。2項目目の反映に失敗した場合は
  1項目目を元へ戻す。編集APIの関数型を公式SDKと参照実装に合わせ、FilterのDebug Win64ビルドと
  既存の描画・入力キャッシュ補間・音楽同期テストが警告0、エラー0ですべて成功した。
- 2026-07-28: `SYNC_Lyrics_FontSettingsForm.dfm`がBOMなしUTF-8だったため、Delphi IDEで
  ANSIとして誤認される文字化けを修正した。フォームの`.pas`と`.dfm`をUTF-8 BOM付き・CRLFへ統一し、
  FilterのDebug Win64ビルドが警告0、エラー0で成功することを再確認した。
- 2026-07-28: 本文サイズとルビサイズを独立して指定し、二段全体を中央へ再配置する現在の方式を
  維持したまま、Filterへ`ルビ間隔補正`と`字間`を追加した。どちらも初期値は`0`で既存表示を変えず、
  ルビ間隔補正は既定の4pxへ加算し、字間は本文の測定・通常描画・同期クリッピング描画へ共通して
  適用する。字間による横幅変化とルビ間隔による上下間隔変化の描画テストが成功し、Filterも
  Debug Win64で警告0、エラー0のビルドに成功した。
- 2026-07-28: Filterへ`事前表示 (秒)`と`表示維持 (秒)`を追加し、初期値`0.50`秒、
  変更単位`0.01`秒とした。オブジェクト先頭から事前表示時間は未消費状態を維持し、その終了以降の音を
  歌詞の表示単位数だけ割り当てる。最後の割り当て音の終了から表示維持時間を超えた後は完全透明にする。
  後続行の音で表示時間が延びないこと、事前・同期中・完了維持・透明化の各境界、および音不足時に
  未完了状態を残すフォールバックの同期テストが成功した。Rendererテストも成功し、Filterは
  Debug Win64で警告0、エラー0のビルドに成功した。
- 2026-07-28: 表示終了をFilter設定とオブジェクト末尾で二重管理しないよう、`表示維持 (秒)`と
  時間経過後の透明化処理を削除した。最後に割り当てた音が終了した後は完了状態をオブジェクト末尾まで
  維持し、表示時間はタイムライン上の末尾位置で直接調整する。`事前表示 (秒)`と表示単位数による
  後続音の除外は維持した。
- 2026-07-28: 表示要素の将来候補には太字・斜体等のフォントスタイルと`字間（ルビ）`を残し、
  縁取り・影等はSYNC_Lyricsへ実装せずAviUtl2の既存Filterで構成する方針を確定した。
- 2026-07-28: 同期調整の第1段階として`同期1`～`同期16`を追加した。値`0`は1音で1表示単位、
  正の`n`は`n + 1`音で1表示単位、負の`n`は1音で`abs(n) + 1`表示単位を連続的に進める。
  GUI範囲は`-63`～`63`とし、16段階を超えた部分は暗黙の`0`で継続する。2音で1表示単位、
  1音で2表示単位、調整段階後の暗黙値を固定MIDIで検証し、MusicSyncテストが成功した。
  FilterもDebug Win64で警告0、エラー0のビルドに成功した。
- 2026-07-28: 同期設定の次期方針を整理した。曲同期とWAV手動同期は、モードと形式バージョンを表す
  ヘッダ付きの共通テキスト項目へ保存し、専用GUIで既存値の読込と全体置換を行う。手動同期はWAV波形を
  表示して`1/4`、`1/2`、`1`倍等で再生し、キーまたは画面操作で表示単位数`+ 1`個の境界を連続登録する。
  FFmpeg DLL等を交換可能な音声処理部として利用できる構成とし、AviUtl2タイムライン上の同期ボタンで
  1件ずつ登録する案は採用しない。
- 2026-07-28: 同期設定の共通テキスト形式第1版を実装した。`同期1`～`同期16`の標準GUI項目を1つの
  `同期データ`文字列へ置き換え、`SYNC_LYRICS_SYNC/1;mode=music;stages=...`から曲同期の段階値を
  復元して既存の連続進捗計算へ渡す。空の段階列は暗黙の`0`、不正なヘッダ、未知・重複項目、範囲外値は
  未消費表示へフォールバックする。シリアライズ往復と不正値のテスト、既存の音楽同期・フレーム補間・
  描画テストがすべて成功し、FilterもDebug Win64で警告0、エラー0のビルドに成功した。
- 2026-07-28: `同期データ`の直前へ`曲同期設定`ボタンを追加し、空の専用フォームを開く入口を実装した。
  曲同期用ユニットは`SYNC_Lyrics_MusicSyncSettingsForm`とし、今後追加する手動同期用ユニットは
  `SYNC_Lyrics_ManualSyncSettingsForm`として役割と名称を分ける。
- 2026-07-28: 曲同期GUIの基準位置取得を検証する第1段階として、Filterが最後に正常解決した絶対フレーム、
  フレームレート分子・分母をスレッド安全に保持する`SYNC_Lyrics_MusicSyncAnchor`を追加した。
  `曲同期設定`ボタンを押すと、フォームへ基準絶対フレームと秒位置を表示する。まだFilterが発火して
  いない場合は未取得と表示する。現段階では選択オブジェクトとの厳密な照合を行わず、最後に発火した
  Filter位置を使って取得経路を実機検証する。
- 2026-07-28: 曲同期GUIへ現在の歌詞、音楽ファイル、トラック、同期データを読み取り専用で渡す
  概要画面を追加した。歌詞はルビ構文を解析し、本文1文字またはルビ対応本文を1つとする同期対象の
  表示単位一覧へ展開する。同期データは曲同期ヘッダ、形式バージョン、明示された段階数を検証して表示する。
  この段階では書き戻しを行わず、次の音と表示単位の対応編集に必要な入力値を確認できるようにした。
- 2026-07-28: 曲同期GUIで音楽ファイルをSongReader経由で読み込み、基準絶対フレームから換算した秒位置
  以降かつ指定トラックに一致するノートの開始・終了時刻を一覧表示するようにした。各行には曲先頭からの
  絶対秒、基準からの相対秒、トラック番号を表示する。大きい音楽データでGUIへ過剰な行を追加しないよう、
  件数は全対象を数えつつ一覧表示は先頭256音に制限する。
- 2026-07-28: 曲同期GUIの構成を作り直す第1段階として、同期関係の表示・編集を入れず、`SYNC_PianoRoll`
  の横表示を参考にした最小ピアノロールを追加した。左に白鍵・黒鍵、右に音階レーン、基準位置を示す
  発音線、1秒間隔の時間線、基準から6秒先までのノート矩形を表示する。表示に必要なMIDIキー番号を
  音楽ノートデータへ追加した。
- 2026-07-28: 最小ピアノロールの黒鍵右側が暗いレーン背景と連続し、白鍵と同じ長さに見える表示を修正した。
  単純な半音等間隔ではサンプルの鍵盤形状にならなかったため、`SYNC_PianoRoll`と同じ白鍵単位の
  音階座標へ変更した。連続した白鍵を先に描き、その境界へ高さ62%、長さ62%の黒鍵を重ねる。
  音階レーンとノート矩形も同じ座標計算を共有する。
- 2026-07-28: ピアノロールの各ノートへ小さい音名を表示した。音名は`C`～`B`、半音は`#`付きとする。
  SongReaderのノート歌詞を表示用データへ引き継ぎ、歌詞がある場合はノート上側、画面上端に余裕が
  なければ下側へ表示する。
- 2026-07-28: ノート内の音名とノート外の歌詞を一段大きくした。黄色のGUI基準線に加え、Filterの
  `事前表示 (秒)`だけ右へ進んだ位置へピンク色の縦線を表示し、同期開始位置を確認できるようにした。
- 2026-07-28: ピンク色の事前表示線へマウスを近づけると左右移動カーソルへ変え、左ドラッグで
  0～6秒の範囲を移動できるようにした。現段階ではフォーム内のプレビュー値だけを変更し、
  Filterの`事前表示 (秒)`や同期データへは書き戻さない。
- 2026-07-28: 事前表示線のドラッグ中にピアノロール全体を直接描き直していたため発生したちらつきを
  修正した。フォームをダブルバッファ化し、ピアノロールは保持用32bit Bitmapへ全要素を描いてから
  1回でPaintBoxへ転送する。今後のノート選択や同期表示も同じオフスクリーン描画経路へ追加する。
- 2026-07-28: 曲同期GUIをピアノロール中心の本番表示へ整理した。基準フレーム、基準時刻、
  音楽ファイル、トラック、ノート数などの確認用表示を外し、下部の操作領域を除く全面へ
  ピアノロールを広げた。読み込み不能など利用者が対処すべき状態だけはロール内へ表示する。
- 2026-07-28: 音楽データ由来の歌詞はノート矩形内で音名の右側へクリップ表示し、矩形外へ
  はみ出さないようにした。Filter側の歌詞は表示単位へ解析して同期対象ノートの直下へ表示する。
  画面下部に歌詞入力欄と適用ボタンを追加し、適用時に対象Filterの`歌詞`項目へ書き戻す。
- 2026-07-28: ピアノロールのノート矩形は、白鍵・黒鍵の形状差に影響されず全音で同じ高さとした。
  ノートの縦中心は対応する鍵盤の音程中心座標へ揃える。
- 2026-07-28: ノート矩形の上下端を小数座標から別々に丸めることで、音程位置によって高さが
  1px変動していた点を修正した。整数の固定高さを先に決め、下端を`上端 + 固定高さ`で求める。
- 2026-07-28: 背景の音程レーンが白鍵では太く黒鍵では細かったため、固定高のノートを載せると
  中心がずれて見える問題を修正した。縦軸をMIDI半音ごとの等間隔へ統一し、背景レーンは全音程で
  同じ高さとする。鍵盤の黒鍵は短い形状を維持し、鍵盤・レーン・ノートの中心座標だけを共有する。
- 2026-07-28: Filter歌詞とノート下端の間隔を5pxへ広げた。ピアノロール上のFilter歌詞を
  クリック可能にし、選択中の1表示単位だけに1本の仮想カーソルを表示する。別の表示単位を
  クリックすると同じカーソルが移動し、複数カーソルや範囲選択は作らない。
- 2026-07-28: 仮想カーソルを縦線から選択歌詞を囲む枠へ変更し、歌詞上のマウス押下から
  左右ドラッグで同期段階を調整できる第1実装を追加した。36pxごとに1段階とし、右は選択歌詞を
  `複数音で1表示単位`へ、左は選択歌詞と左側の歌詞を`1音で複数表示単位`へまとめる。
  ドラッグ結果はピアノロールの歌詞配置へ即時反映し、適用時に既存の`stages`形式で書き戻す。
- 2026-07-28: 左隣から始まる`2文字で1音`の2文字目を右ドラッグした場合、共有を解除して
  `1文字1音`へ戻すだけとした。同じドラッグで選択文字を`2音で1文字`へ進めず、中立の
  `1文字1音`を飛び越えない。さらに右へ進める場合は、共有解除後の次のドラッグで調整する。
- 2026-07-28: ドラッグ時に被覆値から左方向へ同期段階を探索する方式を廃止した。GUI内部では
  `開始歌詞・歌詞数・音数`を持つ同期グループ一覧を正本とし、各歌詞から所属グループを直接引く
  対応表を生成する。左ドラッグは1文字ではなく直前の同期グループ全体を結合するため、
  1～2文字目が共有済みの状態で3文字目を左へ動かすと、開始位置0の3文字共有グループになる。
  適用時だけ同期グループを従来の`stages`値へ変換する。
- 2026-07-28: 共有グループ内の歌詞を右ドラッグしたとき、グループ左端を右へずらす方式では
  選択文字以外も移動していたため、選択文字だけを切り離す方式へ変更した。
  `[を・読・む]`で`む`を右へ動かすと、`[を・読] [む]`となる。選択文字の左右に別の文字が
  残る場合は左右の共有グループをそれぞれ維持し、選択文字だけを1文字1音のグループにする。
- 2026-07-28: 歌詞編集時は既存同期グループを可能な範囲で保持しつつ、崩れた場合に復旧できるよう
  歌詞入力欄の隣へ`同期を初期化`ボタンを追加した。押すと全表示単位を1文字1音のグループへ戻し、
  ピアノロールへ即時反映する。Filterへの書き戻しは従来どおり`適用`時だけ行う。
- 2026-07-28: 音程に追従する既存歌詞とは別に、ピアノロール下部へ固定Yの同期歌詞レーンを
  追加する方針とした。同期グループの時間範囲を本文文字数で等分し、ルビ対応本文が1表示単位でも
  複数音を使う場合は、本文の各文字を時間方向の適切な位置へ分けて表示する。ルビは可能な場合に
  対応本文範囲の中央上へ表示する。第1段階は表示のみとし、操作は表示確認後に追加する。
- 2026-07-28: 曲同期フォームが約1130行まで肥大化したため、フォームをVCLイベントと各要素の
  受け渡しを行う調整役へ変更した。`SYNC_Lyrics_MusicSyncEditModel`は歌詞単位、同期グループ、
  ドラッグ編集、stages変換を担当する。`SYNC_Lyrics_MusicSyncPianoRoll`は鍵盤、背景、ノートと
  歌詞層用のノート座標を担当する。`SYNC_Lyrics_MusicSyncNoteLyrics`はノート追従歌詞の表示、
  選択枠、ヒットテストを担当する。`SYNC_Lyrics_MusicSyncFixedLyrics`は固定Yの本文・ルビ表示を
  担当し、後から専用操作を追加できる境界とした。フォーム本体は約370行となり、各公開処理へ
  責務と受け渡し内容のコメントを追加した。
- 2026-07-28: 下部の固定Y歌詞レーンも、ノート追従歌詞と同じ同期グループ状態を参照する。
  `漢字`が1表示単位・1音の間は第1ノート開始Xから通常の文字間隔で横並びにする。その表示単位を
  2音へ変更した時だけ、`漢`は第1ノートに残して`字`を第2ノート開始Xへ移す。後続表示単位の
  開始ノートも消費音数に従って移動する。ルビは配置後の本文範囲を基準に中央へ表示する。
- 2026-07-28: ノート追従歌詞と固定Y歌詞レーンの選択状態・ドラッグ処理を共通化した。
  どちらの表示をクリックしても同じ歌詞単位を選択し、上段と下段の対応位置へ同時に黄色の
  選択枠を表示する。下段にも歌詞単位別のヒット領域を持たせ、既存の同期グループ編集処理へ
  接続したため、上下どちらからドラッグしても同じ同期結果になる。
- 2026-07-28: 音を持たない文字を同期数から分離する第1段階として、共通歌詞パーサーの
  表示単位へ`ConsumesNote`を追加した。日本語文字・英数字・長音記号・踊り字などは発音対象、
  空白・句読点・括弧は非発音対象と判定する。明示ルビ付きの表示単位は読みが指定されているため
  常に発音対象とする。同じ分類を曲同期GUIの編集モデルまで伝播させたが、この段階では既存同期
  データとの互換性を保つため、表示単位数と同期計算はまだ変更しない。次段階で非発音単位を
  前後の発音単位へ付随させ、同期音を消費しないようにする。
- 2026-07-28: 非発音文字を同期単位から除外した。途中・末尾の空白や記号は直前の発音単位へ、
  行頭の空白や開き括弧は最初の発音単位へ付随させる。共通パーサーの`SyncUnitIndex`をFilterの
  描画進捗と同期対象数に使用し、曲同期GUIでは非発音文字を`PrefixText`/`SuffixText`として
  発音単位へ保持する。上下の歌詞表示と選択枠には付随文字も含めるが、付随文字だけの独立した
  カーソルや同期グループは作らない。例として`「歌、 空。」`は7文字を維持して表示しつつ、
  同期音を消費する単位は`歌`と`空`の2つになる。
- 2026-07-28: 曲同期データが未設定、空文字、または`stages=`が空の場合は、GUIを開いた時点で
  発音表示単位ごとの`1表示単位・1音`グループを生成する処理を`BuildDefaultGroups`へまとめた。
  記号・空白は既に発音単位へ付随しているため初期グループを増やさず、ルビ対応本文は本文が
  複数文字でも1表示単位として初期化する。モデルは既定値生成の有無を保持し、適用時は既存の
  正規形式に従って全0段階を`SYNC_LYRICS_SYNC/1;mode=music;stages=`として保存する。
- 2026-07-28: 歌詞編集時の同期保持を位置だけの比較から、編集前後の発音単位本文に対する
  最長共通部分列の照合へ変更した。途中へ歌詞を追加・削除しても一致する単位の同期グループを
  新しい位置へ移し、新規部分だけを`1表示単位・1音`へ初期化する。複数表示単位を1音へまとめた
  グループは全構成単位が同じ順序で連続して残る場合だけ保持し、内部へ編集が入った場合は安全の
  ため分解する。1表示単位を複数音へ割り当てた設定、記号・空白だけの編集、ルビ読みだけの変更は
  本文が一致する限り保持する。選択中の歌詞も対応後の位置へ追従する。
- 2026-07-28: 曲同期GUIのカスタム描画を96 DPI固定値から`CurrentPPI`基準へ変更した。
  鍵盤幅、固定歌詞レーン高、フォント、文字余白、選択枠、線幅、最小ノート幅、事前表示線の
  ヒット幅、歌詞ドラッグの1段階距離を`ScaleMusicSyncMetric`で同じ倍率にする。フォームは
  `PixelsPerInch=96`と`Scaled=True`を明示し、DPI変更時の`ChangeScale`でオフスクリーン
  Bitmapを破棄して再描画する。192 DPIでは鍵盤幅152px・固定歌詞レーン116pxになることを
  自動テストし、96 DPI時の従来値も維持する。
- 2026-07-28: 黒鍵は鍵盤幅の62%で描いていたが、残り38%の下地がピアノロールと同じ暗色だった
  ため、黒鍵が白鍵と同じ長さに見えていた。鍵盤領域全体を先に白鍵色で塗り、その上へ短い黒鍵を
  重ねる描画順へ変更した。192 DPIの描画テストで黒鍵内部が暗色、黒鍵右側の残り領域が白色に
  なることをピクセル値で検証する。
- 2026-07-28: 鍵盤の白鍵高が半音レーンと同じ一定幅になっていたため、鍵盤部分だけ通常の
  ピアノ形状へ変更した。音程レーンとノートのY座標は半音等間隔のまま維持する。白鍵は上下の
  隣接白鍵との中心中間まで広げるため、黒鍵を挟むC-Dなどと白鍵が隣接するE-Fなどで見かけの
  高さが変わる。黒鍵は従来どおり半音中心へ短い高さ・鍵盤幅62%で重ね、ノートとの中心対応を
  維持する。
- 2026-07-28: 黒鍵の中心座標と横方向62%の長さは維持し、縦方向の厚みだけ半音間隔の
  62%から93%へ変更した。従来比約1.5倍となり、白鍵境界上の黒鍵が見やすくなる。音程レーン、
  ノート、歌詞の座標計算には影響させない。
- 2026-07-28: 曲同期GUIのピンク色の事前表示線を0.01秒単位へ丸め、`適用`時に歌詞・同期データと
  あわせてFilterの`事前表示 (秒)`へ書き戻すようにした。3項目の途中で反映に失敗した場合は、
  先に変更した項目を元の値へ戻す。音楽同期・描画・フレーム補間テストがすべて成功し、
  FilterもDebug Win64で警告0、エラー0のビルドに成功した。
- 2026-07-28: FilterのGUI項目表示順を変更し、先頭から`曲同期設定`、`歌詞`、
  `音楽ファイル`、`トラック (-1=全て)`の順に並べた。項目名と保存値は変更しない。
  FilterはDebug Win64で警告0、エラー0のビルドに成功した。
- 2026-07-28: 本文とルビへ個別に`太字`、`斜体`、`下線`、`取り消し線`を追加した。
  本文用は`フォント名`の直下、ルビ用は`フォント名（ルビ）`の直下へチェックボックスとして配置する。
  既存表示を維持するため太字だけを初期値オンとし、他はオフとする。各設定をWindowsフォント生成へ
  反映し、斜体・下線によって描画画素が変化するテスト、既存の音楽同期・描画・フレーム補間テストが
  すべて成功した。FilterもDebug Win64で警告0、エラー0のビルドに成功した。
- 2026-07-28: AviUtl2実機で、同じレイヤーへ連続配置した2つの専用Input＋Filterオブジェクトを
  AI MIRAIから確認した。第1オブジェクトは0～201フレーム、第2オブジェクトは202～422フレームで、
  歌詞と同期設定が別々に保持されていた。100フレームと300フレームを交互に各3回レンダリングし、
  第1オブジェクトではルビ付き歌詞、第2オブジェクトでは`2番目の歌詞`が毎回描画された。同じ
  フレームの画像SHA-256も3回すべて一致し、オブジェクト間の設定値または描画状態の混線はなかった。
- 2026-07-28: 第2オブジェクトだけ曲同期GUIへノートが表示されず、描画も同期しない現象を調査した。
  第2Inputの素材再生開始は6.700秒、`事前表示 (秒)`は1.00秒であり、Filterが同期対象にするノートは
  曲先頭7.700秒以降となる。指定された`min_full_test.musicxml`は約4秒で終了するため対象ノートがなく、
  キャッシュではなく同期元データと素材再生位置の範囲不一致が直接原因だった。また曲同期GUIは
  オブジェクト先頭秒ではなく最後に描画した現在フレーム秒を`FAnchorSeconds`へ渡しており、
  カーソル位置によってGUI表示範囲がずれる別の問題も確認した。
- 2026-07-28: 曲同期GUIで利用可能なノート数が同期グループの必要音数に足りない場合、
  最初に割り当てられない表示単位以降の歌詞を、画面右端へ赤系の`未割当:`表示として追加した。
  1表示単位へ複数音を割り当てたグループは必要音数を一部しか満たさない場合も未割当とする。
  全歌詞へ割り当て可能な場合は警告を表示しない。判定テストは成功し、FilterもDebug Win64で
  警告0、エラー0のビルドに成功した。
- 2026-07-28: WAV手動同期の速度変更再生準備として、VideoMinerが使用するGyan.dev版
  FFmpeg 8.1.1 full shared buildの必要DLL 6個と、Delphi用`FFmpegApi`／
  `FFmpegAudioTempo`をコピーした。DLLは`ThirdParty/FFmpeg/bin`へ置き、GitHubの
  単一ファイル上限を超える`avfilter-11.dll`を含むためGit LFS対象とした。プロジェクトの
  ルートライセンスをGitHubが検出可能な正式GPLv3全文とし、FFmpegの版、配布元、対応ソース、
  ビルド情報、同梱DLLを`THIRD_PARTY_NOTICES.md`へ記録した。
- 2026-07-28: 手動同期の入口として、`音楽ファイル`選択欄へWAV、MP3、FLAC、M4A、
  AAC、OGG、Opus、WMAを追加した。曲同期設定ボタンは既知の楽譜拡張子を従来フォームへ、
  その他のファイルを遅延ロードしたFFmpegで検査し、音声ストリームがあれば新しい手動同期
  フォームへ振り分ける。手動同期フォームはこの段階でファイル名、再生時間、サンプルレート、
  チャンネル数を表示し、波形・再生・境界編集はまだ行わない。1秒のWAVと変換したMP3で
  FFmpegプローブが再生時間1.000秒、44100Hz、2chを取得することを確認した。楽譜拡張子判定
  テストは成功し、FilterもDebug Win64で警告0、エラー0のビルドに成功した。
- 2026-07-28: 手動同期フォームへ音声全体の静的波形を追加した。FFmpegで入力音声を
  PCM16 stereo 48kHzへ順次変換し、PCM全体は保持せず1024フレームごとのmin/maxブロックへ
  縮約してから最大4096点の包絡線にする。フォームは表示幅ごとに再縮約し、全体ピークを基準に
  自動スケールした青色波形、中央線、4分割時間目盛を描く。WAVとMP3の両方で47点の包絡線を
  生成するテストが成功し、FilterもDebug Win64で警告0、エラー0のビルドに成功した。
- 2026-07-28: 手動同期フォームへ`再生`／`停止`と`1.0`、`0.5`、`0.25`倍速を追加した。
  FFmpegでPCM16 stereo 48kHzへ小分けにデコードし、全音声を保持せずwaveOutへ約1秒先まで
  ストリーミングする。atempoが短い入力を出力しない問題を避けるため2秒分を変換単位とし、
  0.25倍速はatempoの下限0.5を満たすよう0.5×0.5の2段構成にした。再生位置は30ms間隔で
  数値と黄色の波形カーソルへ反映する。PCMデコードと0.25倍速変換のテストに成功し、
  FilterもDebug Win64で警告0、エラー0のビルドに成功した。
- 2026-07-28: 手動同期編集の第1実装として、歌詞の発音表示単位数+1本の境界をWAV全体へ
  等間隔で初期配置した。完成区間は波形下へ右向き矢印付きの歌詞枠として表示する。
  `調整モード`ではピンク色の境界線を前後の境界を越えない範囲でドラッグできる。
  `タイミング入力`では再生中のSpaceで位置を記録し、そのセッションの最初のSpaceを押した時だけ
  初期値または既存値をクリアする。モードを往復しても入力途中の位置は保持し、`打ち直し`後は
  次のSpaceで改めてクリアする。再生ループ、Pの再生/停止、Sの速度循環、確定/キャンセルも追加した。
  手動同期データを対象Filterの歌詞と同期データへ書き戻し、描画時にWAV先頭基準秒として適用する。
  編集モデルの初期配置・入力・再入力・ドラッグ・保存テストが成功し、FilterもDebug Win64で
  警告0、エラー0のビルドに成功した。
- 2026-07-28: 手動同期フォームで停止後または速度変更後に再生すると先頭へ戻る動作を修正した。
  再生開始時に黄色カーソルのWAV秒を音声プレイヤーへ渡し、FFmpegのストリーミングデコード結果を
  対応サンプル位置まで読み飛ばしてからwaveOutへ投入する。再生位置表示には開始秒を加算するため、
  停止・再開と速度変更のどちらでも同じカーソル位置を維持する。曲末の自動ループ時だけは従来どおり
  0秒へ戻す。FilterはDebug Win64で警告0、エラー0のビルドに成功した。
- 2026-07-28: 手動同期波形の初期横軸を音声全体から10秒へ変更した。通常のマウスホイールは
  カーソル位置のWAV秒を固定したまま表示幅を0.8倍／1.25倍し、1～60秒の範囲で拡大縮小する。
  Shift+ホイールは表示幅の25%ずつ時間軸を左右移動する。波形、目盛、同期線、歌詞枠、
  再生カーソル、ドラッグ座標は同じ表示開始秒と表示幅を共有する。再生カーソルが右端を越えた場合は
  次の範囲へ自動追従し、ループ時は0秒表示へ戻す。FilterはDebug Win64で警告0、エラー0の
  ビルドに成功した。
- 2026-07-28: 再生操作を`停止`と`再生／一時停止`へ分離した。一時停止は黄色カーソル位置を
  維持し、再生で同じ位置から再開する。停止は音声を止め、再生カーソルと表示範囲を0秒へ戻す。
  波形内の空き位置または黄色カーソルを左ドラッグすると再生中でも一時停止し、その位置へ
  カーソルを移動できる。黄色線上端へドラッグ対象を示す三角形を追加した。
  また、新規同期位置は音声全体ではなく初期表示の10秒内へ等間隔配置する。保存済み同期位置は
  従来どおりその値を読み込む。編集モデルテストとFilterのDebug Win64ビルドは警告0、
  エラー0で成功した。
- 2026-07-28: 手動同期波形と曲同期ピアノロールの背景ドラッグへ時間軸移動を追加した。
  手動同期では黄色の再生カーソル、調整モードの同期線を先にヒットテストし、それ以外の位置から
  左ドラッグした場合だけ表示開始秒を移動する。再生中に背景移動を始めた場合はカーソル位置を
  維持して一時停止する。ピアノロールでは事前表示線、上下の同期歌詞を優先し、それ以外の背景を
  左ドラッグすると表示開始オフセットを移動する。どちらも表示可能範囲外へは移動しない。
  FilterはDebug Win64で警告0、エラー0のビルドに成功した。
- 2026-07-29: Input開始時間の同期経路を`SYNC_PianoRoll`の修正と比較監査した。
  `SYNC_Lyrics`は既にInputが受け取った開始時間込みフレームを歌詞同期へ使用しており、
  開始時間を`Object_.Time`で上書きする問題がないことを確認した。開始時間を含む非ゼロフレームの
  回帰テストを維持し、新規オブジェクトが1秒より古い共有値を基準へ採用しないテストを追加した。
  FrameContext、MusicSync、Rendererの各テストはPASSし、Input／FilterともWin64の
  Debug・ReleaseビルドおよびAviUtl2プラグインフォルダーへの配置に成功した。
- 2026-07-29: 文字自由配置の第1段階として、`タイプ`へ`文字自由配置`を追加し、共通の
  `表示設定`ボタンと、GUIへ表示されない1024バイトの`data`項目`表示設定データ`を実装した。
  文字自由配置を選んだ時だけデバッグGUIを開き、TMemoのUTF-8文字列を固定長データへ保存して
  再表示する。文字列の往復、Unicode、サイズ上限、データABIサイズの自動テストはPASSし、
  FilterはWin64のDebug・Releaseとも警告0、エラー0でビルドと配置に成功した。AviUtl2実機でも
  保存・再読込が正常に行われることを確認した。
- 2026-07-29: 文字自由配置の背景画像取得を検証する最小実装を追加した。最初に
  `GetImageData`を試したが、これは編集画面全体ではなく透明な歌詞オブジェクト自身の入力画像を
  取得するため、実機では黒く表示された。取得元を合成済みの`GetFramebufferTexture2D`へ変更し、
  D3D11ステージングテクスチャを解像度・形式が変わらない限り再利用してCPU側へ読み出す。
  最後に発火したFilterのフレームバッファを保持し、`表示設定`を開いた時だけRGBAへ変換して
  デバッグGUIへ表示する。取得解像度とDXGI形式、失敗理由もフォーム上部へ表示する。
  FilterのWin64 Debug・Releaseビルドは警告0、エラー0で成功し、Release版を配置した。
  実機で1920x1080、DXGI形式10の合成済み画像を取得できた。初回表示ではVCL側の不要な
  上下反転と追加のsRGB変換により上下と色がずれたため、どちらも除去して再配置した。
- 2026-07-29: 文字自由配置GUIをデバッグ用テキスト編集から、実際の配置編集の最小版へ変更した。
  既存の歌詞パーサーにより通常文字を1文字、ルビ付き範囲を1グループとして背景中央へ横一列に
  初期配置する。背景上へ本文・ルビ・選択枠を描画し、クリックした単一要素を左ドラッグして
  X・Y座標を変更できる。非表示data項目の1024バイトABIは維持し、内部をバージョン2の
  固定長バイナリへ変更した。要素ごとにインデックス・X・Yを保持し、最大100要素を保存する。
  歌詞UTF-8のFNV-1aハッシュと要素数が一致する場合だけ復元し、歌詞変更時は初期配置へ戻す。
  データ往復・歌詞変更・上限超過テストはPASSし、FilterのWin64 Debug・Releaseビルドは
  警告0、エラー0で成功した。Release版をプラグインフォルダーへ配置した。
- 2026-07-29: 自由配置GUIでルビ付き要素だけ本文のY位置が下へずれる問題を修正した。要素の
  Y座標を本文領域の中央として扱い、本文の下端はルビの有無にかかわらず同じ位置へ揃え、
  ルビだけを上方向へ追加する。続けて自由配置専用レンダラーを実装し、保存済みの要素別X・Yを
  `文字自由配置`の実際のFilter出力へ反映した。本文とルビは同じ同期進捗で変化前色から
  変化後色へ進み、従来の全体X・Yと開始・終了演出も最終オフセットとして適用する。
  配置データが未保存または歌詞と一致しない場合は従来の横一列描画へフォールバックする。
  自由配置座標とルビ有無による本文下端一致の描画テスト、既存の音楽同期・描画テストはPASSし、
  FilterのWin64 Debug・Releaseビルドは警告0、エラー0で成功した。Release版を配置した。
- 2026-07-29: 文字自由配置の各要素へ横倍率・縦倍率を追加した。GUIでは選択枠の四隅と各辺へ
  8個のハンドルを表示し、辺のドラッグで片方向、四隅で縦横を変更する。ドラッグしていない反対側の
  辺を固定し、倍率は0.05～10.0に制限する。配置データをバージョン3へ更新し、X・Yは符号付き
  16bit、倍率は1000分率で保存することで、1024バイトdata内の最大100要素を維持した。
  バージョン2の保存データはX・Yを引き継ぎ、倍率1.0として読み込む。自由配置レンダラーでは
  GDIワールド変換により縦横倍率を個別適用し、本文・ルビ・同期色クリップを同じ倍率で描画する。
  データ往復、横拡大・縦縮小、既存描画のテストはPASSし、FilterのWin64 Debug・Release
  ビルドは警告0、エラー0で成功した。Release版をプラグインフォルダーへ配置した。
- 2026-07-29: サイズ変更後に自由配置GUIの文字と選択枠がずれる問題を修正した。VCL Canvasの
  フォント状態とHDCの状態が`SaveDC`／`RestoreDC`後に一致しなくなることが原因だったため、
  GUI描画では状態復元を使わず、要素描画後にワールド変換だけを単位行列へ戻すようにした。
  本文とルビは同じワールド変換内で描画するため、どちらも同じ縦横倍率で変化する。
  FilterのWin64 Debug・Releaseビルドは警告0、エラー0で成功し、Release版を配置した。
- 2026-07-29: 文字自由配置GUIへ複数選択と一括移動を追加した。背景の右ドラッグで選択矩形を
  表示し、矩形と交差する全要素を選択する。Shiftを押しながら右ドラッグすると既存選択へ追加し、
  Shift+左クリックでは個別要素の追加・解除ができる。複数選択時は各要素の黄色枠に加えて
  全体を囲む水色の破線枠を表示し、選択要素のいずれかを左ドラッグすると相対位置を維持したまま
  全選択要素を同じ移動量で動かす。サイズハンドルは単一選択時だけ表示する。
  既存の音楽同期・描画テストはPASSし、FilterのWin64 Debug・Releaseビルドは警告0、
  エラー0で成功した。Release版をプラグインフォルダーへ配置した。
- 2026-07-29: 複数選択の水色全体枠にも8個のサイズハンドルを追加した。グループの辺または
  四隅をドラッグすると、反対側の辺を固定し、各要素の中心座標と縦横倍率をグループ倍率で
  変換する。これにより要素同士の相対配置と、それぞれが既に持つサイズ比率を維持したまま
  グループ全体を拡縮できる。いずれかの要素が0.05～10.0倍の範囲を超えないようグループ倍率を
  制限する。FilterはWin64 Debug・Releaseとも警告0、エラー0でビルドに成功し、
  Release版をプラグインフォルダーへ配置した。
- 2026-07-29: 文字自由配置GUIの右側へ配置要素一覧を追加した。通常文字は1文字ずつ、
  ルビ付き範囲は`[本文](ルビ)`の1グループとして表示し、画面上の選択と一覧の複数選択を
  双方向に同期する。画面外の要素も一覧から選択できる。復旧操作として、選択要素の相対配置を
  保ったまま画面中央へ移動、選択要素だけを初期位置・初期倍率へ復元、全要素を初期配置へ
  復元する3操作を追加した。既存の音楽同期・描画テストはPASSし、FilterのWin64
  Debug・Releaseビルドは警告0、エラー0で成功した。Release版を配置した。
- 2026-07-29: 再配置操作へ`選択を横一列へ`と`選択間隔を均等に`を追加した。横一列への
  整列はルビの有無や縦倍率が異なる要素でも本文の下端が揃うように配置する。均等配置は
  左右端の要素位置を維持し、各要素の表示幅を差し引いた空白部分が等間隔になるよう、
  選択要素を画面上の左から右の順で再配置する。どちらも個別の縦横倍率は変更しない。
  既存の音楽同期・描画テストはPASSし、FilterのWin64 Debug・Releaseビルドは
  警告0、エラー0で成功した。Release版を配置した。
- 2026-07-29: 文字自由配置GUIへ編集表示のズームとパンを追加した。編集画面上の
  マウスホイールで0.25～8倍へ拡大・縮小し、マウス位置の背景座標が動かないよう表示位置を
  補正する。文字やサイズハンドルのない背景を左ドラッグすると、拡大中の表示位置を移動する。
  これらは編集用の表示変換だけを変更し、保存する文字座標や縦横倍率には影響しない。
- 2026-07-29: `TPaintBox`に公開されていない`OnMouseWheel`をDFMへ記述したため、
  フォーム生成時にイベントが存在しないというストリーム読み込みエラーが発生していた。
  DFMの割り当てを削除し、フォームのホイールイベントを実行時に割り当て、マウスが編集画面内に
  ある場合だけズーム処理するよう修正した。
- 2026-07-29: 右側へ選択要素の数値設定を追加した。単一選択ではX・Yと横・縦倍率を
  パーセント指定できる。複数選択では相対配置を意図せず失わないようX・Yを無効にし、
  横・縦倍率だけを全選択要素へ一括適用する。選択要素の倍率が一致しない欄は空欄で表示し、
  値を入力した軸だけを変更する。座標と倍率は保存形式の範囲内へ制限する。
- 2026-07-29: 既存の本文・ルビ用フォント選択画面を文字自由配置GUIへ統合した。当初は
  行全体の共通フォントを変更するボタンとしたが、他の操作と同様に現在の選択要素を対象とする
  `フォント...`へ変更した。単一または複数の選択要素へ本文・ルビフォントを適用し、
  自由配置プレビューと実描画へ反映する。保存データをバージョン5へ更新し、フォント名辞書と
  同じ本文・ルビフォント組み合わせを使う要素のビットマップで、個別フォントを1024バイト内へ
  圧縮保存する。離れた要素を多数選択して同じフォントを指定しても要素数に比例して容量を
  消費しない。未指定要素は従来のFilter共通フォントを使用し、バージョン2・3・4の配置データも
  引き続き読み込む。実描画では同じフォント名のHFONTを1フレーム内で共有する。
- 2026-07-29: `D:\DelphiProg\Lib\ColorPicker`を調査し、Win64 Releaseでエラー0の
  ビルドを確認した。個別の同期前色・同期後色設定へ利用するため、必要な6ユニットとDFM、
  MITライセンスを`Source\Lib\ColorPicker`へコピーした。サンプル、旧版スポイト、
  プロジェクトファイル、ビルド成果物は除外した。イベント連鎖、RGB入力補正、HSV色相表示、
  DPIレイアウト、余分な`end.`、スポイトのマルチモニター・安全な終了・DPI座標等を
  統合前の修正事項として記録した。
- 2026-07-29: 取り込んだカラーピッカーをプラグイン向けに修正した。ソースとDFMをUTF-8へ
  統一し、RGB・HSV変換の共通ユニット、更新中ガード、RGB範囲表示補正、標準HSV色相バー、
  彩度・明度を維持する色相変更、320×267のダイアログ、`ExecuteColorPicker`を追加した。
  スポイトは仮想スクリーンと負座標、`CAPTUREBLT`へ対応し、イベント内ではフォームを
  `Release`して遅延解放する。Filterプロジェクトへ全ユニットを登録し、Win64 Debugは
  警告0、エラー0で成功した。HSV原色、RGB・HSV往復、ダイアログ生成・初期色往復の
  専用テストと既存の音楽同期・描画テストはPASSした。FilterのWin64 Debug・Releaseは
  警告0、エラー0で成功し、Release版を配置した。異なるDPIと実マルチモニターでの
  スポイト操作は実機確認として残す。
- 2026-07-29: WAV手動同期の再生速度から遅すぎる`0.25`倍を削除し、代わりに`0.75`倍を
  追加した。速度選択とSキーによる循環は`1.0`、`0.75`、`0.5`倍の順とし、音声プレイヤーの
  許可値も同じ3種類へ統一した。0.75倍速のPCM変換テストはPASSし、FilterのWin64
  Debug・Releaseは警告0、エラー0で成功した。Release版をプラグインフォルダーへ配置した。
- 2026-07-29: WAV手動同期画面を本番向けに整理した。見出し、フルパス、個別の再生時間、
  サンプルレート、チャンネル数、再生操作見出しを削除し、対象ファイル名と操作列を上部へ
  コンパクトに配置して波形表示を300pxから405pxへ拡大した。フルパスはファイル名の
  ツールチップで確認できる。波形上のカーソルは通常矢印とし、時間軸を実際にドラッグしている
  間だけ左右矢印、再生線・同期線上だけ調整カーソルを表示するよう修正した。
  FilterのWin64 Debug・Releaseコンパイルは警告0、エラー0。AviUtl2が起動中で`.auf2`が
  使用中のため、今回のRelease版配置は再起動後に行う。
- 2026-07-29: WAV手動同期の波形キャンバス内だけ文字が小さくなる問題を修正した。
  時間目盛りと歌詞ラベルのフォント高に加え、描画余白、目盛り位置、歌詞レーン高、
  ラベル内余白、矢印形状、同期線長、再生位置マーカー、線のドラッグ判定幅を96 DPI基準から
  フォームの`CurrentPPI`へ換算する。高DPIでも文字とその表示枠・操作範囲が同じ比率になる。
  FilterのWin64 Debug・Releaseビルドは警告0、エラー0で成功した。
- 2026-07-29: 歌詞テロップ表示プラグインの設定項目整理方針を検討した（未実装）。
  スタイル決定後は他オブジェクトへのコピーまたは独自GUIのユーザースタイル登録で再利用する
  ことを前提とし、ホストへフォントや色の設定値を1項目ずつ大量に公開する方式をやめる。
  フォント、文字装飾、色、文字サイズ、間隔などの表示スタイルは、INI風の文字列リストとして
  一括保存し、独自の表示設定GUIで編集する。表示方式に影響する`タイプ`と、オブジェクトごとに
  変化させる可能性があるアニメーション関係は従来どおり個別設定として残す。

  現在の設定項目は上から順に次のように整理する。

  - `曲同期`：同期方式の選択に必要なため残す。
  - `歌詞`：オブジェクトがどの歌詞データを使用するか識別するため残す。
  - `音楽ファイル`：同期元の指定に必要なため残す。
  - `トラック (-1=全て)`：値は必要。ただし曲同期画面内で設定できるため、独立項目から
    同期GUIへ移動する案を検討する。
  - `X`・`Y`：元のオブジェクト側にも位置設定があるため、重複の必要性と役割を再検討する。
  - `フォント設定`、本文・ルビの`フォント名`、`太字`、`斜体`、`下線`、`取り消し線`：
    ホストの個別項目から削除し、表示設定GUIへ移動する。
  - `変化前色`・`変化後色`などの色：ホストの個別項目から削除し、表示設定GUIへ移動する。
  - `タイプ`：表示方式そのものを選ぶ項目なので残す。
  - `表示設定`：入口として残す。曲同期設定と同じGUIへ統合するか、別GUIを維持するかは
    操作の流れを見て検討する。
  - `同期演出`、`開始演出`、`開始演出時間`、`終了演出`、`終了演出時間`：
    アニメーションと時間に関係するため個別設定として残す。
  - `歌詞サイズ`、`ルビサイズ`、`ルビ間隔補正`、`字間`：
    ホストの個別項目から削除し、表示設定GUIへ移動する。
  - `事前表示 (秒)`：同期方式や表示方法の設計次第では値そのものが不要になる可能性があるため、
    残すか表示設定／同期GUIへ移すかを含めて再検討する。
  - `曲同期設定`：同期編集GUIを開く入口として必要。`表示設定`とのGUI統合も検討する。
  - `同期データ`：通常の文字列設定から、非表示の`Data`型保存領域へ移動する。
- 2026-07-30: 文字別表示スタイル実装の最初として、文字自由配置の各要素へ同期前色と同期後色を
  追加した。単一または複数選択から専用カラーピッカーを開き、選択した色を一括適用できる。
  共通色と同じ値は個別値を持たず、Filterの共通色を継承する。表示設定データをバージョン6へ更新し、
  同じ同期前色・同期後色の組み合わせを持つ要素をビットマップでグループ化して1024バイト内へ保存する。
  バージョン2～5は引き続き読み込める。自由配置GUIのプレビューと実際のFilter描画は文字別色を使用し、
  ルビも対応本文と同じ色で描画する。最大100要素の色・フォント混在データ往復と文字別色描画を
  自動テストへ追加した。MusicSync・RendererテストはPASSし、FilterのWin64 Debug・Release
  ビルドは警告0、エラー0で成功した。Release版をプラグインフォルダーへ配置した。
- 2026-07-30: 文字別色ボタンの動的キャプションが文字化けしたため、Pascalソースの文字コードに
  依存する日本語リテラルをやめ、Unicodeコードポイントから表示文字列を組み立てるよう修正した。
  続けて文字自由配置の各要素へ本文サイズとルビサイズを追加した。単一または複数選択へ
  1～1024pxを一括指定でき、Filterの共通サイズと同じ値は個別値を持たず継承する。
  表示設定データをバージョン7へ更新し、同じ本文・ルビサイズの組み合わせを持つ要素を
  ビットマップでグループ化して1024バイト内へ保存する。バージョン2～6も引き続き読み込む。
  GUIの文字寸法、選択枠、整列処理、実際のFilter描画へ個別サイズを反映した。
  同じフォント名で異なるサイズを使う場合に備え、1フレーム内フォントキャッシュのキーへ
  サイズと太字・斜体・下線・取り消し線を追加した。最大100要素のフォント・色・サイズ混在データ
  往復と文字別サイズ描画を自動テストへ追加した。MusicSync・RendererテストはPASSし、
  FilterのWin64 Debug・Releaseビルドは警告0、エラー0で成功した。Release版を
  プラグインフォルダーへ配置した。
- 2026-07-30: 文字自由配置の各要素へ本文・ルビそれぞれの太字、斜体、下線、取り消し線を
  追加した。既存の`フォント...`画面へ本文用とルビ用の装飾チェックを追加し、フォント名と
  装飾を同じ画面で編集する。単一または複数選択へ一括適用でき、Filterの共通装飾と同じ
  組み合わせは個別値を持たず継承する。表示設定データをバージョン8へ更新し、同じ本文・ルビ
  装飾の組み合わせを持つ要素をビットマップでグループ化して1024バイト内へ保存する。
  バージョン2～7も引き続き読み込む。自由配置GUIの文字寸法・プレビューと実際のFilter描画へ
  個別装飾を反映し、最大100要素のフォント・色・サイズ・装飾混在データ往復と文字別装飾描画を
  自動テストへ追加した。MusicSync・RendererテストはPASSし、FilterのWin64 Debug・Release
  ビルドは警告0、エラー0で成功した。Release版をプラグインフォルダーへ配置した。
- 2026-07-30: 文字自由配置の選択操作へ2種類の状態を追加した。未選択要素をクリックすると
  黄色の選択状態1になり、選択済み要素を移動せず再クリックすると水色の選択状態2へ切り替わる。
  状態1のドラッグは従来どおり移動・変形、状態2の横ドラッグは選択要素のルビ字間調整とする。
  ルビのない要素を含む場合は状態2へ移行せず、複数選択時は全要素に同じ操作を適用する。
  表示設定データをバージョン9へ更新し、要素別ルビ字間を値と対象ビットマップで圧縮保存する。
  バージョン2～8は引き続き読み込む。GUIプレビューと実際のFilter描画へ字間を反映し、
  保存往復と描画幅変化の自動テストを追加した。MusicSync・RendererテストはPASSし、
  FilterのWin64 Debug・Releaseビルドは警告0、エラー0で成功した。Release版を
  プラグインフォルダーへ配置した。選択状態2への切替直後に再描画して水色を即時反映し、
  状態2の左右ハンドルは選択枠の縦中央へ配置する。水色モードのマウス判定は選択枠全体ではなく、
  左右それぞれのハンドル中心に判定範囲を設け、枠線Xから内外へ7pxずつ反応させる。
  本番用になった表示設定フォームからDebug名称を除去した。次の段階は文字サイズと
  縦横比の正規化である。
- 2026-07-30: 文字自由配置の選択操作を3状態へ拡張した。黄色の状態1は移動・変形、水色の
  状態2は前後の配置要素との文字間、紫の状態3はルビ専用とする。状態2では左右ハンドルを
  独立判定し、単一選択は左側または右側の隣接要素、複数選択は選択範囲内のX間隔を調整する。
  状態3では左右ハンドルでルビ字間、上中央ハンドルで本文に対するルビの相対X・Y位置を調整する。
  ルビのない要素を含む選択では状態3へ移行しない。表示設定データをバージョン10へ更新し、
  ルビ字間と相対X・Yを対象要素ビットマップで圧縮保存する。バージョン2～9は引き続き読み込む。
  GUIプレビューと実描画へ相対位置を反映し、保存往復、ルビ字間、ルビ位置の自動テストを追加した。
  MusicSync・RendererテストはPASSし、FilterのWin64 Debug・Releaseビルドは警告0、
  エラー0で成功した。Release版をプラグインフォルダーへ配置した。
- 2026-07-30: 肥大化した文字自由配置フォームを整理した。現在の画面を文字単位の自由配置専用と
  明確にするため、ユニットを`SYNC_Lyrics_CharacterLayoutSettingsForm`、フォームクラスを
  `TFormLyricsCharacterLayoutSettings`へ改名した。将来の1行単位表示画面には
  `SYNC_Lyrics_LineDisplaySettingsForm`を使用し、同じフォームへ混在させない。
  選択モード遷移、ハンドル判定、文字間・ルビ字間・ルビ位置のドラッグ計算を
  `SYNC_Lyrics_CharacterLayoutInteraction`へ、選択色とハンドル描画を
  `SYNC_Lyrics_CharacterLayoutDrawing`へ分離した。Interaction処理の自動テストを追加して
  PASSし、旧Debugフォームおよび汎用的な旧フォーム名は削除した。現在残る数値入力欄は
  GUI完成までの暫定確認用であり、機能拡張せず最終的に削除する。
- 2026-07-30: 水色の選択状態2を、配置要素間の座標調整ではなく、選択要素内の本文（漢字側）の
  字間調整へ修正した。文字サイズ、縦横倍率、要素座標を維持したまま字間値だけを変更し、
  `-1024`～`1024`の負値・正値を許可する。中央基準描画と本文の文字間数を考慮して
  ハンドル移動量から字間を逆算し、操作した枠線の内側へ本文端が追従するよう修正した。
  複数選択では各選択要素の文字間数に応じた変化量を適用する。ルビ字間の端追従計算は保留する。
  表示設定データをバージョン11へ更新し、本文字間を既存のルビ字間・ルビ相対位置と同じ
  対象要素ビットマップ方式で圧縮保存する。バージョン2～10は引き続き読み込む。
  GUIプレビューと実描画へ反映し、正負の本文字間、文字高さ不変、保存往復、バージョン10読込の
  自動テストを追加した。MusicSync・RendererテストはPASSし、FilterのWin64 Debug・Release
  ビルドは警告0、エラー0で成功した。Release版をプラグインフォルダーへ配置した。
  1024バイト上限に対する追加最適化と限界検証は後の段階へ保留した。
- 2026-07-30: β版のため旧形式との互換性を不要とし、文字自由配置の保存先を1024バイト固定長の
  `data`項目からTEdit相当の単行`string`項目`表示設定`へ移行した。改行を含まない`SL1`形式へ
  歌詞ハッシュ、配置座標、倍率、フォント、色、サイズ、装飾、本文・ルビ字間、ルビ相対位置を
  シリアライズする。旧Data項目とバージョン2～11の読込処理はプラグイン登録・実行経路から廃止した。
  100配置の全設定を埋めた1024文字超・32,767文字未満の単行文字列について、Unicodeフォント名を
  含む保存復元と歌詞変更時の拒否を自動テストし、MusicSync・RendererテストはPASSした。
  AviUtl2 SDKの`string`項目には明記された最大長がないため、Windows単行Editの既定入力上限に
  合わせてプラグイン側の保存上限を32,767文字とした。
- 2026-07-30: 本文字間を広げた際、水色枠の右端と本文末尾の間へ中央の字間と同じ余白が残る問題を
  修正した。GDIの文字幅計測に含まれる末尾1文字分の`SetTextCharacterExtra`を可視幅から除外し、
  プレビューの自然幅・中央配置・選択枠と実描画を同じ「文字数−1個の字間」定義へ統一した。
- 2026-07-30: 利用者による実機確認で、1文字ごとの自由配置モードの操作がすべて正常に
  動作することを確認した。
- 2026-07-30: Syncroh2の`ToolbarButtons.pas`を基礎に、文字装飾GUI用の独立ユニット
  `SYNC_Lyrics_ToolbarButtons.pas`を追加した。画像・描画・フォーム組込はまだ行わず、コマンド、
  トグル、ダイアログ、区切りの種別、未選択・選択・混在状態、将来描画する装飾Glyph、動的色、
  キーボード実行、個別・ツールバー共通通知を定義した。モーダルダイアログを安全に開けるよう、
  左MouseUpで押下状態を解除してから処理を実行する。Delphi 37 Win64単体コンパイルは成功した。
- 2026-07-30: `SYNC_Lyrics_ToolbarButtons`へWord風の独自Glyph描画を実装し、文字自由配置フォーム
  上部へ配置した。フォント、太字、斜体、下線、取り消し線、同期前後色と、中央移動、選択初期化、
  全初期化、横一列、均等配置を既存処理へ接続した。装飾は本文とルビへ同時適用し、複数選択の
  混在状態、現在色の下線、ホバー・押下・選択・無効・フォーカス状態を描画する。既存の右側ボタンと
  設定欄は移行確認のため残した。フォーム画像を生成して配置とGlyphを目視確認し、ツールバー状態の
  自動テストを追加した。
- 2026-07-30: 文字自由配置の全設定操作をキャンバスのマウス操作、上部アイコンツールバー、
  フォント・色ダイアログから行えることを確認し、右側の暫定数値設定欄と重複ボタンを削除した。
  右側は画面外要素の選択・復旧にも使う配置要素一覧へ一本化し、一覧をパネル全高へ拡張した。
  配置要素の最終矩形はマウス変形で決め、本文・ルビの基準文字サイズと縦横倍率は正規化せず
  現在の内部モデルを維持する。MusicSync・RendererテストはPASSし、FilterのWin64 Debug・
  Releaseビルドは警告0、エラー0で成功した。Release版をプラグインフォルダーへ配置した。
- 2026-07-30: 利用者による実機確認で、ルビサイズを含む配置グループの大きさもマウスから
  目的どおり調整できることを確認し、1文字ごと表示GUIを完成扱いとした。
- 2026-07-30: 1行表示設定の検討開始に先立ち、歌詞入力だけを持つ最小デバッグフォーム
  `SYNC_Lyrics_LineDisplaySettingsForm`を追加した。`文字自由配置`以外で`表示設定`を押すと
  このフォームを開き、OK時に歌詞項目を保存するとともに、AviUtl2 SDK 2.12の
  `EDIT_SECTION.set_object_name`で選択中のFilterオブジェクト名を同じ歌詞へ変更する。
  Delphi側の`TEDIT_SECTION`をSDKのフィールド順に従って同APIまで拡張した。Filterの
  Win64 Debugビルドは警告0、エラー0で成功し、Debug版をプラグインフォルダーへ配置した。
  利用者によるAviUtl2実機確認で、入力したルビ構文を含む歌詞がタイムライン上の
  オブジェクト名へ反映されることを確認した。
- 2026-07-30: 1行表示設定フォームへ、最後に受け取ったフレームを背景とする簡易プレビューを
  追加した。入力中の歌詞を既存のルビ構文解析へ通し、Filterの共通フォント、サイズ、装飾、
  同期前色、ルビ間隔を使って本文と全ルビを中央表示する。本文クリックでは本文全体を黄色、
  いずれかのルビクリックでは全ルビを紫の選択枠で示す。設定変更操作と保存データの追加は
  まだ行わない。FilterのWin64 Debugビルドは警告0、エラー0で成功し、Debug版を
  プラグインフォルダーへ配置した。利用者によるAviUtl2実機確認で、本文・ルビの表示と
  本文全体の選択が意図どおり動作することを確認した。
- 2026-07-30: 1行配置と文字自由配置を同期演出から分離するため、共通表示単位の設計を確定した。
  既存の`TLyricsDisplayUnit`は歌詞範囲と音の対応だけを持つ論理同期単位として維持し、
  配置モード別の解決後に本文・ルビの座標、矩形、倍率、継承解決済みスタイルを持つ
  `TLyricsResolvedDisplayUnit`相当の実行時モデルへ変換する。通常カラオケ、文字単位強調、
  1文字ずつ出現は同じ共通単位へ一時状態として適用し、開始・終了演出は単位列全体へ適用する。
  共通単位自体は保存せず、1行設定と自由配置設定から実行時に生成する方針とした。
- 2026-07-30: 共通表示単位の第1段階として
  `SYNC_Lyrics_ResolvedDisplayUnits`を追加した。歌詞構文と論理同期単位から、本文・ルビ文字列、
  元文字列範囲、同期番号、音消費有無、座標、倍率、ルビ相対位置、継承解決済みのフォント・装飾・
  字間・同期前後色を持つ実行時単位列を生成する。1行配置と文字自由配置の両描画経路を同じ生成処理へ
  接続し、自由配置描画内に重複していた個別設定の継承解決を共通処理へ移した。行配置の既定値、
  自由配置の全個別上書き、配置数不一致を自動テストへ追加した。RendererテストはPASSし、
  FilterのWin64 Debugビルドは警告0、エラー0で成功してDebug版をプラグインフォルダーへ配置した。
- 2026-07-30: 共通表示単位の第2段階として、本文・ルビの描画原点と測定済み矩形、単位基準点、
  本文とルビを囲むグループ矩形、全単位を囲む行矩形を実行時モデルへ追加した。1行配置では
  全文の字間を維持したまま論理同期単位ごとの本文範囲と対応ルビ位置を測定し、通常カラオケの
  本文クリップ幅、ルビ位置、ルビクリップ幅を解決済み単位から求める。文字自由配置でも個別の
  座標・縦横倍率・ルビ相対位置を反映したシーン上の各矩形と行矩形を生成する。既存の描画回帰
  テストはすべてPASSし、FilterのWin64 Debugビルドは警告0、エラー0で成功してDebug版を
  プラグインフォルダーへ配置した。
- 2026-07-30: 配置モードから独立した単位演出状態`TLyricsUnitEffectState`を追加した。
  通常カラオケ、文字単位強調、1文字ずつ出現について、同期前レイヤーの表示有無と同期後色の
  クリップ進捗を共通処理で解決し、基本配置を変更しない一時状態として扱う。1行配置と文字自由配置の
  両描画経路を同じ状態へ接続したため、自由配置でも強調は対象単位全体を同期後色にし、
  1文字ずつ出現は未到達単位を非表示にする。共通状態の直接テストと両配置モードの描画回帰テストは
  すべてPASSし、FilterのWin64 Debugビルドは警告0、エラー0で成功してDebug版を
  プラグインフォルダーへ配置した。
- 2026-07-30: Filter標準GUIの旧`タイプ`を、`配置モード`の1行配置／文字自由配置と、
  `同期演出`の通常カラオケ／文字単位強調／1文字ずつ出現へ分離した。従来から存在した
  なし／バウンドの項目は、役割と名称の衝突を避けるため`同期アニメーション`へ改名した。
  表示設定ボタンのフォーム振り分け、自由配置設定の復元、描画関数の選択は配置モードだけを参照し、
  描画へ渡す演出は同期演出だけを参照する。RendererテストはPASSし、FilterのWin64 Debug
  ビルドは警告0、エラー0で成功してDebug版をプラグインフォルダーへ配置した。
- 2026-07-30: 1行表示設定フォームへ文字自由配置フォームと同系統の書式ツールバーを追加した。
  本文全体またはルビ全体のクリック選択に連動して、フォント、太字、斜体、下線、取り消し線、
  文字サイズを編集し、行共通の同期前色・同期後色を専用カラーピッカーで編集できる。選択変更時は
  ツールバー状態とサイズ欄を切り替え、変更をプレビューへ即時反映する。OK時は歌詞、本文・ルビの
  フォントと4装飾、両サイズ、同期前後色を対応するFilter項目へ一括反映し、途中失敗時はそれまでの
  適用項目をロールバックする。RendererテストはPASSし、FilterのWin64 Debugビルドは警告0、
  エラー0で成功してDebug版をプラグインフォルダーへ配置した。
- 2026-07-30: 1行表示設定フォームのフォント選択をモーダルなフォントダイアログから
  システムフォント一覧のコンボボックスへ変更した。フォントコンボとサイズ欄を同じ行へ左詰めで
  配置し、本文全体／ルビ全体の選択変更時に両方の値を対象へ合わせて切り替える。ツールバーから
  フォントボタンを削除し、装飾と同期前後色だけを残した。RendererテストはPASSし、Filterの
  Win64 Debugビルドは警告0、エラー0で成功してDebug版をプラグインフォルダーへ配置した。
- 2026-07-30: 1行表示設定フォームの選択連動フォント欄を、常時表示する`歌詞フォント`と
  `ルビフォント`の2つのコンボボックスへ変更した。両コンボを同じ行へ左詰めで配置し、
  本文／ルビの選択状態に依存せず個別設定できる。サイズ数値欄は削除し、既存値を維持したまま
  将来のプレビュー上のマウス操作へ移す方針とした。RendererテストはPASSし、Filterの
  Win64 Debugビルドは警告0、エラー0で成功してDebug版をプラグインフォルダーへ配置した。
- 2026-07-30: 1行表示設定フォームへ、本文内ドラッグによる行全体移動、ルビ内の上下ドラッグによる
  本文位置を維持したルビ間隔補正、本文・ルビ各選択枠の四隅による縦横比固定サイズ変更、
  左右中央点による字間変更を追加した。選択状態は本文全体／ルビ全体の2種類のままとし、
  操作種別は押下位置から決定する。位置、両サイズ、本文字間、ルビ字間、ルビ間隔補正を
  プレビューとFilter項目の読込・一括保存へ接続し、実描画にも新設した`字間（ルビ）`を反映した。
  RendererテストはPASSし、FilterのWin64 Debugビルドは警告0、エラー0で成功してDebug版を
  プラグインフォルダーへ配置した。
- 2026-07-30: 1行表示設定フォームのドラッグ操作中に背景と文字が強くちらつく問題へ対し、
  文字自由配置フォームと同じフォーム単位のダブルバッファ描画を有効にした。
- 2026-07-30: 表示設定文字列を`SL2`へ更新し、行共通のX／Y、本文・ルビのフォント、4装飾、
  同期前後色、両サイズ、ルビ間隔補正、本文字間、ルビ字間と、文字自由配置の個別設定を
  1つの専用データへ統合した。歌詞変更時は行共通設定を維持し、不一致になった個別配置だけを
  無効化する。統合済みの19個のFilter設定項目と旧フォント設定ボタンを標準画面から削除した。
  MusicSync・RendererテストはPASSし、FilterのWin64 Debugビルドは警告0、エラー0で成功して
  Debug版をプラグインフォルダーへ配置した。
- 2026-07-30: Filter標準画面の`表示設定`直下へ、10枠のプリセット選択リストと
  `プリセット保存`、`プリセット読込`ボタンを追加した。保存先は
  `マイドキュメント\SYNC_Lyrics`固定とし、ファイル処理は次段階で接続する。
  MusicSync・RendererテストはPASSし、FilterのWin64 Debugビルドは警告0、エラー0で成功した。
- 2026-07-30: プリセットボタン名を`保存`、`読込`へ短縮し、選択値0～9に対応する
  `マイドキュメント\SYNC_Lyrics\0.slpreset`～`9.slpreset`へ共通表示書式と演出設定を
  無確認で上書き保存・読込する処理を接続した。読込時は現在オブジェクトの座標と文字自由配置を
  維持し、ファイル不存在・破損・入出力失敗は無表示で現在値を維持する。
  プリセット形式の往復、破損拒否、座標保持テストはPASSし、FilterのWin64 Debugビルドは
  警告0、エラー0で成功してDebug版をプラグインフォルダーへ配置した。
- 2026-07-31: Filter単体で曲全体の歌詞入力から同期・配置まで行う新しい編集画面の第1段階として、
  初回歌詞入力用`TFrameLyricsInitialInput`と親画面`TFormLyricsSyncEditor`を追加した。
  既存の歌詞項目が空の状態で`曲同期設定`を押すと新画面を開き、デバッグ用のルビ付き5行を
  `TMemo`へ初期表示する。`歌詞を確定`すると入力Frameを隠し、同じフォーム内の同期編集予定領域へ
  切り替える。既存歌詞がある場合の従来同期画面は変更していない。新形式データへの保存はまだ行わない。
  初回実機表示で、親を持たない動的FrameのDFM読込中に`LyricsMemo.Lines.Strings`がMemoの
  ウィンドウハンドルを要求して表示エラーになる問題を確認した。デバッグ歌詞をDFMから外し、
  Frameを親パネルへ接続した後にコードから設定する方式へ変更した。フォームの`OnShow`中に
  子Frameへ直接フォーカスを設定する処理も、生成時の`ActiveControl`指定へ変更した。
  単独VCLスモークテストでフォーム生成・ハンドル作成・表示・非表示が成功し、Filterの
  Win64 Debugビルドも警告0、エラー0で成功してDebug版をプラグインフォルダーへ配置した。
- 2026-07-31: 曲全体編集の作業モデル`TLyricsSongModel`と行レコード`TLyricsSongLine`を追加した。
  初回歌詞を空行を除いて行単位へ分割し、各行へ安定した行ID、元構文、構文解析後本文、ルビ区間、
  初期値1の表示段、未設定の同期状態を保持する。歌詞確定後は同期編集親フォームの左側へ
  オーナードロー一覧を表示し、解析後本文と表示段を確認できる。数字キーまたはテンキーの1～3で
  選択行の表示段を変更し、次の行へ自動移動する操作も接続した。行モデルの構文解析・初期値・
  表示段範囲テストはPASSし、フォームスモークテストでは5行生成、先頭選択、表示段変更後の
  次行移動まで`FORM_OK`を確認した。FilterのWin64 Debugビルドは警告0、エラー0で成功し、
  Debug版をプラグインフォルダーへ配置した。
- 2026-07-31: 既存の楽譜同期フォームを曲全体編集画面内で再利用する
  `TFrameLyricsMusicSyncEditor`を追加し、歌詞確定後の右側領域へピアノロール、歌詞欄、
  同期初期化操作を組み込んだ。同期ボタンを押した時点の音楽ファイル、トラック、事前表示時間、
  Filter発火位置を新画面へ渡す。左一覧の選択変更と数字キーによる次行移動に合わせ、対象行の
  歌詞構文と行別同期データを同期UIへ読み込み、編集した事前表示時間・同期文字列を行モデルへ退避する。
  行モデルテストはPASSし、フォームスモークテストでは同期Frame生成、先頭行読込、次行への
  読替えまで`FORM_OK`を確認した。FilterのWin64 Debugビルドは警告0、エラー0で成功し、
  Debug版をプラグインフォルダーへ配置した。
- 2026-07-31: 曲全体編集画面へ選択行の同期状態ラベルと`同期を確定`ボタンを追加した。
  行一覧は未設定`[ ]`、仮設定`[~]`、確定`[C]`、不整合`[!]`を表示する。同期UIの変更は
  行切替時に仮設定として退避し、ボタン操作で確定または確定解除できる。確定済み行の同期内容を
  再編集した場合は不整合へ遷移させ、明示的な再確定を必要とするため、前方行を編集しても後続の
  確定状態を暗黙に変更しない基礎を整えた。モデルの状態遷移テストはPASSし、フォームスモーク
  テストでは確定、行切替後の状態保持、解除まで`FORM_OK`を確認した。FilterのWin64 Debug
  ビルドは警告0、エラー0で成功し、Debug版をプラグインフォルダーへ配置した。
- 2026-07-31: 曲全体編集の左一覧へ`追加`・`削除`ボタンと`F2`構文編集を追加した。
  行モデルへ挿入・削除・構文更新・曲全体テキスト再構築を実装し、操作対象外の行ID、表示段、
  同期データを維持する。同期済み行の歌詞構文を変更した場合は同期状態を不整合へ移す。
  曲全体モードで右側に埋め込んだ同期GUIの歌詞欄は読み取り専用とし、構文編集経路を`F2`へ統一した。
  行モデルの挿入・削除・ID保持・再解析・不整合遷移テストはPASSし、フォームスモークテストも
  `FORM_OK`を確認した。FilterのWin64 Debugビルドは警告0、エラー0で成功したが、AviUtl2が
  使用中のため生成DLLから`.auf2`への上書き配備だけは保留している。
- 2026-07-31: 曲全体データの保存先を外部ファイルと誤認して一度実装したが、Filterのtext項目へ
  格納してAviUtl2プロジェクトに保存する仕様へ修正した。外部ファイル項目、保存ダイアログ、
  ファイル入出力を撤回し、`曲全体データ`を32,767文字以下の単一行`SLS1`文字列項目へ変更した。
  行ID、歌詞構文、表示段、同期状態、事前表示時間、表示・同期の開始終了フレーム、同期文字列、
  配置文字列を含む。`適用して閉じる`でFilter項目へ反映し、`キャンセル`では反映しない。
  文字列往復、破損拒否、上限超過拒否テストはPASSし、フォームスモークテストでは既存文字列の
  読込と編集後の再生成まで`FORM_OK`を確認した。FilterのWin64 Debugビルドは警告0、エラー0で
  成功した。
- 2026-07-31: Filter描画側へ曲全体データを接続した。単一行`SLS1`の解析結果は最大16件の
  読み取り専用LRUキャッシュへ保持し、毎フレームの再解析を避ける。オブジェクト内の現在フレームから
  表示範囲に該当する行を解決し、行別の歌詞、表示段、同期文字列、同期開始、表示開始・終了を
  既存の同期計算・アニメーション・レンダラーへ渡す。範囲未設定データは先頭行へフォールバックする。
  MusicSyncアンカーへ最後に処理したオブジェクト内フレームを追加し、編集画面の`開始設定`・`終了設定`で
  選択行の表示・同期範囲を記録できるようにした。開始設定では事前表示時間から同期開始フレームも算出し、
  ピアノロールの基準を行の表示開始へ切り替える。行範囲・キャッシュ・対象行解決テスト、
  MusicSyncテストはPASSし、フォームスモークテストは`FORM_OK`を確認した。FilterのWin64 Debug
  ビルドは警告0、エラー0で成功し、Debug版をプラグインフォルダーへ配置した。
- 2026-07-31: 曲全体同期を1本の連続した音符列として扱う開始音符番号を行データへ追加した。
  初期状態では各表示単位が1音を消費し、次行は前行の最終割当音の次から始まる。1音複数字・
  複数音1字のグループ編集に応じて未確定・仮設定の後続行を累積再計算し、確定・不整合の行は
  開始音符番号を固定する。同期画面と実描画は曲全体の共通基準から開始音符数を飛ばす方式へ統一し、
  左一覧には1始まりの開始音符番号`N`を表示する。開始音符番号を`SLS1`へ保存・復元するよう更新した。
  行モデル、保存形式、MusicSync、フォームスモークテストはPASSし、FilterのWin64 Debugビルドは
  警告0、エラー0で成功した。
- 2026-07-31: 曲全体同期画面で行を選択した際、選択行の割当先頭音を同期開始線へ合わせるよう
  ピアノロールの表示原点を更新した。同期開始を行の基準とし、黄色の表示開始線はそこから行別の
  事前表示秒を引いた位置、桃色の同期開始線は割当先頭音の位置へ描画する。曲全体の音符列起点には
  Filter共通の事前表示時間を使用し、行別の事前表示変更で割当音符番号が変化しないよう分離した。
  MusicSyncテストはPASS、フォームスモークテストは`FORM_OK`、FilterのWin64 Debugビルドは
  警告0、エラー0で成功した。
- 2026-07-31: 曲全体同期画面で、10秒のMIDIが表示されているにもかかわらず
  `同期開始 31.33秒以降に対象ノートがありません`と表示される問題を修正した。背景Inputから
  取得した絶対フレームを外部MIDIの0秒基準へ加算していたことが原因だった。曲全体編集の
  ピアノロールと実描画は歌詞テロップオブジェクト内の相対時刻を使用し、背景の配置時刻から分離した。
  MusicSyncテストはPASS、フォームスモークテストは`FORM_OK`、FilterのWin64 Debugビルドは
  警告0、エラー0で成功した。
- 2026-07-31: 曲全体同期画面の固定歌詞レーンへ、選択行の直前・直後1行を参考表示する機能を
  追加した。上段の`前:`と下段の`次:`は解析後本文を小さな低コントラスト色で描画し、中央の
  編集中歌詞だけが従来どおり同期選択・ドラッグ対象となる。先頭・最終行では存在する側だけを表示する。
  MusicSyncテストはPASS、フォームスモークテストは`FORM_OK`、FilterのWin64 Debugビルドは
  警告0、エラー0で成功した。
- 2026-07-31: 前後行の参考表示場所が意図と異なっていたため、下部固定歌詞レーンへの表示を撤回して
  従来の高さと選択行だけの表示へ戻した。前後行は各行の開始音符番号と同期グループを参照し、
  ピアノロール上の対応ノート直下へ低コントラストで描画する。前後行はヒット領域を作らず、
  選択・ドラッグ対象にはしない。MusicSyncテストはPASS、フォームスモークテストは`FORM_OK`、
  FilterのWin64 Debugビルドは警告0、エラー0で成功した。
- 2026-07-31: 行同期の変更反映を行切替時から操作直後へ変更した。同期グループのドラッグ、
  同期初期化、事前表示線の変更ごとに現在行へ同期文字列を退避し、後続の同期未編集行は変更された
  終了音の直後へ開始音符番号を追従させる。仮設定を含めて一度でも同期編集した後続行は位置を固定する。
  左一覧の開始音符番号とピアノロール上の前後行参考表示も同じ操作内で再描画する。
  行モデル、MusicSyncテストはPASS、フォームスモークテストは`FORM_OK`、FilterのWin64 Debug
  ビルドは警告0、エラー0で成功した。
- 2026-07-31: 曲全体の連続同期操作を完成扱いとし、保留していた複数表示段の同時描画へ進んだ。
  オブジェクト内の現在フレームについて表示段1～3ごとに有効な行を解決し、範囲が重なる最大3行を
  それぞれの歌詞、同期進捗、開始・終了アニメーション、段別Y位置で既存レンダラーへ渡す。
  同じ表示段で範囲が重なる場合は表示開始位置が新しい行を優先し、曲全体データがない従来1行表示は
  旧経路へフォールバックする。行範囲・重複段解決テストとRendererテストはPASSし、Filterの
  Win64 Debugビルドは警告0、エラー0で成功した。
- 2026-07-31: 1行目しか表示されない原因だった表示範囲未設定フォールバックを置き換えるため、
  各行へ初期値0.5秒の`維持時間`を追加した。同期開始は先頭割当音の開始、同期終了は最終消費音の終了、
  表示開始は同期開始から事前表示時間を引いた位置、表示終了は同期終了へ維持時間を加えた位置として、
  全行のオブジェクト内フレーム範囲を音楽データから自動生成する。ピアノロールへ橙色の表示終了線を
  追加し、Filter描画は保存された表示終了を越えた行を対象外とする。維持時間の編集UIはまだ持たず、
  表示重複は表示段1～3の変更で回避する。行モデル・保存形式・MusicSync・範囲解決テストはPASS、
  フォームスモークテストは`FORM_OK`、FilterのWin64 Debugビルドは警告0、エラー0で成功した。
- 2026-07-31: 1行1オブジェクト方式を正式に廃案とし、1曲分の歌詞を1つのFilterオブジェクト内の
  行配列として入力、同期、配置、描画する方針へ`DESIGN.md`を更新した。実描画では保存済みの
  `同期開始フレーム`より前を未消費、`同期終了フレーム`以後を全消費として固定し、範囲内だけ
  MIDI/WAVまたは手動同期から進捗を解決する。これにより同期終了後の維持区間で必ず同期済み表示になる。
  同じ表示段で範囲が重なる場合に新しい行を選ぶテストと、同期開始・終了境界の進捗テストを追加した。
  SongLyricsData、SongLyricsModel、MusicSyncテストはPASSし、FilterのWin64 Debugビルドは
  警告0、エラー0で成功し、Debug版をプラグインフォルダーへ配置した。
