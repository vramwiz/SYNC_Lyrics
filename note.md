# SYNC_Lyrics 作業ノート

作業再開時に最初に確認する、現在の開発状況だけを扱う短いメモ。
確定済みの詳細仕様、開発規約、過去の経緯は必要なときだけ各文書を参照する。

## 文書の役割

| 文書 | 内容 | 通常の作業開始時 |
| --- | --- | --- |
| `note.md` | 現在の開発段階、直近の方針、次の作業 | 読む |
| `DESIGN.md` | 確定済みの製品仕様・設計 | 関係する節だけ読む |
| `DEVELOPMENT.md` | 安全性、ビルド、配布、コメント、Git運用 | 関係する節だけ読む |
| `HISTORY.md` | 完了済みの実装経緯と日付付き検証履歴 | 原則として読まない |
| `README.md` | 利用者向け概要 | 公開・利用者向け作業時に読む |

- 製品名とDelphiプロジェクト名は `SYNC_Lyrics` とする。
- 作業フォルダーは `D:\DelphiProg\test\SYNC_Lyrics` とする。
- 試行錯誤を逐次追記せず、現在の方針が変わったときだけこの文書を更新する。
- 完了した作業は要点を `HISTORY.md` へ移し、この文書へ蓄積しない。

## 現在の開発段階

文字自由配置を基礎として、フォント、同期前色、同期後色、文字サイズ等の表示スタイルを
配置要素ごとに正しく設定、保存、復元、プレビュー、実描画できる状態へ進める。

- 1文字ごとの自由配置GUIは完成しており、ルビサイズを含む操作も実機確認で正常動作している。
- 配置要素は通常文字1文字、またはルビ付き本文とルビをまとめた1グループとする。
- 未指定の個別設定はFilterの行共通設定を継承する。
- 複数選択へ同じ値を一括適用できるようにする。
- 個別設定は改行を含まないバージョン付き文字列へシリアライズし、TEdit相当の
  `string`項目`表示設定`へ保存する。β版の旧Data形式との互換性は持たせない。
  Windows単行Editの既定入力上限に合わせ、保存文字列は32,767文字以下に制限する。
- 色の編集には`Source\Lib\ColorPicker`へ統合した専用カラーピッカーを使用する。
- 文字別フォント、同期前色、同期後色、本文サイズ、ルビサイズ、本文・ルビの
  太字・斜体・下線・取り消し線、および本文字間・ルビ字間は実装済み。
- 選択済み要素の再クリックで、黄色の状態1（移動・変形）、水色の状態2（本文字間）、
  紫の状態3（ルビ）を循環する。ルビのない要素を含む場合は状態1と状態2だけを循環する。
- 状態2の左右点は、選択要素内の本文（漢字側）の字間を調整する。文字サイズ、縦横倍率、
  要素座標は変更せず、正の字間と負の字間の両方を許可する。ドラッグ中は対象本文の端が
  操作した枠線の内側へ追従する。
- 状態3の左右点はルビ字間、上中央点は本文に対するルビの相対X・Y位置を調整する。
- 固定長`data`項目は廃止済みであり、1024バイト上限は表示設定へ適用されない。
- 現在の編集画面は文字単位自由配置専用の
  `SYNC_Lyrics_CharacterLayoutSettingsForm`とする。1行単位表示用画面は別責務の
  `SYNC_Lyrics_LineDisplaySettingsForm`とする。現在は歌詞入力、オブジェクト名変更、
  背景上での本文・ルビ簡易表示、本文全体・ルビ全体のクリック選択、歌詞・ルビそれぞれの
  フォントコンボ、選択連動の太字・斜体・下線・取り消し線、行共通の同期前後色編集まで
  実装している。サイズ数値欄は設けず、今後プレビュー上のマウス操作へ割り当てる。
  OK時は変更したFilter項目を一括反映し、途中で失敗した場合は適用済み項目を元へ戻す。
- 文字装飾と配置初期化系は上部のWord風アイコンツールバーから操作できる。
  移行確認を完了し、右側の暫定数値設定欄と重複ボタンは削除済みである。
- 配置要素の最終的な矩形サイズはマウスによる縦横変形で決める。本文・ルビの基準文字サイズと
  縦横倍率を100%へ正規化せず、現在の内部モデルを維持する。右側は画面外要素の復旧にも使う
  配置要素一覧だけを表示する。

## 次の作業

共通表示単位へ、GDIで測定した本文・ルビの原点と矩形、単位基準点、グループ矩形、
全体の行矩形まで追加した。1行配置・文字自由配置とも同じ実行時モデルを生成し、
通常カラオケの同期進捗とルビ描画は共通単位の同期番号・矩形・文字列を使用している。
文字単位強調と1文字ずつ出現も、基本配置を書き換えない共通の一時演出状態として
両配置モードへ適用した。Filterの旧`タイプ`は`配置モード`と`同期演出`へ分離済みで、
1行配置・文字自由配置のどちらでも3種類の同期演出を選択できる。従来のバウンド項目は
`同期アニメーション`へ改名した。1行表示GUIの基本書式操作もFilter項目へ接続済みである。
1行表示GUIでは本文内の行全体移動、ルビ内の間隔補正、本文・ルビ各枠の四隅による
縦横比固定サイズ変更、左右点による字間変更を実装し、位置、両サイズ、両字間、
ルビ間隔補正をプレビューとFilter項目へ接続した。次は実機で各ドラッグ方向、
操作点の掴みやすさ、保存後の実描画一致を確認し、必要なら操作感を調整する。
ドラッグ時の再描画は文字自由配置GUIと同じフォームのダブルバッファを使用する。
