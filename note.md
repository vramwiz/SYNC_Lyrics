# SYNC_Lyrics 作業ノート

作業再開時に最初に確認する開発メモ。現在の仕様、未決事項、共通ルール、ビルド方法をここへ置く。

- 利用者向けの概要、導入、配置、操作説明は、公開準備時に `README.md` へ分ける。
- 完了済みの実装経緯、検証結果、日付付き履歴が増えた場合は `HISTORY.md` へ分ける。
- 実装途中の細かな試行錯誤を逐次追記せず、方針または検証結果が確定した時点で更新する。
- 製品名とDelphiプロジェクト名は `SYNC_Lyrics` とする。
- 作業フォルダーは `D:\DelphiProg\test\SYNC_Lyrics` とする。

## 兄妹プラグイン

Syncroh2から音楽関係の機能を分離し、次の3製品とする。共通接頭辞の `SYNC_` はシリーズ名であると同時に、
音楽との同期も表す。

| 製品名 | 日本語名称 | 役割 |
| --- | --- | --- |
| `SYNC_Motion` | 音楽同期アニメーション | 音楽に合わせて画像等を縦・横に揺らす |
| `SYNC_Lyrics` | 歌詞テロップ | カラオケやMV用の歌詞を描画する |
| `SYNC_PianoRoll` | ピアノロール | 音楽に同期したピアノロールを描画する |

## 実装参照元

入力、フィルター、歌詞データ管理、時間同期、描画処理を検討するときは、次を参照する。

- `D:\DelphiProg\test\SYNC_PianoRoll`
- `D:\DelphiProg\test\Syncroh2\Syncroh2_Filter_LyricTelop.dpr`
- `D:\DelphiProg\test\Syncroh2\Syncroh2_Extension2.dpr`
- `D:\DelphiProg\test\Syncroh2\Plugin_Filter\LyricTelop`
- `D:\DelphiProg\test\Syncroh2\Plugin_Extension\Music\LyricTelop`

`SYNC_PianoRoll`からは現在のAviUtl2 Input／Filter境界、仮想入力、共有メモリ、並列呼び出し対策を
参照する。Syncroh2からは歌詞テロップ固有のデータ、同期、描画、拡張機能の責務を確認する。
旧構成をそのままコピーせず、SYNC_Lyricsに必要な依存だけを採用する。

## AviUtl2上の識別名

表示名は内部の識別やAPIの引数にも使われるため、一般的な日本語名称だけにはしない。
空白や装飾記号を避け、次の形式を固定識別名として使う。

```text
SYNC_<日本語機能名>_<Input|Filter>
```

SYNC_Lyricsでは次を使用する。

| 種類 | 識別名 |
| --- | --- |
| 入力プラグイン | `SYNC_歌詞テロップ_Input` |
| フィルタープラグイン | `SYNC_歌詞テロップ_Filter` |
| フィルターグループ名 | `SYNC` |

- 識別名はエイリアス、設定取得・書き戻し、既存プロジェクトの互換性へ影響するため、公開後は変更しない。
- Delphiプロジェクト名と配布ファイル名にはASCIIの `SYNC_Lyrics_Input` と
  `SYNC_Lyrics_Filter` を使う。
- GUI項目名もAPIの引数に使う可能性があるため、確定後の変更は互換性を確認してから行う。

## 目的

従来の「曲全体の歌詞を1つのオブジェクトへ集中して処理する方式」をやめ、歌詞を原則1行単位の
編集素材として扱えるようにする。行ごとの編集作業は増えるが、位置、色、サイズ、アニメーション、
表示方法を各行へ個別に設定でき、凝った表示を作りやすくする。

同期計算と描画方法を分離し、同じ消費結果を複数の表示タイプで利用できる構造を目指す。

## 打ち合わせで確定した基本方針

- 完成品はWAV、専用Input、専用Filterの3オブジェクトを3レイヤーで使用する。
- 専用Inputと専用Filter側の描画オブジェクトは、必ずグループ化して扱う。
- グループを歌詞の行ごとに分割し、原則として1つのFilterオブジェクトが1行だけを担当する。
- WAVは実際の音を鳴らし、Inputは本来のフレーム位置を渡し、Filterは歌詞を描画する。
- 歌詞の進行は、フレーム位置から得られる音の消費数で決める。
- 同期パラメーター番号は音番号でも文字番号でもなく、同期段階の処理順だけを表す。
- 同期パラメーター値にかかわらず、1段階が終われば必ず番号が1つ大きい次のパラメーターへ進む。
- オブジェクト先頭側の延長区間を同期対象から除外するため、`事前表示 (秒)`をFilter設定として持つ。
- コピーで同じ値が複製される同期識別IDは使用しない。
- 描画方法は `タイプ` で選択できるようにする。
- 標準GUIにはフォント選択機能がないため、専用GUIは残す。

## 完成品のタイムライン構成

完成品は主に3オブジェクト、3レイヤーを使う。

| オブジェクト | 役割 |
| --- | --- |
| WAV音声オブジェクト | 実際に鳴る音声データ |
| `SYNC_Lyrics_Input` の素材オブジェクト | 要求された本来の絶対フレーム位置をFilterへ渡す |
| `SYNC_Lyrics_Filter` を適用した描画オブジェクト | 受け取ったフレーム位置に対応する歌詞を描画する |

InputとFilter側の描画オブジェクトをグループ化して配置、移動、分割する。歌詞の1行ごとの区間で
グループを分割し、分割後の各グループに1行を担当させる。WAV音声オブジェクトは実音を担当し、
このグループには含めない。

Inputはフレーム位置を渡すだけの単純なプラグインとし、利用者向けには必要最小限の説明に留める。

### 利用者の基本編集手順

1. WAV音声オブジェクトを配置する。
2. 専用Inputの素材オブジェクトと、専用Filter側の描画オブジェクトを配置する。
3. InputとFilter側の描画オブジェクトをグループ化する。
4. グループを歌詞の1行に対応する区間ごとに分割する。
5. 分割後の各Filterへ、その区間が担当する1行の歌詞、ルビ、同期値、表示タイプ、装飾を設定する。
6. 発音前から見せる量はグループの先頭側の長さとFilterの`事前表示 (秒)`を同じ値にして調整する。
   発音後に残す量はグループの末尾位置を直接編集して調整する。
7. 行ごとに位置、色、サイズ、開始・同期・終了演出を調整する。

分割後もInputとFilter側の描画オブジェクトを同じグループとして移動・長さ変更することが、
正しい同期を維持するための前提となる。

## InputとFilterの同期

Inputは描画内容を判断せず、自分へ要求された本来の絶対フレーム位置を共有メモリへ公開する。
Filterはその位置から、同期すべき音と歌詞の進行状態を求めて描画する。

Inputプラグインの映像キャッシュにより、同じ素材の読込処理が2回目以降に発火せず、共有フレームが
更新されない場合がある。そのためFilterは、最初に受信した共有絶対フレームと、その時点の
Filter側ローカルフレームの対応を記憶する。

- 共有情報が更新された場合は、新しい絶対フレームとローカルフレームで基準を更新する。
- 更新されない間は、記憶した対応関係と現在のローカルフレームとの差から絶対フレームを補間する。
- InputとFilterをグループ化したまま分割することで、分割後もInputから本来のフレーム位置を得る。
- 複数オブジェクトや複数フィルターの対応関係が混在しないよう、コンテキストを分離する。
- AviUtl2からの並列呼び出しを前提に、共有状態の読み書きを排他する。

コピーすると同じ値が複製され、オブジェクト同士を直接同期させる構造でもないため、利用者が設定する
同期識別IDは設けない。

## 1行の消費モデル

フレーム位置から、その時点までに同期すべき音がいくつ進んだかを取得する。この進行を「音の消費」と呼ぶ。
1行に含まれる表示対象を順番に消費し、担当する歌詞をすべて消費した時点でその行を完了状態とする。

音と表示対象が常に1対1になるとは限らないため、順番に処理する数値パラメーター列を持つ。
各パラメーターの初期値は `0` とし、値の意味は次のとおりとする。

| 値 | その同期段階で消費する内容 |
| ---: | --- |
| `0` | 1音で1表示単位 |
| 正の `n` | `n + 1`音で1表示単位 |
| 負の `n` | 1音で `abs(n) + 1` 表示単位 |

例:

| 値 | 意味 |
| ---: | --- |
| `2` | 3音で1表示単位 |
| `1` | 2音で1表示単位 |
| `0` | 1音で1表示単位 |
| `-1` | 1音で2表示単位 |
| `-2` | 1音で3表示単位 |

### パラメーター番号の重要な規則

`同期1`、`同期2`、…の番号は音番号でも文字番号でもなく、単なる処理順である。値によって消費する
音数や表示単位数が変わっても、1段階が完了した後は必ず次のパラメーターへ進む。

```text
同期1 = -1  → 1音で2表示単位を消費 → 次は同期2
同期2 =  1  → 2音で1表示単位を消費 → 次は同期3
```

処理中は概念上、少なくとも次の3つを独立して進める。

| 状態 | 意味 |
| --- | --- |
| 同期パラメーター位置 | 現在処理している `同期n` の `n` |
| 音消費位置 | 行の開始後、いくつの音を消費したか |
| 表示消費位置 | 歌詞の先頭から、いくつの表示単位を消費したか |

例えば `同期1 = -1` の場合、音消費位置は1、表示消費位置は2進み、次の処理位置は必ず
`同期2` になる。`同期1 = 1` の場合は、音を2つ消費し終えた時点で表示消費位置が1進み、
次の処理位置は `同期2` になる。値を音位置または文字位置への添字として使用してはならない。

歌詞の文字数と同期パラメーター数は一致させない。特に英語では、複数文字または複数単語を1音で
まとめて消費する場合があり、文字数より少ない同期段階で表現できる。1行の日本語歌詞は通常15～25文字、
長くても30文字前後を想定し、内部文字列には余裕を持たせる。

英語では最大級のまとまりとして「4単語を1音で消費」のようなケースも想定する。このため、
同期パラメーターの必要数を歌詞の文字数から決めず、実際の発音上のまとまり数を基準にする。

### 行内の処理状態

| 状態 | 条件 | 基本描画 |
| --- | --- | --- |
| 消費前 | 行の最初の同期音より前 | 全体を変化前状態で表示 |
| 消費中 | 1つ以上の音を消費し、歌詞が残っている | 消費済みと未消費の状態をタイプに従って描き分ける |
| 消費完了 | 歌詞の全文字／全表示単位を消費済み | 全体を変化後状態で表示 |

行の終了判定は「同期パラメーターをすべて読んだか」ではなく、担当する歌詞の表示単位をすべて
消費したかを基本とする。オブジェクト区間が実際の発音より長い場合も、消費完了状態を維持できるようにする。

### 音楽ファイル同期の第1検証

同期データの読込には`SYNC_PianoRoll\Source\Lib\SongReader`と同じソース一式を使用する。
SongReaderが各形式を共通のノート開始・終了秒へ変換し、その開始時刻を音の消費位置として使用する。

| 分類 | 対応拡張子 |
| --- | --- |
| MIDI | `.mid`、`.midi` |
| UTAU | `.ust` |
| VOCALOID | `.vsq`、`.vsqx` |
| MusicXML | `.musicxml`、`.mxl`、`.xml` |
| MuseScore | `.mscx`、`.mscz` |

Filterへ次の設定を追加する。

| 設定 | 意味 |
| --- | --- |
| 音楽ファイル | 同期元のMIDIファイル |
| トラック (-1=全て) | 0始まりのMIDIトラック番号。`-1`は全トラック |

Filterオブジェクト先頭に対応する入力素材の絶対秒を同期開始位置とし、そこから現在の絶対秒までに
開始した対象トラックのノート数を数える。第1検証では同期値をすべて`0`とみなし、1音につき本文の
先頭から1表示単位を変化前色の白から変化後色の水色へ切り替える。

通常文字は1文字を1表示単位とするが、`[漢字](かんじ)`のようなルビ指定は対応本文全体とルビ全体を
合わせて1表示単位とする。本文とルビは同じノート開始・終了時刻から同じ進捗率を求める。ただし横幅は
個別に測り、本文側は本文幅、ルビ側はルビ幅へ進捗率を掛けて、それぞれ左から右へ水色のクリップ領域を
広げる。同じピクセル幅や画面上の同じ境界座標は共有しない。

```text
進捗率 = (現在秒 - ノート開始秒) / (ノート終了秒 - ノート開始秒)
本文境界 = 対応本文の横幅 × 進捗率
ルビ境界 = 対応ルビの横幅 × 進捗率
```

ノート開始時は全体が白、発音中はフレームごとにクリップ境界が進み、ノート終了時は表示単位全体が
水色になる。終了時刻が開始時刻以下など有効な長さを持たないノートは即時完了として扱う。

第1検証時点では同期値をすべて`0`としていた。現在は`同期1`～`同期16`を適用し、16段階を
超えた部分は暗黙の`0`として処理を継続する。表示単位の明示区切り、開始・同期・終了演出はまだ適用しない。

### 同期調整で検証すること

- GUIへ`同期1`～`同期16`を実装した。16段階を超える部分は1音1表示単位で進むため通常行は完了できるが、
  17段階目以降にも個別調整が必要な実データがあるか検証する。
- 「表示単位」をUnicode文字、書記素、歌詞データ内の明示区切り、単語のどれとして扱うか。
- 英語の「4単語を1音」のような指定は負数で複数表示単位をまとめられるが、文字数の指定より
  明示区切りの方が編集しやすいか検証する。
- 終端値は設けず、歌詞の表示単位をすべて消費した時点で完了する。未指定段階は`0`として扱う。
- ルビ対応本文とルビ全体は既に1表示単位として扱う。

## オブジェクト区間と表示時間

発音前から表示する場合はグループオブジェクトの開始を前へ延ばし、発音後も表示する場合は終了を
後ろへ延ばす。ただし、延長した全区間を通常どおり同期させると、先頭側では前の歌詞、末尾側では
次の歌詞の音へ同期してしまう。このため、オブジェクトの長さに加えてFilter設定として
`事前表示 (秒)`を持つ。表示終了はオブジェクトの末尾位置だけで決める。

- オブジェクト先頭から`事前表示 (秒)`まで: 同期消費を進めず、担当行の未消費状態を表示する。
- 事前表示の終了から: 歌詞の表示単位数と同じ数の音だけを、この行の同期対象として消費する。
- 最後に割り当てた音の終了後: 担当行の完了状態をオブジェクト末尾まで表示する。
- `事前表示 (秒)`の初期値は`0.50`秒、変更単位は`0.01`秒とする。
- Inputの絶対フレーム受信とキャッシュ時の相対補間は全区間で継続するが、事前表示区間では
  その位置を歌詞消費へ反映しない。
- 割り当て可能な音が表示単位数より少ない場合は、入力不整合を隠さず未完了状態を
  オブジェクト末尾まで表示する。

開始演出と終了演出の時間幅は別に必要であり、オブジェクトの先頭または末尾からの相対時間として扱う。

## Filterの設定要素

旧方式の設定をそのまま移植するのではなく、1行単位方式で必要性を判定する。

### 現時点で必要

| 要素 | 用途 |
| --- | --- |
| 歌詞 | このオブジェクトが担当する1行 |
| ルビ | 1行分のルビ情報。対応を残す場合 |
| 音楽・同期データファイル | フレーム位置から音の進行を取得する元データ |
| X／Y | 画面中央を`0, 0`とする行単位の位置調整。行全体の配置にはグループ制御を使用する |
| フォント名 | 本文に使用するフォント |
| フォント設定ボタン | 専用GUIを開き、本文とルビのフォント設定を編集する |
| フォント名（ルビ） | ルビに使用するフォント |
| 変化前色 | 未消費部分の色 |
| 変化後色 | 消費済み部分の色 |
| 歌詞サイズ | 本文の大きさ |
| ルビサイズ | ルビの大きさ |
| ルビ間隔補正 | フォントによって変わる本文とルビの縦方向の間隔を調整する。初期値は`0` |
| 字間 | 本文の文字間隔を調整する。初期値は`0` |
| タイプ | 通常テロップ等の表示方法を選ぶ |
| 事前表示 (秒) | オブジェクト先頭で同期を進めず、未消費状態を表示する時間 |
| 同期パラメーター列 | 音と表示単位の消費関係を処理順に指定する |
| 同期演出 | 消費の進行に伴う文字演出 |
| 開始演出 | 行の表示開始時の演出 |
| 終了演出 | 行の表示終了時の演出 |

同じ行を構成するInput、Filter等のオブジェクトは同じレイヤーへ配置し、グループ制御によってまとめて
位置を決める。Filterの`X`、`Y`はグループ制御を置き換えるものではなく、行ごとの最小限の微調整に
使用する。本文とルビには同じ座標オフセットを適用する。

追加を検討する表示設定として、太字、斜体等のフォントスタイルと、ルビだけを詰めたり広げたりする
`字間（ルビ）`がある。フォントスタイルを扱う場合は、フォント名だけでなくスタイル値をFilterへ
保存できる形式が必要になるため、専用GUIの保存方式を決める段階で確定する。

縁取り、影等の文字装飾はSYNC_Lyricsへ重複実装せず、AviUtl2の既存Filterを後段へ適用して構成する。
これにより歌詞同期と文字装飾の責務を分離し、AviUtl2側で装飾の種類、強さ、重ね順を編集できるようにする。

### 削除または不要候補

| 要素 | 理由 |
| --- | --- |
| 行高 | 原則1オブジェクト1行なので不要 |
| 同期識別ID／更新日時ID | コピーで複製され、今回の同期方式では識別に使わない |

### 旧方式からの移行整理

| 旧方式の要素 | 新方式での扱い |
| --- | --- |
| 歌詞テロップファイル | 曲全体一括ではなく、1行分の歌詞と同期元データの指定方法へ再設計 |
| フォント選択 | 専用GUIに残す |
| フォント名 | 残す |
| 変化前色／変化後色 | 残す |
| 歌詞サイズ／ルビサイズ | 残す |
| 行高 | 原則削除 |
| 表示先行／表示維持 | 表示先行だけを`事前表示 (秒)`として残し、表示維持はオブジェクト末尾で調整する |
| 同期演出 | 残す |
| 開始演出／終了演出 | 残す |
| 更新日時等の識別値 | 同期識別には使わない |

### 歌詞入力の第1検証

専用構文と同期消費へ進む前に、標準GUIへ1行文字列の`歌詞`項目を1つ登録し、入力したUnicode文字列を
描画できることを確認する。この段階ではルビ構文を解釈せず、入力値全体を1行の白文字として透明背景の
中央へ描画する。空文字は透明画像にする。

この設定は入力経路と描画経路を分離して検証するための最小実装であり、文字列の保存形式、専用構文、
ルビ対応、同期による色分け、フォントや装飾の設定は後続段階で追加する。

### ルビ構文

既存の歌詞テロップと互換性のある`[本文](ルビ)`を使用する。本文が1文字の場合は角括弧を省略して
`字(ルビ)`と書ける。

```text
[漢字](かんじ)を読(よ)む
```

表示本文は`漢字を読む`となり、`かんじ`は`漢字`、`よ`は`読`の幅の中央上へ配置する。
1行内へ複数のルビを指定できる。閉じ括弧が不足するなど完成していない構文は入力中の文字を失わないよう、
構文記号を含む通常文字として表示する。専用GUIによる構文入力補助は後続段階で追加する。

### 専用GUI

タイプだけなら標準パラメーターで選択できるが、標準機能にフォント選択がないため専用GUIを用意する。
フォント設定GUIと同期設定GUIは役割を分ける。同期設定GUIは、曲同期では音と表示単位の対応関係を
編集し、WAV手動同期では波形表示と音声再生を使って同期境界を登録する。

### 同期設定の次期方針

`同期1`～`同期16`は消費モデルを検証するための数値パラメーター列として実装した後、標準GUIから削除し、
曲同期と手動同期の設定値を同じ1つの`同期データ`テキスト項目へ保存する方式へ移行した。曲同期の初版形式は
次のとおりとし、段階値が空の場合はすべて暗黙の`0`として扱う。

```text
SYNC_LYRICS_SYNC/1;mode=music;stages=1,-1,0
```

ヘッダは`SYNC_LYRICS_SYNC/<形式バージョン>`、モードは`mode=music`または将来の`mode=manual`とする。
現行バージョンで未知の項目、重複項目、範囲外の段階値を含む文字列は処理せず、未消費表示へ戻す。

- テキスト先頭へ形式とモードを表すヘッダを付け、曲同期と手動同期を処理側で区別する。
- 設定ボタンから専用GUIを開き、既存の値が対応するヘッダならGUIの初期状態へ復元する。
- GUIで確定した値は既存文字列への追加ではなく、ヘッダを含む新しい文字列で全体を置き換える。
- 保存先は曲同期と手動同期で共通とし、モードごとに個別の数値パラメーター群は持たない。
- ヘッダには将来の形式変更を判別できるバージョンを含める。

曲同期では、同期元の音楽データから得た音と歌詞の表示単位の対応関係を専用GUIで編集する。現在の
`同期1`～`同期16`で検証した、複数音で1表示単位または1音で複数表示単位を消費する考え方は、
GUIが生成する共通テキストへ移して再利用する。

手動同期ではWAVファイルを指定し、専用GUIへ該当区間の波形と再生位置を表示する。利用者は音声を
再生しながらキーを押すか、画面上の登録操作を行って同期境界を連続入力する。

- 各登録位置は表示単位の開始位置とする。
- 表示単位が`N`個なら、最後の表示単位の終了位置を含めて`N + 1`個の境界を登録する。
- 隣り合う境界の間で、その表示単位の進捗を`0%`から`100%`まで連続的に進める。
- 最初の境界より前は全表示単位を未消費、最後の境界より後は全表示単位を完了として扱う。
- 波形上でも境界の登録、選択、移動、削除、直前操作の取り消しができるようにする。
- 次に登録する表示単位をGUI上で明示し、登録済み境界も波形上へ表示する。

連続入力で通常速度では操作が間に合わない場合があるため、少なくとも`1/4`、`1/2`、`1`倍の再生を
用意する。高速な位置確認用として`2`倍再生も候補とする。低速再生中に保存する位置は変速後の経過時間
ではなく、元WAV上の時刻へ変換する。キー入力時刻は画面更新タイマーではなく、音声再生クロックを基準に
求め、音声バッファによる遅延も考慮する。

この専用GUI方式が成立するため、AviUtl2のタイムラインカーソルを止め、Filterの同期ボタンを1回ずつ
押して登録する旧手動案は採用しない。手動同期の登録操作は専用GUI内へ一本化する。

音声処理ライブラリはSYNC_Lyrics本体から分離し、配布条件に合わせてライセンスを変更できる構成とする。
FFmpeg DLL等の利用を許可し、WAVのデコード、シーク、変速処理へ使用できるものとする。音声出力、
波形生成、変速再生、元WAV時刻への変換は責務を分け、利用ライブラリを交換できる境界を設ける。

曲同期とWAV手動同期のどちらも確定結果は同じテキスト入力項目へ保存する。同期設定GUIは保存済みヘッダを
優先して対応モードを復元し、未設定時または別モードへ切り替える場合は利用者がモードを選択できるように
する。モードの選択をファイルの有無だけには依存させない。

## 表示タイプ

同期処理は共通化し、`タイプ` によって描画だけを切り替える。最初に実装するタイプと追加候補は、
実装開始前に絞り込む。

候補:

- 通常のカラオケ歌詞テロップ
- MV向けの文字単位強調
- 1文字ずつ出現する表示
- 同期結果を利用したその他の行単位演出

`SYNC_PianoRoll` は兄妹製品として独立しているため、ピアノロールそのものを
SYNC_Lyricsの表示タイプへ統合するかは未決定とする。

## 現在の最小プロジェクト構成

プロジェクトごとの子フォルダーを増やさず、リポジトリのルートを最小限に保つ。

```text
SYNC_Lyrics_Input.dpr
SYNC_Lyrics_Input.dproj
SYNC_Lyrics_Filter.dpr
SYNC_Lyrics_Filter.dproj
Source\
  Common\
    Timeline\
      SYNC_Lyrics_FrameShared.pas
      SYNC_Lyrics_ContextManager.pas
      SYNC_Lyrics_Time.pas
  Plugin\
    Input\
      SYNC_Lyrics_InputPlugin.pas
    Filter\
      SYNC_Lyrics_FilterPlugin.pas
  ...
  Lib\
    ...
Tests\
  SYNC_Lyrics_FrameContextTests.dpr
```

- `SYNC_Lyrics_Input` は `.synclyrics` の透明ベース映像を提供し、要求された絶対フレーム位置を
  `Local\SYNC_Lyrics_Frame_V1` へ公開する。
- 仮想素材のファイル名形式は `Width_Height_MaxSec_Fps_Scale.synclyrics` とする。
- 値を省略または解釈できない場合は、幅1、高さ1、3600秒、30fps、scale 1を使う。
- `SYNC_Lyrics_Filter` は `SYNC_歌詞テロップ_Filter`、グループ名 `SYNC` として登録する。
- 現在のFilterは設定項目と描画処理をまだ持たず、入力映像を変更しない。ただし、共有絶対フレームの
  受信と、Inputがキャッシュで再発火しない間のFilter相対フレーム補間は実装済みである。
- 設定項目が0個でも、AviUtl2へは直接nilではなく、nil終端した項目ポインター配列を渡す。
- SDKの最小ABI定義と共有メモリ基礎処理は `SYNC_PianoRoll\Source\Lib` からコピーした。

- `.dpr` はexport境界と必要ユニットの列挙だけに留める。
- InputとFilterのプラグインユニットは登録と処理の入口を担当する。
- 歌詞解析、同期消費、文字レイアウト、各表示タイプ、GUI等は責務ごとに専用ユニットへ分ける。
- 2つ以上の固有ユニットで共通化できる処理と汎用SDK定義は `Source\Lib` へ置く。
- 元プロジェクトからライブラリをコピーする場合も、不要なユニットを一括で持ち込まず、
  実際に使う依存だけを置く。

## 安全性・性能ルール

- グローバルな可変状態を避け、AviUtl2からの並列呼び出しを前提に共有キャッシュと共有メモリを設計する。
- Input、Filter、GUIの各コールバック境界からDelphi例外を外へ漏らさない。
- ファイルI/O、解析、歌詞データ異常によってAviUtl2を停止・終了させないことを最優先とする。
- 毎フレームの処理ではファイル再読込、不要なメモリ確保、GUI値の書き戻しを行わない。
- 同じ同期データを複数オブジェクトが参照する場合は、解析結果を読み取り専用キャッシュとして共有する。
- ファイル不存在、未知形式、破損、解析失敗時の表示とフォールバック動作は実装前に確定する。

## 共通ビルドルール

- Delphi 37.0を使用し、対象プラットフォームはWin64だけとする。
- `_Input` と `_Filter` の両プロジェクトについて、DebugとReleaseのビルド設定を保つ。
- コンパイル警告とエラーを確認し、原則として警告0、エラー0で完了とする。
- Debugは生成した `.dll` と `.rsm` を調査用に残し、プラグイン拡張子のファイルも作る。
- Releaseは `.aui2` または `.auf2` を作った後、同じ出力先の `.dll` と `.rsm` を削除する。
- `Win32`、`Win64`、`.dcu`、`.rsm`、`.dll`、`.aui2`、`.auf2` はGitHubへ同期しない。
- ビルド前に `C:\ProgramData\aviutl2\Plugin\SYNC_Lyrics` がなければ作成し、DLLを同フォルダーへ出力する。
- Debugは同フォルダーでDLLを `.aui2` または `.auf2` へコピーし、DLLとRSMも残す。
- Releaseは同フォルダーでDLLを `.aui2` または `.auf2` へコピーした後、DLLとRSMを削除する。

作業フォルダーを使うビルドコマンドは次のとおり。

Input Debug Win64:

```powershell
cmd /c "call ""C:\Program Files (x86)\Embarcadero\Studio\37.0\bin\rsvars.bat"" && msbuild ""D:\DelphiProg\test\SYNC_Lyrics\SYNC_Lyrics_Input.dproj"" /t:Build /p:Config=Debug /p:Platform=Win64"
```

Filter Debug Win64:

```powershell
cmd /c "call ""C:\Program Files (x86)\Embarcadero\Studio\37.0\bin\rsvars.bat"" && msbuild ""D:\DelphiProg\test\SYNC_Lyrics\SYNC_Lyrics_Filter.dproj"" /t:Build /p:Config=Debug /p:Platform=Win64"
```

Input Release Win64:

```powershell
cmd /c "call ""C:\Program Files (x86)\Embarcadero\Studio\37.0\bin\rsvars.bat"" && msbuild ""D:\DelphiProg\test\SYNC_Lyrics\SYNC_Lyrics_Input.dproj"" /t:Build /p:Config=Release /p:Platform=Win64"
```

Filter Release Win64:

```powershell
cmd /c "call ""C:\Program Files (x86)\Embarcadero\Studio\37.0\bin\rsvars.bat"" && msbuild ""D:\DelphiProg\test\SYNC_Lyrics\SYNC_Lyrics_Filter.dproj"" /t:Build /p:Config=Release /p:Platform=Win64"
```

配備先:

```text
C:\ProgramData\aviutl2\Plugin\SYNC_Lyrics\SYNC_Lyrics_Input.aui2
C:\ProgramData\aviutl2\Plugin\SYNC_Lyrics\SYNC_Lyrics_Filter.auf2
```

## 配布・インストール用変数

パッケージインストーラーのパス指定には、固定の絶対パスではなく次の変数を使う。
特に解析キャッシュ、設定、その他の永続データを配置する段階では `{dataDir}` を使用する。

| 変数 | 指す場所 | 主な用途 |
| --- | --- | --- |
| `{tmp}` | パッケージとバージョンごとに自動作成される一時フォルダー | ダウンロード先、ZIP展開先、コピー元 |
| `{pluginsDir}` | AviUtl2のプラグイン配置先 | `.aui2`、`.auf2`、付属ファイルの配置 |
| `{scriptsDir}` | AviUtl2のスクリプト配置先 | `.anm2` 等のスクリプト配置 |
| `{dataDir}` | `Language`、`Plugin`、`Script` の親に当たるAviUtl2データ保存先 | 永続データ、解析キャッシュ、共通設定 |

インストール処理には `ダウンロード`、`ZIP展開`、`EXE実行`、`コピー` の4種類がある。
ダウンロードとZIP展開では保存先を指定せず、自動作成される `{tmp}` を使用する。
EXEの実行パスと引数、コピー元とコピー先の指定には上記変数を使用する。

コピー元にはファイルまたはフォルダーを指定できるが、コピー先にはフォルダーを指定する。
フォルダーをコピー元にした場合は、そのフォルダー自体ではなく内部のファイル一式がコピー先へ入る。

```text
コピー元: {tmp}/<展開フォルダー>/SYNC_Lyrics
コピー先: {pluginsDir}/SYNC_Lyrics
```

将来データファイルが必要になった場合は、プラグイン本体へ混在させず次のように分ける。

```text
コピー元: {tmp}/<展開フォルダー>/Data/SYNC_Lyrics
コピー先: {dataDir}/SYNC_Lyrics
```

アンインストール処理には `削除` と `EXE実行` を使用できる。利用者が生成した設定や解析キャッシュを
アンインストール時に削除するかは別途決め、インストール物と利用者生成データを同じ削除対象へまとめない。

バージョン管理にはハッシュ値を使用する。ハッシュ対象は主要ファイルに絞り、少なくとも
`SYNC_Lyrics_Input.aui2` と `SYNC_Lyrics_Filter.auf2` を候補とする。

## コメントルール

- コメントはコードを読み直しただけで分かる内容ではなく、目的、責務、注意点、状態や値の意味を補うために書く。
- 古い仕様や現在の実装と食い違うコメントは、見つけた時点で更新する。
- 不要なコメント、同じ内容の重複、処理を日本語へ置き換えただけのコメントを増やさない。
- ユニット先頭には、そのユニットの目的と担当範囲を `//` で書く。
- `interface` に公開する関数・手続きには、呼び出し側から見た責務、入出力、重要な副作用を書く。
- フィールドや定数の短い説明は行末へ置き、同じブロックでは `:`、`=`、`//` の位置を可能な範囲で揃える。
- レコードの各フィールドには用途または値の意味を書き、ABI定義では特に配置と型の理由を明記する。
- コメントと対象の宣言または実装の間に不要な空行を入れない。
- `var` ブロック内へローカル関数・手続きを置かず、必要な補助処理は同じ `implementation` の独立関数へ分ける。
- `property`、`procedure`、`function` の宣言は、112文字以内なら折り返さない。
- 日本語文字列リテラルを持つ `.pas` と `.dpr` はUTF-8 BOM付きで保存する。
- DelphiのSDKレコードはC/C++側のABIと正確に一致させ、フィールド追加時は順序、型、
  アラインメントを公式SDKと照合する。

## GitHub同期ルール

- 同期対象は `.pas`、`.dpr`、`.dproj`、`.res`、文書、配布・検証に必要なスクリプトと素材。
- ビルド成果物、IDEローカル設定、履歴・復旧データは同期しない。
- `.gitattributes` でPascal、プロジェクト、文書の改行をCRLFへ統一する。
- `.res`、画像、WAV、AviUtl2プロジェクト等はbinaryとして扱う。
- GitHub Releasesへ配布物を登録する場合も、通常のGit履歴へビルド成果物を直接追加しない。

## 初期実装前に決めること

1. 1行分の歌詞、ルビ、表示単位、同期パラメーター列の保存形式を決める。
2. 同期パラメーター数と終端規則を、実際の日本語・英語歌詞で検証して決める。
3. 同期元として読むファイル形式と、音の単位を決める。
4. 専用GUIの編集項目、表示タイプ、開始・同期・終了演出の初期候補を絞る。
5. 実装済みの共有レコードと補間コンテキストをAviUtl2実機で確認し、複数オブジェクト時の対応規則を確定する。
6. 異常データ時の無表示、未消費表示、またはその他のフォールバック動作を決める。

## 作業ログ

- 2026-07-27: `SYNC_Motion\note.md` から共通の識別名、ビルド、配布、コメント、保守、
  GitHub同期ルールを移植した。
- 2026-07-27: 打ち合わせ内容をもとに、3オブジェクト構成、InputとFilterのグループ分割、
  1行単位処理、音の消費モデル、順序を表す同期パラメーター、表示時間、専用GUI、表示タイプを整理した。
- 2026-07-27: 打ち合わせの確定事項、利用者の基本編集手順、消費処理で独立して進む3位置、
  行内の消費状態、英語の複数単語消費、旧方式の設定要素からの移行方針を追記した。
- 2026-07-27: `SYNC_PianoRoll` を参考にInputとFilterの最小プロジェクトを作成した。
  Inputは透明ベースと共有フレーム公開、Filterは設定項目なしのパススルー登録とした。
  Debug Win64で両プロジェクトが警告0、エラー0でビルドできることを確認した。
- 2026-07-27: 作業フォルダーを `D:\DelphiProg\test\SYNC_Lyric` から
  `D:\DelphiProg\test\SYNC_Lyrics` へ変更し、ノート内のビルドパスも更新した。
- 2026-07-27: Gitリポジトリを初期化し、`SYNC_PianoRoll` と同じローカル設定、
  `.gitignore`、`.gitattributes`、CRLF・binary属性を適用した。リモートは
  `https://github.com/vramwiz/SYNC_Lyrics` とする。
- 2026-07-27: Inputが公開した絶対フレームとFilterの相対フレームを
  `Object_.ID + Object_.EffectID`ごとに対応付ける補間コンテキストを実装した。
  Inputの共有更新番号が変化したときだけ再基準化し、入力キャッシュで再発火しない間は
  `現在の相対フレーム - 基準相対フレーム`を最後の絶対フレームへ加算する。
- 2026-07-27: 統合テストでInputを絶対150フレームに一度だけ発火させ、Filter相対フレーム
  10、11、40から絶対150、151、180を取得した。Inputを絶対300で再発火させると300へ
  再基準化し、次の相対フレームで301となることを確認した。FilterのDebug Win64構成も
  ロード中の配置済みファイルへ触れない検証出力先で、警告0・エラー0のビルドに成功した。
- 2026-07-27: 実装参照元として`SYNC_PianoRoll`、Syncroh2の歌詞テロップFilter／Extension
  プロジェクトと両実装フォルダーを記録した。
- 2026-07-27: 更新版Input／FilterをAviUtl2へ配置して再起動し、Rootのレイヤー1・
  230～378フレームにある`.synclyrics`素材＋`SYNC_歌詞テロップ_Filter`構成で実機確認した。
  先頭230フレームではInput共有値が絶対0、rate 30、scale 1、0秒となった。231、232を
  初回描画すると共有値は1、2へ進み、更新番号も6、8へ進んだ。その後231、232へ戻って
  再描画すると、Filter相対フレームは1、2へ変化している一方、Input共有値は絶対2、
  更新番号8、更新時刻も同じ値のまま停止した。入力キャッシュでInputが再発火しない状態を
  実機で再現し、その間を相対フレーム差で補間する必要性と実装条件を確認した。
- 2026-07-27: 歌詞の事前表示と事後維持をオブジェクト長だけで表現する方針を変更した。
  前後へ延長した区間で前の歌詞または次の歌詞へ同期することを防ぐため、
  `事前時間 (秒)`と`事後時間 (秒)`をFilter設定として持ち、その区間では同期消費を
  進めず、未消費状態または完了状態を維持する。
- 2026-07-27: 歌詞入力の第1検証として、Filterの標準GUIへ1行文字列の`歌詞`項目を追加した。
  入力したUnicode文字列を96pxの白文字で透明RGBA画像の中央へ描画し、空文字では完全透明にする。
  単体テストで日本語文字列が可視画素を生成することと空文字が透明になることを確認し、Filter全体も
  Debug Win64でビルドできることを確認した。
- 2026-07-27: AviUtl2実機で`歌詞`項目へ入力した通常文字列が表示されることを確認した。
  第1検証では専用構文を未解析のためルビは表示されない。次段階では既存の歌詞テロップ実装を参照し、
  ルビ構文の解析結果を本文とルビの上下二段へ分けて描画する。
- 2026-07-27: 既存実装の`[本文](ルビ)`構文に対応した。複数のルビ区間を解析し、構文を除いた
  本文全体を中央配置したうえで、各ルビを対応本文区間の中央上へ42pxで描画する。本文は96pxとする。
  解析テスト、ルビ上段の描画テスト、空文字テスト、入力キャッシュ補間の回帰テストがすべて成功し、
  Filter全体もDebug Win64でビルドできることを確認した。
- 2026-07-27: 本文が1文字の場合の省略記法`字(ルビ)`へ対応した。
  `[漢字](かんじ)を読(よ)む`を本文`漢字を読む`と2つのルビ区間へ解析できることを確認した。
- 2026-07-27: AviUtl2実機で`[漢字](かんじ)を読(よ)む`を表示し、複数文字のルビ`かんじ`と
  1文字省略記法のルビ`よ`が、それぞれ対応する本文の中央上へ表示されることを確認した。
  ルビ入力・解析・基本描画の段階は完了し、次は音楽データのノート時刻列を使う同期処理へ進む。
- 2026-07-27: MIDI同期の第1検証を実装した。音楽ファイルと0始まりのトラック番号を設定し、
  Filterオブジェクト先頭から現在までのノートオン数を1音1文字として消費する。消費済み本文と、
  対応本文をすべて消費したルビは水色、未消費部分は白で描画する。固定MIDIデータから0秒と0.5秒の
  2音を復元するテスト、消費済み色の描画テスト、ルビ解析・描画テスト、入力キャッシュ補間テストが
  すべて成功した。
- 2026-07-27: `SYNC_PianoRoll\Source\Lib\SongReader`の`.pas`ソース一式を取り込み、独自の
  最小MIDI解析を`TSongData.LoadFromMusicFile`利用へ置き換えた。MIDI、UST、VSQ／VSQX、
  MusicXML、MuseScoreをPianoRollと同じ読込規則で扱う。SongReaderへ一時MIDIファイルを渡す
  統合テストでノート開始時刻と区間内消費数を確認し、全ReaderをリンクしたFilterの
  Debug Win64ビルドにも成功した。
- 2026-07-27: AviUtl2実機で音楽ファイルを指定し、ノート開始時刻に合わせて歌詞が先頭から
  白から水色へ変化することを確認した。Inputの絶対フレーム、キャッシュ補間、SongReaderの
  ノート時刻列、1音1文字の消費、描画色変更までの最小同期経路が成立した。
- 2026-07-27: ノート開始時に1文字全体を即時変色する方式から、ノート開始・終了間の進捗率で
  左から右へクリッピングする方式へ変更した。ルビ指定された対応本文とルビは1表示単位として
  同じ進捗率を共有し、それぞれ異なる実測横幅へ進捗率を掛けて別々のクリップ境界を求める。
  ノート中間で`0.5`表示単位となる同期テストと、`[漢字](かんじ)`の本文・ルビ両方へ50%の
  変化後色が描画されるテストに成功した。
- 2026-07-27: AviUtl2実機で本文とルビが同じ進捗率を共有し、異なる実測幅で滑らかに変色することを
  確認した。行全体は同じレイヤーのオブジェクトをグループ制御で配置し、Filterには中央基準の
  `X`、`Y`だけを最小限の座標設定として追加した。今後追加する色、本文・ルビ別フォントとサイズ、
  フォント設定ボタン、ルビ間隔補正、字間、および不足候補もFilter設定要素へ記録した。
- 2026-07-28: 基本表示設定の第1段階として、Filterへ`フォント名`、`フォント名（ルビ）`、
  `変化前色`、`変化後色`、`歌詞サイズ`、`ルビサイズ`を追加した。描画処理へ設定レコードを渡し、
  本文とルビのフォント名・サイズおよび同期前後の色へ反映する。白文字マスクを色描画と別に生成して
  透明度を決め、黒や暗色も透明にならず表示できるようにした。設定色とサイズの描画テスト、
  既存のルビ・同期・座標・入力キャッシュ補間・音楽同期テストがすべて成功し、FilterもDebug Win64で
  警告0、エラー0のビルドに成功した。
- 2026-07-28: Syncroh2の歌詞テロップFilterを参照し、標準GUIの`フォント設定`ボタンから開く
  専用GUIを追加した。インストール済みフォントを本文用・ルビ用の一覧へ表示し、それぞれの
  プレビューを確認して選択できる。OK時だけ選択中オブジェクトの`フォント名`と
  `フォント名（ルビ）`へUTF-8で書き戻し、キャンセル時は変更しない。2項目目の反映に失敗した場合は
  1項目目を元へ戻す。編集APIの関数型を公式SDKと参照実装に合わせ、FilterのDebug Win64ビルドと
  既存の描画・入力キャッシュ補間・音楽同期テストが警告0、エラー0ですべて成功した。
- 2026-07-28: `SYNC_Lyrics_FontSettingsForm.dfm`がBOMなしUTF-8だったため、Delphi IDEで
  ANSIとして誤認される文字化けを修正した。フォームの`.pas`と`.dfm`をUTF-8 BOM付き・CRLFへ統一し、
  FilterのDebug Win64ビルドが警告0、エラー0で成功することを再確認した。
- 2026-07-28: 本文サイズとルビサイズを独立して指定し、二段全体を中央へ再配置する現在の方式を
  維持したまま、Filterへ`ルビ間隔補正`と`字間`を追加した。どちらも初期値は`0`で既存表示を変えず、
  ルビ間隔補正は既定の4pxへ加算し、字間は本文の測定・通常描画・同期クリッピング描画へ共通して
  適用する。字間による横幅変化とルビ間隔による上下間隔変化の描画テストが成功し、Filterも
  Debug Win64で警告0、エラー0のビルドに成功した。
- 2026-07-28: Filterへ`事前表示 (秒)`と`表示維持 (秒)`を追加し、初期値`0.50`秒、
  変更単位`0.01`秒とした。オブジェクト先頭から事前表示時間は未消費状態を維持し、その終了以降の音を
  歌詞の表示単位数だけ割り当てる。最後の割り当て音の終了から表示維持時間を超えた後は完全透明にする。
  後続行の音で表示時間が延びないこと、事前・同期中・完了維持・透明化の各境界、および音不足時に
  未完了状態を残すフォールバックの同期テストが成功した。Rendererテストも成功し、Filterは
  Debug Win64で警告0、エラー0のビルドに成功した。
- 2026-07-28: 表示終了をFilter設定とオブジェクト末尾で二重管理しないよう、`表示維持 (秒)`と
  時間経過後の透明化処理を削除した。最後に割り当てた音が終了した後は完了状態をオブジェクト末尾まで
  維持し、表示時間はタイムライン上の末尾位置で直接調整する。`事前表示 (秒)`と表示単位数による
  後続音の除外は維持した。
- 2026-07-28: 表示要素の将来候補には太字・斜体等のフォントスタイルと`字間（ルビ）`を残し、
  縁取り・影等はSYNC_Lyricsへ実装せずAviUtl2の既存Filterで構成する方針を確定した。
- 2026-07-28: 同期調整の第1段階として`同期1`～`同期16`を追加した。値`0`は1音で1表示単位、
  正の`n`は`n + 1`音で1表示単位、負の`n`は1音で`abs(n) + 1`表示単位を連続的に進める。
  GUI範囲は`-63`～`63`とし、16段階を超えた部分は暗黙の`0`で継続する。2音で1表示単位、
  1音で2表示単位、調整段階後の暗黙値を固定MIDIで検証し、MusicSyncテストが成功した。
  FilterもDebug Win64で警告0、エラー0のビルドに成功した。
- 2026-07-28: 同期設定の次期方針を整理した。曲同期とWAV手動同期は、モードと形式バージョンを表す
  ヘッダ付きの共通テキスト項目へ保存し、専用GUIで既存値の読込と全体置換を行う。手動同期はWAV波形を
  表示して`1/4`、`1/2`、`1`倍等で再生し、キーまたは画面操作で表示単位数`+ 1`個の境界を連続登録する。
  FFmpeg DLL等を交換可能な音声処理部として利用できる構成とし、AviUtl2タイムライン上の同期ボタンで
  1件ずつ登録する案は採用しない。
- 2026-07-28: 同期設定の共通テキスト形式第1版を実装した。`同期1`～`同期16`の標準GUI項目を1つの
  `同期データ`文字列へ置き換え、`SYNC_LYRICS_SYNC/1;mode=music;stages=...`から曲同期の段階値を
  復元して既存の連続進捗計算へ渡す。空の段階列は暗黙の`0`、不正なヘッダ、未知・重複項目、範囲外値は
  未消費表示へフォールバックする。シリアライズ往復と不正値のテスト、既存の音楽同期・フレーム補間・
  描画テストがすべて成功し、FilterもDebug Win64で警告0、エラー0のビルドに成功した。
- 2026-07-28: `同期データ`の直前へ`曲同期設定`ボタンを追加し、空の専用フォームを開く入口を実装した。
  曲同期用ユニットは`SYNC_Lyrics_MusicSyncSettingsForm`とし、今後追加する手動同期用ユニットは
  `SYNC_Lyrics_ManualSyncSettingsForm`として役割と名称を分ける。
- 2026-07-28: 曲同期GUIの基準位置取得を検証する第1段階として、Filterが最後に正常解決した絶対フレーム、
  フレームレート分子・分母をスレッド安全に保持する`SYNC_Lyrics_MusicSyncAnchor`を追加した。
  `曲同期設定`ボタンを押すと、フォームへ基準絶対フレームと秒位置を表示する。まだFilterが発火して
  いない場合は未取得と表示する。現段階では選択オブジェクトとの厳密な照合を行わず、最後に発火した
  Filter位置を使って取得経路を実機検証する。
- 2026-07-28: 曲同期GUIへ現在の歌詞、音楽ファイル、トラック、同期データを読み取り専用で渡す
  概要画面を追加した。歌詞はルビ構文を解析し、本文1文字またはルビ対応本文を1つとする同期対象の
  表示単位一覧へ展開する。同期データは曲同期ヘッダ、形式バージョン、明示された段階数を検証して表示する。
  この段階では書き戻しを行わず、次の音と表示単位の対応編集に必要な入力値を確認できるようにした。
- 2026-07-28: 曲同期GUIで音楽ファイルをSongReader経由で読み込み、基準絶対フレームから換算した秒位置
  以降かつ指定トラックに一致するノートの開始・終了時刻を一覧表示するようにした。各行には曲先頭からの
  絶対秒、基準からの相対秒、トラック番号を表示する。大きい音楽データでGUIへ過剰な行を追加しないよう、
  件数は全対象を数えつつ一覧表示は先頭256音に制限する。
- 2026-07-28: 曲同期GUIの構成を作り直す第1段階として、同期関係の表示・編集を入れず、`SYNC_PianoRoll`
  の横表示を参考にした最小ピアノロールを追加した。左に白鍵・黒鍵、右に音階レーン、基準位置を示す
  発音線、1秒間隔の時間線、基準から6秒先までのノート矩形を表示する。表示に必要なMIDIキー番号を
  音楽ノートデータへ追加した。
- 2026-07-28: 最小ピアノロールの黒鍵右側が暗いレーン背景と連続し、白鍵と同じ長さに見える表示を修正した。
  単純な半音等間隔ではサンプルの鍵盤形状にならなかったため、`SYNC_PianoRoll`と同じ白鍵単位の
  音階座標へ変更した。連続した白鍵を先に描き、その境界へ高さ62%、長さ62%の黒鍵を重ねる。
  音階レーンとノート矩形も同じ座標計算を共有する。
- 2026-07-28: ピアノロールの各ノートへ小さい音名を表示した。音名は`C`～`B`、半音は`#`付きとする。
  SongReaderのノート歌詞を表示用データへ引き継ぎ、歌詞がある場合はノート上側、画面上端に余裕が
  なければ下側へ表示する。
- 2026-07-28: ノート内の音名とノート外の歌詞を一段大きくした。黄色のGUI基準線に加え、Filterの
  `事前表示 (秒)`だけ右へ進んだ位置へピンク色の縦線を表示し、同期開始位置を確認できるようにした。
- 2026-07-28: ピンク色の事前表示線へマウスを近づけると左右移動カーソルへ変え、左ドラッグで
  0～6秒の範囲を移動できるようにした。現段階ではフォーム内のプレビュー値だけを変更し、
  Filterの`事前表示 (秒)`や同期データへは書き戻さない。
- 2026-07-28: 事前表示線のドラッグ中にピアノロール全体を直接描き直していたため発生したちらつきを
  修正した。フォームをダブルバッファ化し、ピアノロールは保持用32bit Bitmapへ全要素を描いてから
  1回でPaintBoxへ転送する。今後のノート選択や同期表示も同じオフスクリーン描画経路へ追加する。
- 2026-07-28: 曲同期GUIをピアノロール中心の本番表示へ整理した。基準フレーム、基準時刻、
  音楽ファイル、トラック、ノート数などの確認用表示を外し、下部の操作領域を除く全面へ
  ピアノロールを広げた。読み込み不能など利用者が対処すべき状態だけはロール内へ表示する。
- 2026-07-28: 音楽データ由来の歌詞はノート矩形内で音名の右側へクリップ表示し、矩形外へ
  はみ出さないようにした。Filter側の歌詞は表示単位へ解析して同期対象ノートの直下へ表示する。
  画面下部に歌詞入力欄と適用ボタンを追加し、適用時に対象Filterの`歌詞`項目へ書き戻す。
- 2026-07-28: ピアノロールのノート矩形は、白鍵・黒鍵の形状差に影響されず全音で同じ高さとした。
  ノートの縦中心は対応する鍵盤の音程中心座標へ揃える。
- 2026-07-28: ノート矩形の上下端を小数座標から別々に丸めることで、音程位置によって高さが
  1px変動していた点を修正した。整数の固定高さを先に決め、下端を`上端 + 固定高さ`で求める。
- 2026-07-28: 背景の音程レーンが白鍵では太く黒鍵では細かったため、固定高のノートを載せると
  中心がずれて見える問題を修正した。縦軸をMIDI半音ごとの等間隔へ統一し、背景レーンは全音程で
  同じ高さとする。鍵盤の黒鍵は短い形状を維持し、鍵盤・レーン・ノートの中心座標だけを共有する。
- 2026-07-28: Filter歌詞とノート下端の間隔を5pxへ広げた。ピアノロール上のFilter歌詞を
  クリック可能にし、選択中の1表示単位だけに1本の仮想カーソルを表示する。別の表示単位を
  クリックすると同じカーソルが移動し、複数カーソルや範囲選択は作らない。
- 2026-07-28: 仮想カーソルを縦線から選択歌詞を囲む枠へ変更し、歌詞上のマウス押下から
  左右ドラッグで同期段階を調整できる第1実装を追加した。36pxごとに1段階とし、右は選択歌詞を
  `複数音で1表示単位`へ、左は選択歌詞と左側の歌詞を`1音で複数表示単位`へまとめる。
  ドラッグ結果はピアノロールの歌詞配置へ即時反映し、適用時に既存の`stages`形式で書き戻す。
- 2026-07-28: 左隣から始まる`2文字で1音`の2文字目を右ドラッグした場合、共有を解除して
  `1文字1音`へ戻すだけとした。同じドラッグで選択文字を`2音で1文字`へ進めず、中立の
  `1文字1音`を飛び越えない。さらに右へ進める場合は、共有解除後の次のドラッグで調整する。
- 2026-07-28: ドラッグ時に被覆値から左方向へ同期段階を探索する方式を廃止した。GUI内部では
  `開始歌詞・歌詞数・音数`を持つ同期グループ一覧を正本とし、各歌詞から所属グループを直接引く
  対応表を生成する。左ドラッグは1文字ではなく直前の同期グループ全体を結合するため、
  1～2文字目が共有済みの状態で3文字目を左へ動かすと、開始位置0の3文字共有グループになる。
  適用時だけ同期グループを従来の`stages`値へ変換する。
- 2026-07-28: 共有グループ内の歌詞を右ドラッグしたとき、グループ左端を右へずらす方式では
  選択文字以外も移動していたため、選択文字だけを切り離す方式へ変更した。
  `[を・読・む]`で`む`を右へ動かすと、`[を・読] [む]`となる。選択文字の左右に別の文字が
  残る場合は左右の共有グループをそれぞれ維持し、選択文字だけを1文字1音のグループにする。
- 2026-07-28: 歌詞編集時は既存同期グループを可能な範囲で保持しつつ、崩れた場合に復旧できるよう
  歌詞入力欄の隣へ`同期を初期化`ボタンを追加した。押すと全表示単位を1文字1音のグループへ戻し、
  ピアノロールへ即時反映する。Filterへの書き戻しは従来どおり`適用`時だけ行う。
- 2026-07-28: 音程に追従する既存歌詞とは別に、ピアノロール下部へ固定Yの同期歌詞レーンを
  追加する方針とした。同期グループの時間範囲を本文文字数で等分し、ルビ対応本文が1表示単位でも
  複数音を使う場合は、本文の各文字を時間方向の適切な位置へ分けて表示する。ルビは可能な場合に
  対応本文範囲の中央上へ表示する。第1段階は表示のみとし、操作は表示確認後に追加する。
- 2026-07-28: 曲同期フォームが約1130行まで肥大化したため、フォームをVCLイベントと各要素の
  受け渡しを行う調整役へ変更した。`SYNC_Lyrics_MusicSyncEditModel`は歌詞単位、同期グループ、
  ドラッグ編集、stages変換を担当する。`SYNC_Lyrics_MusicSyncPianoRoll`は鍵盤、背景、ノートと
  歌詞層用のノート座標を担当する。`SYNC_Lyrics_MusicSyncNoteLyrics`はノート追従歌詞の表示、
  選択枠、ヒットテストを担当する。`SYNC_Lyrics_MusicSyncFixedLyrics`は固定Yの本文・ルビ表示を
  担当し、後から専用操作を追加できる境界とした。フォーム本体は約370行となり、各公開処理へ
  責務と受け渡し内容のコメントを追加した。
- 2026-07-28: 下部の固定Y歌詞レーンも、ノート追従歌詞と同じ同期グループ状態を参照する。
  `漢字`が1表示単位・1音の間は第1ノート開始Xから通常の文字間隔で横並びにする。その表示単位を
  2音へ変更した時だけ、`漢`は第1ノートに残して`字`を第2ノート開始Xへ移す。後続表示単位の
  開始ノートも消費音数に従って移動する。ルビは配置後の本文範囲を基準に中央へ表示する。
- 2026-07-28: ノート追従歌詞と固定Y歌詞レーンの選択状態・ドラッグ処理を共通化した。
  どちらの表示をクリックしても同じ歌詞単位を選択し、上段と下段の対応位置へ同時に黄色の
  選択枠を表示する。下段にも歌詞単位別のヒット領域を持たせ、既存の同期グループ編集処理へ
  接続したため、上下どちらからドラッグしても同じ同期結果になる。
- 2026-07-28: 音を持たない文字を同期数から分離する第1段階として、共通歌詞パーサーの
  表示単位へ`ConsumesNote`を追加した。日本語文字・英数字・長音記号・踊り字などは発音対象、
  空白・句読点・括弧は非発音対象と判定する。明示ルビ付きの表示単位は読みが指定されているため
  常に発音対象とする。同じ分類を曲同期GUIの編集モデルまで伝播させたが、この段階では既存同期
  データとの互換性を保つため、表示単位数と同期計算はまだ変更しない。次段階で非発音単位を
  前後の発音単位へ付随させ、同期音を消費しないようにする。
- 2026-07-28: 非発音文字を同期単位から除外した。途中・末尾の空白や記号は直前の発音単位へ、
  行頭の空白や開き括弧は最初の発音単位へ付随させる。共通パーサーの`SyncUnitIndex`をFilterの
  描画進捗と同期対象数に使用し、曲同期GUIでは非発音文字を`PrefixText`/`SuffixText`として
  発音単位へ保持する。上下の歌詞表示と選択枠には付随文字も含めるが、付随文字だけの独立した
  カーソルや同期グループは作らない。例として`「歌、 空。」`は7文字を維持して表示しつつ、
  同期音を消費する単位は`歌`と`空`の2つになる。
- 2026-07-28: 曲同期データが未設定、空文字、または`stages=`が空の場合は、GUIを開いた時点で
  発音表示単位ごとの`1表示単位・1音`グループを生成する処理を`BuildDefaultGroups`へまとめた。
  記号・空白は既に発音単位へ付随しているため初期グループを増やさず、ルビ対応本文は本文が
  複数文字でも1表示単位として初期化する。モデルは既定値生成の有無を保持し、適用時は既存の
  正規形式に従って全0段階を`SYNC_LYRICS_SYNC/1;mode=music;stages=`として保存する。
- 2026-07-28: 歌詞編集時の同期保持を位置だけの比較から、編集前後の発音単位本文に対する
  最長共通部分列の照合へ変更した。途中へ歌詞を追加・削除しても一致する単位の同期グループを
  新しい位置へ移し、新規部分だけを`1表示単位・1音`へ初期化する。複数表示単位を1音へまとめた
  グループは全構成単位が同じ順序で連続して残る場合だけ保持し、内部へ編集が入った場合は安全の
  ため分解する。1表示単位を複数音へ割り当てた設定、記号・空白だけの編集、ルビ読みだけの変更は
  本文が一致する限り保持する。選択中の歌詞も対応後の位置へ追従する。
- 2026-07-28: 曲同期GUIのカスタム描画を96 DPI固定値から`CurrentPPI`基準へ変更した。
  鍵盤幅、固定歌詞レーン高、フォント、文字余白、選択枠、線幅、最小ノート幅、事前表示線の
  ヒット幅、歌詞ドラッグの1段階距離を`ScaleMusicSyncMetric`で同じ倍率にする。フォームは
  `PixelsPerInch=96`と`Scaled=True`を明示し、DPI変更時の`ChangeScale`でオフスクリーン
  Bitmapを破棄して再描画する。192 DPIでは鍵盤幅152px・固定歌詞レーン116pxになることを
  自動テストし、96 DPI時の従来値も維持する。
- 2026-07-28: 黒鍵は鍵盤幅の62%で描いていたが、残り38%の下地がピアノロールと同じ暗色だった
  ため、黒鍵が白鍵と同じ長さに見えていた。鍵盤領域全体を先に白鍵色で塗り、その上へ短い黒鍵を
  重ねる描画順へ変更した。192 DPIの描画テストで黒鍵内部が暗色、黒鍵右側の残り領域が白色に
  なることをピクセル値で検証する。
- 2026-07-28: 鍵盤の白鍵高が半音レーンと同じ一定幅になっていたため、鍵盤部分だけ通常の
  ピアノ形状へ変更した。音程レーンとノートのY座標は半音等間隔のまま維持する。白鍵は上下の
  隣接白鍵との中心中間まで広げるため、黒鍵を挟むC-Dなどと白鍵が隣接するE-Fなどで見かけの
  高さが変わる。黒鍵は従来どおり半音中心へ短い高さ・鍵盤幅62%で重ね、ノートとの中心対応を
  維持する。
- 2026-07-28: 黒鍵の中心座標と横方向62%の長さは維持し、縦方向の厚みだけ半音間隔の
  62%から93%へ変更した。従来比約1.5倍となり、白鍵境界上の黒鍵が見やすくなる。音程レーン、
  ノート、歌詞の座標計算には影響させない。
- 2026-07-28: 曲同期GUIのピンク色の事前表示線を0.01秒単位へ丸め、`適用`時に歌詞・同期データと
  あわせてFilterの`事前表示 (秒)`へ書き戻すようにした。3項目の途中で反映に失敗した場合は、
  先に変更した項目を元の値へ戻す。音楽同期・描画・フレーム補間テストがすべて成功し、
  FilterもDebug Win64で警告0、エラー0のビルドに成功した。
- 2026-07-28: FilterのGUI項目表示順を変更し、先頭から`曲同期設定`、`歌詞`、
  `音楽ファイル`、`トラック (-1=全て)`の順に並べた。項目名と保存値は変更しない。
  FilterはDebug Win64で警告0、エラー0のビルドに成功した。
- 2026-07-28: 本文とルビへ個別に`太字`、`斜体`、`下線`、`取り消し線`を追加した。
  本文用は`フォント名`の直下、ルビ用は`フォント名（ルビ）`の直下へチェックボックスとして配置する。
  既存表示を維持するため太字だけを初期値オンとし、他はオフとする。各設定をWindowsフォント生成へ
  反映し、斜体・下線によって描画画素が変化するテスト、既存の音楽同期・描画・フレーム補間テストが
  すべて成功した。FilterもDebug Win64で警告0、エラー0のビルドに成功した。
- 2026-07-28: AviUtl2実機で、同じレイヤーへ連続配置した2つの専用Input＋Filterオブジェクトを
  AI MIRAIから確認した。第1オブジェクトは0～201フレーム、第2オブジェクトは202～422フレームで、
  歌詞と同期設定が別々に保持されていた。100フレームと300フレームを交互に各3回レンダリングし、
  第1オブジェクトではルビ付き歌詞、第2オブジェクトでは`2番目の歌詞`が毎回描画された。同じ
  フレームの画像SHA-256も3回すべて一致し、オブジェクト間の設定値または描画状態の混線はなかった。
- 2026-07-28: 第2オブジェクトだけ曲同期GUIへノートが表示されず、描画も同期しない現象を調査した。
  第2Inputの素材再生開始は6.700秒、`事前表示 (秒)`は1.00秒であり、Filterが同期対象にするノートは
  曲先頭7.700秒以降となる。指定された`min_full_test.musicxml`は約4秒で終了するため対象ノートがなく、
  キャッシュではなく同期元データと素材再生位置の範囲不一致が直接原因だった。また曲同期GUIは
  オブジェクト先頭秒ではなく最後に描画した現在フレーム秒を`FAnchorSeconds`へ渡しており、
  カーソル位置によってGUI表示範囲がずれる別の問題も確認した。
- 2026-07-28: 曲同期GUIで利用可能なノート数が同期グループの必要音数に足りない場合、
  最初に割り当てられない表示単位以降の歌詞を、画面右端へ赤系の`未割当:`表示として追加した。
  1表示単位へ複数音を割り当てたグループは必要音数を一部しか満たさない場合も未割当とする。
  全歌詞へ割り当て可能な場合は警告を表示しない。判定テストは成功し、FilterもDebug Win64で
  警告0、エラー0のビルドに成功した。
- 2026-07-28: WAV手動同期の速度変更再生準備として、VideoMinerが使用するGyan.dev版
  FFmpeg 8.1.1 full shared buildの必要DLL 6個と、Delphi用`FFmpegApi`／
  `FFmpegAudioTempo`をコピーした。DLLは`ThirdParty/FFmpeg/bin`へ置き、GitHubの
  単一ファイル上限を超える`avfilter-11.dll`を含むためGit LFS対象とした。プロジェクトの
  ルートライセンスをGitHubが検出可能な正式GPLv3全文とし、FFmpegの版、配布元、対応ソース、
  ビルド情報、同梱DLLを`THIRD_PARTY_NOTICES.md`へ記録した。
- 2026-07-28: 手動同期の入口として、`音楽ファイル`選択欄へWAV、MP3、FLAC、M4A、
  AAC、OGG、Opus、WMAを追加した。曲同期設定ボタンは既知の楽譜拡張子を従来フォームへ、
  その他のファイルを遅延ロードしたFFmpegで検査し、音声ストリームがあれば新しい手動同期
  フォームへ振り分ける。手動同期フォームはこの段階でファイル名、再生時間、サンプルレート、
  チャンネル数を表示し、波形・再生・境界編集はまだ行わない。1秒のWAVと変換したMP3で
  FFmpegプローブが再生時間1.000秒、44100Hz、2chを取得することを確認した。楽譜拡張子判定
  テストは成功し、FilterもDebug Win64で警告0、エラー0のビルドに成功した。
- 2026-07-28: 手動同期フォームへ音声全体の静的波形を追加した。FFmpegで入力音声を
  PCM16 stereo 48kHzへ順次変換し、PCM全体は保持せず1024フレームごとのmin/maxブロックへ
  縮約してから最大4096点の包絡線にする。フォームは表示幅ごとに再縮約し、全体ピークを基準に
  自動スケールした青色波形、中央線、4分割時間目盛を描く。WAVとMP3の両方で47点の包絡線を
  生成するテストが成功し、FilterもDebug Win64で警告0、エラー0のビルドに成功した。
- 2026-07-28: 手動同期フォームへ`再生`／`停止`と`1.0`、`0.5`、`0.25`倍速を追加した。
  FFmpegでPCM16 stereo 48kHzへ小分けにデコードし、全音声を保持せずwaveOutへ約1秒先まで
  ストリーミングする。atempoが短い入力を出力しない問題を避けるため2秒分を変換単位とし、
  0.25倍速はatempoの下限0.5を満たすよう0.5×0.5の2段構成にした。再生位置は30ms間隔で
  数値と黄色の波形カーソルへ反映する。PCMデコードと0.25倍速変換のテストに成功し、
  FilterもDebug Win64で警告0、エラー0のビルドに成功した。
- 2026-07-28: 手動同期編集の第1実装として、歌詞の発音表示単位数+1本の境界をWAV全体へ
  等間隔で初期配置した。完成区間は波形下へ右向き矢印付きの歌詞枠として表示する。
  `調整モード`ではピンク色の境界線を前後の境界を越えない範囲でドラッグできる。
  `タイミング入力`では再生中のSpaceで位置を記録し、そのセッションの最初のSpaceを押した時だけ
  初期値または既存値をクリアする。モードを往復しても入力途中の位置は保持し、`打ち直し`後は
  次のSpaceで改めてクリアする。再生ループ、Pの再生/停止、Sの速度循環、確定/キャンセルも追加した。
  手動同期データを対象Filterの歌詞と同期データへ書き戻し、描画時にWAV先頭基準秒として適用する。
  編集モデルの初期配置・入力・再入力・ドラッグ・保存テストが成功し、FilterもDebug Win64で
  警告0、エラー0のビルドに成功した。
- 2026-07-28: 手動同期フォームで停止後または速度変更後に再生すると先頭へ戻る動作を修正した。
  再生開始時に黄色カーソルのWAV秒を音声プレイヤーへ渡し、FFmpegのストリーミングデコード結果を
  対応サンプル位置まで読み飛ばしてからwaveOutへ投入する。再生位置表示には開始秒を加算するため、
  停止・再開と速度変更のどちらでも同じカーソル位置を維持する。曲末の自動ループ時だけは従来どおり
  0秒へ戻す。FilterはDebug Win64で警告0、エラー0のビルドに成功した。
- 2026-07-28: 手動同期波形の初期横軸を音声全体から10秒へ変更した。通常のマウスホイールは
  カーソル位置のWAV秒を固定したまま表示幅を0.8倍／1.25倍し、1～60秒の範囲で拡大縮小する。
  Shift+ホイールは表示幅の25%ずつ時間軸を左右移動する。波形、目盛、同期線、歌詞枠、
  再生カーソル、ドラッグ座標は同じ表示開始秒と表示幅を共有する。再生カーソルが右端を越えた場合は
  次の範囲へ自動追従し、ループ時は0秒表示へ戻す。FilterはDebug Win64で警告0、エラー0の
  ビルドに成功した。
- 2026-07-28: 再生操作を`停止`と`再生／一時停止`へ分離した。一時停止は黄色カーソル位置を
  維持し、再生で同じ位置から再開する。停止は音声を止め、再生カーソルと表示範囲を0秒へ戻す。
  波形内の空き位置または黄色カーソルを左ドラッグすると再生中でも一時停止し、その位置へ
  カーソルを移動できる。黄色線上端へドラッグ対象を示す三角形を追加した。
  また、新規同期位置は音声全体ではなく初期表示の10秒内へ等間隔配置する。保存済み同期位置は
  従来どおりその値を読み込む。編集モデルテストとFilterのDebug Win64ビルドは警告0、
  エラー0で成功した。
- 2026-07-28: 手動同期波形と曲同期ピアノロールの背景ドラッグへ時間軸移動を追加した。
  手動同期では黄色の再生カーソル、調整モードの同期線を先にヒットテストし、それ以外の位置から
  左ドラッグした場合だけ表示開始秒を移動する。再生中に背景移動を始めた場合はカーソル位置を
  維持して一時停止する。ピアノロールでは事前表示線、上下の同期歌詞を優先し、それ以外の背景を
  左ドラッグすると表示開始オフセットを移動する。どちらも表示可能範囲外へは移動しない。
  FilterはDebug Win64で警告0、エラー0のビルドに成功した。
- 次回調整: 背景ドラッグによる時間軸移動自体は正常だが、手動同期波形の時間目盛り用縦線が
  画面内の固定4分割位置へ描かれ続けるため、波形だけが移動して目盛線が移動していないように見える。
  絶対秒の一定間隔を基準に縦線位置を計算し、表示開始秒の移動に合わせて縦線と時刻ラベルも
  一緒に流れる表示へ変更する。今回はコードを修正せず、次回作業項目として残す。
