# SYNC_Lyrics 作業ノート

作業再開時に最初に確認する、現在の開発状況だけを扱う短いメモ。
確定済みの詳細仕様、開発規約、過去の経緯は必要なときだけ各文書を参照する。

## 文書の役割

| 文書 | 内容 | 通常の作業開始時 |
| --- | --- | --- |
| `note.md` | 現在の開発段階、直近の方針、次の作業 | 読む |
| `DESIGN.md` | 確定済みの製品仕様・設計 | 関係する節だけ読む |
| `DEVELOPMENT.md` | 安全性、ビルド、配布、コメント、Git運用 | 関係する節だけ読む |
| `HISTORY.md` | 完了済みの実装経緯と日付付き検証履歴 | 原則として読まない |
| `README.md` | 利用者向け概要 | 公開・利用者向け作業時に読む |

- 製品名とDelphiプロジェクト名は `SYNC_Lyrics` とする。
- 作業フォルダーは `D:\DelphiProg\test\SYNC_Lyrics` とする。
- 試行錯誤を逐次追記せず、現在の方針が変わったときだけこの文書を更新する。
- 完了した作業は要点を `HISTORY.md` へ移し、この文書へ蓄積しない。

## 現在の開発段階

文字自由配置を基礎として、フォント、同期前色、同期後色、文字サイズ等の表示スタイルを
配置要素ごとに正しく設定、保存、復元、プレビュー、実描画できる状態へ進める。

- 1文字ごとの自由配置GUIは完成しており、ルビサイズを含む操作も実機確認で正常動作している。
- 配置要素は通常文字1文字、またはルビ付き本文とルビをまとめた1グループとする。
- 未指定の個別設定は`表示設定`データ内の行共通設定を継承する。
- 複数選択へ同じ値を一括適用できるようにする。
- 行共通設定と個別設定は改行を含まない`SL3`バージョン付き文字列へシリアライズし、TEdit相当の
  `string`項目`表示設定`へ保存する。β版の旧Data形式との互換性は持たせない。
  Windows単行Editの既定入力上限に合わせ、保存文字列は32,767文字以下に制限する。
- 色の編集には`Source\Lib\ColorPicker`へ統合した専用カラーピッカーを使用する。
- 文字別フォント、同期前色、同期後色、本文サイズ、ルビサイズ、本文・ルビの
  太字・斜体・下線・取り消し線、および本文字間・ルビ字間は実装済み。
- 選択要素の文字透明度、縁取り、縁ぼかし、影の色・透明度・形状を個別設定できる。未指定項目は
  行共通装飾を継承し、ルビ付き要素は本文とルビを1グループとして同じ装飾を使う。
- 1行配置では表示段1～3を曲共通の配置1～3へ対応させ、同じ表示段の全歌詞行が位置、書式、色、
  縁、影等を共有する。配置GUIは歌詞行ではなく配置1～3を選択して編集する。
- 選択済み要素の再クリックで、黄色の状態1（移動・変形）、水色の状態2（本文字間）、
  紫の状態3（ルビ）を循環する。ルビのない要素を含む場合は状態1と状態2だけを循環する。
- 状態2の左右点は、選択要素内の本文（漢字側）の字間を調整する。文字サイズ、縦横倍率、
  要素座標は変更せず、正の字間と負の字間の両方を許可する。ドラッグ中は対象本文の端が
  操作した枠線の内側へ追従する。
- 状態3の左右点はルビ字間、上中央点は本文に対するルビの相対X・Y位置を調整する。
- 固定長`data`項目は廃止済みであり、1024バイト上限は表示設定へ適用されない。
- 現在の編集画面は文字単位自由配置専用の
  `SYNC_Lyrics_CharacterLayoutSettingsForm`とする。1行単位表示用画面は別責務の
  `SYNC_Lyrics_LineDisplaySettingsForm`とする。現在は歌詞入力、オブジェクト名変更、
  背景上での本文・ルビ簡易表示、本文全体・ルビ全体のクリック選択、歌詞・ルビそれぞれの
  フォントコンボ、選択連動の太字・斜体・下線・取り消し線、行共通の同期前後色編集まで
  実装している。サイズ数値欄は設けず、今後プレビュー上のマウス操作へ割り当てる。
  閉じる操作では歌詞と専用の`表示設定`文字列を一括反映し、途中で失敗した場合は適用済み項目を元へ戻す。
- 文字装飾と配置初期化系は上部のWord風アイコンツールバーから操作できる。
  移行確認を完了し、右側の暫定数値設定欄と重複ボタンは削除済みである。
- 1行配置と文字自由配置の編集画面は共通ダークテーマを使用する。候補・フォントコンボは
  オーナードローし、通常時と選択時の背景色・文字色を明示している。
- 曲同期画面も共通ダークテーマを使用する。曲全体同期フォーム、初回歌詞入力、歌詞行一覧、
  単体・埋め込み楽譜同期の入力欄と操作パネルを暗色化し、既存の暗色ピアノロールへ揃えている。
- 曲同期ピアノロールのカーソルは、事前表示線または歌詞同期単位を横移動できる場合だけ横矢印とし、
  空白領域のホバーと表示範囲パン中は通常カーソルを使う。
- 配置要素の最終的な矩形サイズはマウスによる縦横変形で決める。本文・ルビの基準文字サイズと
  縦横倍率を100%へ正規化せず、現在の内部モデルを維持する。自由配置画面の右端は固定幅の
  同期前後カラーピッカーとし、配置要素の単一選択コンボは上部操作列へ置いて編集画面を広く使う。
- 文字の測定と実描画はGDIからGoogle Skiaへ移行した。β版の既存表示・配置データとの互換性は
  持たせず、編集画面のプレビューは配置概要として扱い、実描画との完全一致を要件にしない。
- 同期演出、同期アニメーション、開始・終了演出の計算と設定は現在の実装を維持し、最終的な
  文字測定とラスタライズだけをSkiaへ置き換えた。
- 表示後のフェード、スライド、ズーム、ワイプ、ブラー、回転は、本文・ルビ・装飾・同期効果を含む
  Skia描画済み行レイヤー全体へ適用し、1行配置と文字自由配置の両方で動作する。
- 表示前のフェード、スライド、ズーム、ポップ、ワイプ、ブラー、回転、バウンドも同じ行レイヤーへ
  適用し、方向とズーム開始側をFilter項目から反映する。
- `前 時間`と`後 時間`は0.01～3.00秒、初期値0.30秒で各演出の速度を指定する。終了側の実効時間は
  同期中へ食い込まないよう`後 時間`と`表示維持`の短い方とする。
- 参考プロジェクトのSkia共通部を`Source\Lib\TextRenderer`へコピーし、Filterプロジェクトへの登録と
  `sk4d.dll`のビルド後配備まで完了した。共通部は太字・斜体・下線・取り消し線、正負の字間、
  グリフ単位の原点・送り幅・実描画矩形・独立画像を返せる。`SYNC_Lyrics_Renderer`は本文・ルビ、
  個別倍率、同期前後色のクリップ合成をSkia画像で処理し、旧GDI/DIB描画は削除済みである。

## 次の作業

1行配置と文字自由配置を同じウィンドウで切り替える新しい表示設定フォームを段階移行中である。
共通フォーム、拡張可能なモードページ基底、1行配置ページ、文字自由配置ページ、共通色パネルの
表示部分を追加した。構築中の`Resize`は全子コントロール生成後だけ実行する。Filterの`表示設定`は
新しい共通フォームを開き、同じフォーム内でモードページを切り替える。既存2フォームは処理移植元として
残すが、正式な表示経路からは外す。表示を実機で調整しながら、既存処理をモードページへ順次移植する。
共通フォームの基準フォントと最小サイズは96 DPI基準値から`CurrentPPI`で換算する。初期クライアント
サイズは96 DPI基準`990×650`とし、画面中央へ表示する。OK・キャンセルは置かず、閉じるアイコンと
タイトルバーの閉じる操作は両配置モードの全候補を一つの`SLD1`更新として保存する。上部の復元アイコンは
両モードの編集内容と候補位置をフォームを開いた時点へ戻し、画面は閉じない。
候補コンボはモード別の候補配列と選択位置を保持し、1行配置では配置1～3、文字自由配置では実際の
歌詞行候補をFilterの配置コンテキストから読み込む。モードを往復しても各選択位置を維持する。
1行配置ページは配置1～3の歌詞と共通設定を読み込み、候補ごとに編集中の値を保持する。プレビューの
本文・ルビ範囲をクリックして選び、各フォントと太字・斜体・下線・取り消し線、共通の縁取り・影を
編集できる。閉じる時は3配置を`SLD1`の曲共通配置へ保存する。背景付きプレビューでは実際の本文・ルビ
範囲を選択枠として表示し、本文のドラッグで行全体の位置、選択枠右下のドラッグで本文・ルビの文字サイズを
編集できる。選択枠左右中央の操作点で本文字間・ルビ字間、ルビ範囲のドラッグで本文との上下間隔も
編集でき、変更後の字間は矩形端で欠けないプレビュー描画へ反映する。移動、サイズ、字間、ルビ間隔の
操作箇所へホバーすると、操作方向に応じたカーソルへ切り替える。縁・影が有効なときは、縁幅、縁ぼかし、
影位置、影ぼかし、影の広がりを編集する操作点を表示し、
ドラッグ中は現在値を表示する。本文・ルビのプレビュー文字はSkia装飾画像として生成し、縁、縁ぼかし、
影、影ぼかし、広がり、同期前後の色・透明度を背景へアルファ合成する。次はルビを連結表示する暫定配置を
行う際は装飾なしの先頭グリフ原点を固定基準とし、縁幅・ぼかし・影で画像外周が広がっても文字本体の
位置を動かさない。ルビは連結せず、旧フォームと同じく各ルビを対応する本文範囲の中央へ個別配置し、
本文位置はルビ高さから独立して中央へ固定し、ルビサイズ変更ではルビだけを上方向へ伸ばす。配置1～3を
またいで編集した内容はページ内に保持し、閉じる時に3件すべてを一つの歌詞データ更新として保存する。
文字自由配置ページはFilterの実歌詞候補、`SL3`共通設定、保存済み配置要素を読み込み、通常文字1文字または
ルビ付き本文とルビの1グループを選択コンボと背景プレビューへ表示する。候補ごとの選択文字は候補を往復しても保持する。
プレビュークリックとコンボ選択は双方向に同期し、選択要素は黄色枠で表示する。Shiftクリックまたは右ドラッグで
複数要素を選択でき、選択集合の移動と、四辺・四隅の操作点による縦横変形を一体で行う。横倍率も文字自体へ
反映する。右ドラッグで範囲選択でき、選択済み要素の再クリックでは黄色の移動・変形、水色の本文字間、
紫のルビ操作を循環する。水色の左右点は本文字間、紫の左右点はルビ字間、紫の上中央点はルビ相対位置を
変更する。文字自由配置ページもカーソル位置固定のホイール拡大縮小0.25～8倍と、空白の左ドラッグによる
表示移動を持つ。上部の中央移動、選択初期化、全初期化、横一列、均等配置を接続した。閉じる時は
全歌詞候補の編集結果を個別`SL3`へ変換し、一つの`SLD1`更新として保存する。選択要素の本文・
ルビフォント、太字・斜体・下線・取り消し線、地色・縁色・影色・ぼかし色と同期前後透明度は複数選択、
候補保持、`SL3`保存へ接続済みである。個別の縁・影の有効状態と幅、ぼかし、位置、広がりも
上部の個別装飾設定から複数選択、候補保持、`SL3`保存へ接続済みである。次は自由配置の概要表示を
Skia装飾プレビューへ置き換える。動作確認用に、プレビューはカーソル位置を固定した
ホイール拡大縮小0.25～8倍と、空白部分の左ドラッグによる背景・歌詞一体の表示移動を持つ。同期前後の
地色・縁・影・縁ぼかし色と
各透明度は共通カラーパネルへ接続済みで、対象切替時に現在値を読み直し、変更を選択配置へ保持する。
Filterが最後に確保したRGBAフレームは新共通フォームを開く際に1回取得し、1行配置・文字自由配置の
両プレビューへ渡す。共通背景部品が黒背景と縦横比を維持した画像領域を描画する。表示設定の文字サイズは
画面DPIではなく元画像からプレビューへの縮尺で表示し、新規設定の初期値は本文64、ルビ28とする。

専用Inputを前提とする方式から、`FILTER_FLAG_VIDEO or FILTER_FLAG_FILTER`でタイムラインへ直接配置する
単体Filter方式へ変更した。Filterは共有メモリを読まず、自身のローカルフレームを使用する。
映像処理では`GetImageData`で渡された元画像を描画バッファへコピーし、その上へ歌詞を合成して返す。
歌詞が空または表示対象行がないフレームでも元画像を維持する。
同期編集画面の歌詞行をダブルクリックすると、その安定行IDをオブジェクト先頭行として`SLD1`へ保存する。
実行時は`max(0, 同期開始秒 - 現在の同期前表示秒)`を曲基準開始位置とし、事前表示や同期位置の変更へ
追従する。Filterの単体配置と途中行開始はAviUtl2実機で正常動作を確認済みである。

同期編集GUIはこの状態でいったん完成扱いとし、新しい要件が出るまで追加開発しない。
次は両配置編集画面のコンボの閉じた状態と展開状態、選択色を実機確認する。
初回歌詞入力では、親フォーム左上の右矢印アイコンで歌詞を確定して同期編集へ進む。全体操作は
左上へ共通アイコンツールバーとしてまとめ、閉じる、復元、同期確定、同期初期化をアイコン化した。
歌詞行の追加・削除・修正は左一覧の直上に表示設定と共通の`＋`・ゴミ箱・鉛筆アイコンで配置した。
歌詞一覧はプロジェクト内へ独立移植したインライン編集部品を使用し、追加では新しい行を挿入して空の
編集状態にする。鉛筆と`F2`も選択行を直接編集し、Enterまたはフォーカス移動で確定、Escで取り消す。
閉じるアイコンとタイトルバーの閉じる操作は編集内容を保存し、復元アイコンは
フォームを開いた時点の歌詞データへ復元する。`同期を初期化`も上部へ移し、埋め込み同期画面下部の
読み取り専用歌詞欄とボタン列を削除してピアノロール領域を広げた。
あわせて1行配置、文字自由配置、本文・ルビ、同期色境界、各アニメーションの表示結果を確認する。
Skia画像は現在フレームごとに生成しているが、実機再生で負荷は確認されなかったため、描画キャッシュは
必要性が生じるまで追加しない。

共通表示単位へ、Skiaで測定した本文・ルビの原点と矩形、単位基準点、グループ矩形、
全体の行矩形まで追加した。1行配置・文字自由配置とも同じ実行時モデルを生成し、
通常カラオケの同期進捗とルビ描画は共通単位の同期番号・矩形・文字列を使用している。
文字単位強調と1文字ずつ出現も、基本配置を書き換えない共通の一時演出状態として
両配置モードへ適用した。Filterの旧`タイプ`は配置モードと`同期演出`へ分離済みで、
1行配置・文字自由配置のどちらでも3種類の同期演出を選択できる。従来のバウンド項目は
`同期アニメーション`へ改名した。配置モードは両表示設定画面上部の切替アイコンで選択し、切替時に
現在の編集内容とともに`SLD1`へ曲共通保存して即時再描画し、もう一方の編集画面へ遷移する。
AviUtl2標準画面の独立項目は削除した。1行表示GUIの基本書式操作もFilter項目へ接続済みである。
1行表示GUIでは本文内の行全体移動、ルビ内の間隔補正、本文・ルビ各枠の四隅による
縦横比固定サイズ変更、左右点による字間変更を実装し、位置、両サイズ、両字間、
ルビ間隔補正をプレビューとFilter項目へ接続した。上部は編集対象、フォント、書式ツールバーだけへ
整理し、操作ヘルプ、歌詞入力、選択状態の各行を削除した。本文・ルビのフォントコンボを150px幅へ
縮め、書式アイコンを同じ行の右側へ配置したため、編集対象ありでは2段、なしでは1段まで上部を詰める。
1行表示GUIの上部に縁取り・影切替、右側に共通色対象列、同期前後2つの埋め込みカラーピッカーと
透明度を配置した。同期前後の地色、縁、影、縁ぼかし色と透明度、前後共通の縁幅・縁ぼかし、
影位置・影ぼかし・広がりを表示設定データとSkia描画へ接続済みである。装飾サイズはプレビュー上の
操作点で編集する。プレビューは常に歌詞中央を同期50%境界とし、左半分へ同期後、右半分へ同期前の
完成装飾を表示する。色やフォントなどの編集後も50%表示を維持する。次は実機で操作点の位置と
Skia実描画との見た目の差を調整する。プレビュー文字はセリフ表示と同じSkiaレンダラーを使用し、
太い縁・縁ぼかし・影を装飾済みRGBA画像として合成する。装飾操作点は現在値に応じて文字枠から外側へ移動し、ドラッグ中は
縁幅・ぼかし・影XY・広がりの数値を表示する。範囲外へ出る場合はプレビュー端へ収める。
ドラッグ時の再描画は文字自由配置GUIと同じフォームのダブルバッファを使用する。
文字自由配置GUIは上部の`行共通設定`アイコンから同じ1行表示設定画面を開き、個別配置を保持したまま
行共通のフォント、同期前後色、縁、影、ぼかしと各サイズを編集できる。自由配置プレビューも
実描画と同じSkia装飾画像を使用し、各配置要素の中央を同期50%境界として左に同期後、右に同期前を
表示する。個別の色・フォント・サイズ・字間と配置要素の縦横変形も反映する。
行共通の位置、フォント、装飾、色、サイズ、字間、ルビ間隔は`SL3`表示設定データへ統合し、
対応するFilter個別項目と旧フォント設定ボタンを削除した。配置モードと1行配置の配置1～3は`SLD1`の
曲共通部へ保存する。次はAviUtl2実機で配置1～3の選択、保存・再読込、同じ表示段の別歌詞行への
共通反映、新規オブジェクトの初期表示、配置モード切替後の画面と描画を確認する。

Filter標準画面は共通項目と`表示設定`の下へ、参照元セリフ表示と同じ`表示前`、`常時`、`同期`、
`表示後`グループを置く。全行共通の`事前表示`は`表示前`、`表示維持`は`表示後`グループ内に置く。
旧表示プリセットは廃止し、
保存・読込UI、ファイル処理、専用ユニットを削除する。同期アニメーションは参照元セリフ表示の
同期位置・色帯計算ユニットを専用Libへコピーして再利用し、色変え、前面、背面、下線、拡大、発光、
ジャンプを本文・ルビと両配置モードへ接続する。行全体を動かす旧ジャンプは使用しない。
同期の色変えは`色塗り`（`文字単位`／`なめらか`）と`通過後`（`戻す`／`維持`）の2軸を持ち、
4通りを描画へ反映する。既定値は従来動作を保つ`なめらか`＋`維持`とする。項目再構成と
色変え4モードを含むRelease版はプラグインフォルダーへ配置済み。`なめらか`＋`戻す`では
`同期 サイズ`を移動色帯の幅として使い、100%を約1表示単位幅とする。停止中は色帯を保持し、
次の単位へ進むと後端から通常色へ戻す。同期終了時の色帯は、最後の同期段階の継続時間を
0.15～0.60秒へ制限した時間で左から右へ通常色へ戻す。維持時間が短い場合はそこへ圧縮し、
復帰後の残り維持時間は通常色で表示する。取得不能時は0.30秒を使う。
ほかの3モードではサイズを色変えへ使用しない。
`同期 形`はセリフ表示と同じ自動・丸・四角・三角を持ち、形状を使う同期種類へ適用する。
同期進捗0の事前表示区間は現在文字なしとして扱い、先頭文字へ同期色や同期効果を先行適用しない。
拡大は本文下端を変形基準とし、文字とルビを上方向へ拡大して行の底辺を維持する。1行配置の本文と
ルビはそれぞれ共通ベースラインへ揃え、同期アニメーションが`なし`でも字形ごとのY位置をずらさない。
両修正を含むRelease版はプラグインフォルダーへ配置済み。

### Filter単体による曲全体編集方針

拡張プラグインは設けず、Filterプラグインだけで曲全体の歌詞入力、行管理、同期、配置、描画を行う。
通常は1曲につき1つの歌詞テロップオブジェクトを配置し、その内部に安定した行IDを持つ行データ配列を
保持する。各行は歌詞構文、表示段1～3、同期状態、表示・同期の開始終了時刻、同期データ、配置データを持つ。
同時表示は最大3行とし、表示段の標準配置を各行が継承し、必要な行だけ個別配置を上書きする。

- `同期`ボタン押下時に歌詞データがなければ、`TMemo`で曲全体を入力する初回画面を表示する。
- 歌詞確定後は同じ親フォーム内で初回入力Frameを隠し、左の歌詞行一覧と右の同期編集画面へ切り替える。
- 一覧はルビ構文解析後の読みやすい歌詞をオーナードローし、行追加・削除、`F2`による構文再編集を許可する。
- 表示段の初期値は1とし、数字キー1～3で選択行へ設定して次の行へ進める。
- 同期は未設定、仮設定、確定、不整合を区別し、前方を再編集しても後続の確定行を自動で動かさない。
- 配置画面は最後にFilterが処理したフレームから対象行を初期選択するが、左の歌詞一覧から常に
  別の行へ切り替えられるようにし、同期設定ミスによる編集不能を防ぐ。
- 既存の1行同期GUIと配置GUIは、再利用できる範囲を順次`TFrame`へ分離して新しい親画面へ組み込む。
- 全行の歌詞、同期、範囲、配置はFilter文字列項目`歌詞データ`の単一`SLD1`文書へ格納し、
  AviUtl2プロジェクトと一緒に保存・読込する。旧`歌詞`、旧`同期データ`、`SLS1`は使用しない。

初回歌詞入力用の`SYNC_Lyrics_InitialLyricsFrame`と親画面
`SYNC_Lyrics_SyncEditorForm`を追加した。`歌詞データ`が空のときに同期ボタンから開き、
デバッグ用のルビ付き5行を初期表示する。`歌詞を確定`すると入力Frameを隠して同期編集予定領域へ
切り替わる。曲全体の作業モデルと行ID、構文、解析後本文、ルビ、表示段、同期状態、同期文字列を持つ
行配列を追加し、
確定後は左側のオーナードロー一覧へ解析後本文を表示する。数字キー1～3で選択行の表示段を設定して
次行へ進める。既存の楽譜同期フォームを再利用する`TFrameLyricsMusicSyncEditor`を右側へ組み込み、
一覧選択または数字キーによる移動時に対象行の歌詞と同期データを読み替え、変更した行の内容を
作業モデルへ退避する。行別同期は未設定`[ ]`、仮設定`[~]`、確定`[C]`、不整合`[!]`を一覧へ表示し、
上部のボタンで選択行を確定または解除する。確定済み同期を再編集した場合は不整合へ移し、
再確定するまで確定扱いに戻さない。左上の`追加`・`削除`で行を管理し、`F2`で選択行の歌詞構文を
再編集する。追加・削除・編集の影響を受けない行IDと設定は維持し、同期済み行の構文変更は不整合へ移す。
曲全体モードの右側歌詞欄は読み取り専用とし、構文編集経路を`F2`へ統一する。

歌詞データは外部ファイルへ保存せず、Filterの文字列項目`歌詞データ`へバージョン1の単一行文書
`SLD1`として格納し、AviUtl2のプロジェクト保存・読込に委ねる。各行の行ID、構文、表示段、同期状態、
事前表示時間、表示開始・終了フレーム、同期開始・終了フレーム、同期文字列、行別配置文字列を含める。
Windows単行Editの上限に合わせて32,767文字以下に制限する。操作ボタンは画面左上へまとめ、
`SLD1`内のBase64項目はMIME折り返しを除去し、長い行別`SL3`を含む場合もCR/LFを一切含めない。
Filterで使用する`トラック`、`音楽オフセット`、`同期演出`は登録項目として保持し、
プロジェクト保存・読込の対象から外さない。
`閉じる`ボタンとタイトルバーの閉じる操作は現在行を退避してFilter文字列へ反映する。
取消終了は設けず、`編集開始前に戻す`でフォームを開いた時点の歌詞データへ復元して編集を継続する。
Filter描画側は`歌詞データ`の解析結果を最大16件の読み取り専用キャッシュへ保持し、オブジェクト内の
現在フレームから表示範囲に該当する行を選択して、行別の歌詞、表示段、同期データ、同期開始、
開始・終了アニメーション範囲を既存レンダラーへ渡す。範囲が1件も未設定なら先頭行を表示する。
編集画面にはFilterが最後に処理したオブジェクト内フレームと選択行の自動生成範囲を表示する。
手動範囲指定用だった`開始設定`と`終了設定`は廃止し、ピアノロールは曲全体で共通の同期基準を使用する。
各行は曲の有効音符列に対する開始音符番号を持ち、
初期状態では表示単位1つにつき1音を消費する。次行は前行が消費した直後の音から始め、前方行の
グループ対応を変更した場合は同期未編集の後続行だけを即時再計算し、一度でも編集した行は同期状態に
かかわらず開始音符番号を固定する。左一覧の`N1`等は各行の開始音符番号を1始まりで示す。
行選択時は割当先頭音を同期開始線へ
合わせ、同期開始からFilter共通の同期前表示秒を引いた位置を表示開始線とする。外部MIDI/WAVの同期時刻は
背景オブジェクトの絶対フレームではなく、歌詞テロップオブジェクト内の相対時刻を使用する。
同期画面のピアノロールには選択行の前後1行を各行の同期音符位置へ低コントラストで表示し、
同期対象やドラッグ判定には含めない。下部の選択行固定歌詞レーンは削除し、ピアノロールを下端まで使う。
曲全体の連続同期操作はこの段階で完成扱いとする。同じフレームで表示範囲が重なる行は表示段1～3を
段ごとに解決し、段1・2・3を上・中央・下の固定位置へ最大3行同時描画する。異なる段の行は
共通RGBAバッファへ合成し、行ごとの描画で先に描いた行を消去しない。同じ表示段で前行が消える前に
次行を表示するデータはサポート対象外とする。

### 行表示時間

各行の同期音符から表示範囲を自動生成する。Filter標準画面の`事前表示`と
`表示維持`を全行へ共通適用し、どちらも秒単位、初期値0.5秒、範囲0～60秒、変更単位0.01秒とする。
保存済み行の同期開始・終了範囲を基準に、現在の2設定から実行時の表示開始・終了範囲を再生成するため、
設定変更は同期画面を開き直さず再生へ反映される。

- `同期開始`は、その行へ割り当てられた最初の音符の開始位置とする。
- `同期終了`は、その行が同期グループによって消費する最後の音符の終了位置とする。
- 実描画は保存済み`同期開始`より前を未消費、`同期終了`以後を全消費として固定し、
  範囲内だけ同期元データから進捗を解決する。
- `表示開始 = 同期開始 - 同期前表示時間`とし、オブジェクト先頭より前は0へ制限する。
- `表示終了 = 同期終了 + 同期後維持時間`とし、この位置を越えた行は実描画しない。
- ピアノロールには既存の表示開始線・同期開始線に加えて、橙色の表示終了線を描画する。
- 同期グループ、開始音符番号、同期前表示時間、同期後維持時間、音楽ファイル、トラックが変わった場合は、
  対象行と同期未編集の後続行について同期開始・終了、表示開始・終了フレームを再計算する。
- 同じ表示段では次行の表示開始を前行の表示終了後に置き、表示範囲を重ねない。
- 音数不足で同期終了を求められない行は範囲未確定として警告し、先頭行フォールバックには戻さない。

同期開始・終了と範囲生成時の表示開始・終了はオブジェクト内フレームとして歌詞データへ保持する。
実描画では同期開始・終了を音楽オフセットで補正後、現在の同期前表示・同期後維持から表示範囲だけを
軽量に再生成して範囲判定する。全行範囲の自動生成と表示終了線、表示終了後の非表示は
同期画面は歌詞入力、行管理、同期設定だけで操作を完結させ、配置GUIを開くボタンは置かない。
配置は同期完了後にFilter標準の`表示設定`から行う。曲全体モードでは現在フレームの表示範囲に重なる
全行を配置画面上部のコンボへ出し、同期範囲内の行、次いで同期範囲までの距離が近い行を初期選択する。
文字自由配置の確定時は選択行の`PlacementText`だけを保存する。次はAviUtl2実機で重複区間の候補、
初期選択、行別保存・再読込、実描画一致に加えて、
同期前表示・同期後維持の設定変更が全行へ即時反映されることを確認する。

Filterの`トラック`直下に`音楽オフセット (秒)`を持つ。初期値0、範囲-5～+5秒、単位0.01秒とし、
正値で音楽データをオブジェクト時間上の後ろへ移動する。範囲自動生成、同期GUI、実描画へ共通適用し、
先頭0秒から発音するデータでも正のオフセット分を事前表示として確保できる。同期済み範囲には生成時の
オフセットを保存し、Filter上の現在値との差分を描画時に適用するため、値変更は直ちに表示へ反映される。
